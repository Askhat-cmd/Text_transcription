# ОБНОВЛЕНИЕ Части 4 мастер-плана после PRD-047.42-APPLY-17 (v4.20 → v4.21)

**Файл:** `docs/MASTER_STRATEGIC_PLAN_NEO_MindBot_v4_RU.md`
**Дата:** 2026-07-16
**Подготовил:** архитектор, после независимой верификации APPLY-17
(коммиты 364aff99 / c19e4dc5).

---

## ИНСТРУКЦИЯ ДЛЯ ИСПОЛНИТЕЛЯ — строго ТРИ отдельных шага

```text
Шаг 1. В шапке документа (строка ~7) заменить "v4.20" на "v4.21"
  в строке "**Версия:** ...". Остальной текст шапки не менять.

Шаг 2 (БЛОК А). Заменить тело Части 4 — весь текст от строки
  "# ЧАСТЬ 4 — ГДЕ МЫ СЕЙЧАС  ← ЕДИНСТВЕННЫЙ ОБНОВЛЯЕМЫЙ РАЗДЕЛ"
  (включительно) до строки
  "Журнал правок (новая строка поверх существующих):"
  (НЕ включая её) — на текст Блока А ниже.
  САМ ЖУРНАЛ (всё после этой строки, до начала ЧАСТИ 5) НЕ ТРОГАТЬ.

Шаг 3 (БЛОК Б). Вставить ОДНУ новую запись журнала (Блок Б ниже)
  сразу ПОСЛЕ строки "Журнал правок (новая строка поверх существующих):"
  и пустой строки — ПЕРЕД самой верхней существующей записью
  (сейчас "- 2026-07-16 v4.19 -> v4.20: ..."). Существующие записи
  НЕ удалять, НЕ переформатировать, НЕ сокращать.

Коммит: "Update master plan part 4 after PRD-047.42-APPLY-17 (v4.21)".
Один коммит на все три шага. Перед пушем ОБЯЗАТЕЛЬНО: git diff и
глазами убедиться, что из журнала не исчезло НИ ОДНОЙ старой записи.
```

---

## БЛОК А — новое тело Части 4 (без журнала)

```markdown
# ЧАСТЬ 4 — ГДЕ МЫ СЕЙЧАС  ← ЕДИНСТВЕННЫЙ ОБНОВЛЯЕМЫЙ РАЗДЕЛ

Эпоха: 1 (Консолидация).
Последний принятый PRD: PRD-047.42-APPLY-17 (ACCEPTED, commits
  364aff99 / c19e4dc5). Верифицировано архитектором независимо через
  GitHub: пересчёт snapshot из живого кода, SHA1 user_prompt по всем
  трём сценариям (цепочка не менялась с APPLY-15), 122 passed по всем
  контрактным тестам серии + прямым тестам slice10, канонический
  широкий прогон — ровно 19 падений, список идентичен эталону APPLY-16.

Статус декомпозиции _call_llm: серия format() (APPLY-11..16) закрыта;
  из 4 пост-format кластеров закрыт 1 из 4
  (prompt_constraint_append_and_debug_bookkeeping -> slice10).
  Хвост _call_llm сжался до ~47 строк (writer_agent.py = 1464).

Следующий PRD: PRD-047.42-APPLY-18 — кластер
  runtime_settings_and_system_prompt_selection (slice11).
  Границы на конец сессии: строки 548-556 (9 строк; start_ts на 547
  НЕ входит — это таймер диспатча). Пересчитать перед стартом.
  Особенности кластера: третья namesake-история с dialogue_profile
  (переприсваивание через normalize_dialogue_profile) и первая
  self-зависимость через вызов метода _resolve_runtime_settings
  (определён в защищённом writer_agent_lifecycle_mixin.py) —
  передаётся в helper как callable.
  Затем по одному PRD: provider_dispatch (сейчас 557-567),
  response_unpack_cost_and_return (сейчас 568-593).
Шлюз в Эпоху 2: НЕ пройден. Работаем по Сценарию А
  (владелец-решение #2 от 2026-07-14).

Корпус законов Fable: получен полностью (R01-R07), НЕ ратифицирован.

ВЛАДЕЛЕЦ-РЕШЕНИЕ (2026-07-13, действует): 4 файла — thread_manager.py
  (1482), orchestrator.py (1481), api/debug_routes.py (1380),
  writer_context_package.py (1376) — превышают порог DoD §5.6
  (~1200 строк), но их декомпозиция сознательно отложена владельцем на
  ОТДЕЛЬНЫЙ трек ПОСЛЕ реализации Эпохи 2 "умности" — не пересматривать
  без явного нового решения владельца.

ВЛАДЕЛЕЦ-РЕШЕНИЕ #2 (2026-07-14, действует): цель текущей рабочей
  линии — Сценарий А (полностью привести в порядок writer_agent.py:
  оставшиеся кластеры _call_llm, затем картирование и, вероятно,
  декомпозиция _enforce_answer_compliance (610 строк) и
  _enforce_mvp_free_dialogue_compliance (225 строк)). Буквальный DoD
  §5.6 в конце этой линии закрыт НЕ будет — расхождение сознательное,
  зафиксированное, не blocker.

PRD-047.42-APPLY-17 закрыт (Stage 3a, первый пост-format кластер):
- кластер prompt_constraint_append_and_debug_bookkeeping (48 строк,
  границы 531-578) вынесен в writer_agent_call_llm_slice10.py;
  механика (c) + last_debug_patch (18 ключей одним update, порядок
  исходных присваиваний сохранён — влияет на байты snapshot,
  json.dumps без sort_keys);
- сохранены обе критичные семантики: условное дописывание user_prompt
  и исходная асимметрия "is not None" vs "isinstance(dict)"
  (перенесена дословно, не "исправлена");
- закрыта дыра покрытия: snapshot-харнесс не задевает ветку
  дописывания (во всех 3 сценариях decision=None) — добавлены прямые
  тесты обеих веток + тест эталонного порядка 18 ключей + тест
  асимметрии (инициатива Исполнителя, принята);
- импорт format_prompt_constraint_section_v1 удалён из writer_agent.py
  после zero-match grep (подтверждено архитектором независимо);
- Исполнитель расширил no_mutation_proof до 25 файлов — принято как
  ужесточение; канонический список архитектора — 15 (см. ниже);
- цифры владельческого окружения ("14 failed") по критерию PRD не
  считаются — только канонический изолированный прогон (19, идентично).

Защищённые файлы (канонический список архитектора): 14 из transfer
  brief v3 + writer_agent_call_llm_slice9.py
  (f69c7217137ddc23ea1c09e7ba6dd9a1886d0249)
  + writer_agent_call_llm_slice10.py
  (hash зафиксировать при приёмке APPLY-18) = 16 после APPLY-18.

Отложено сознательно (не потеряно):
- provider_dispatch и response_unpack_cost_and_return — по одному PRD
  после APPLY-18, границы пересчитывать.
- core_required_fields (10 обязательных ctx[...] без дефолтов) — вне
  серии, отдельное решение после кластеров.
- _enforce_answer_compliance (610 строк) и _enforce_mvp_free_dialogue_
  compliance (225 строк) — сначала boundary mapping отдельным PRD.
- thread_manager.py, orchestrator.py, api/debug_routes.py,
  writer_context_package.py — отдельный трек, после Эпохи 2.
- writer_contract.py (979 строк) — под порогом ~1200, отдельное решение.
- diagnostic_center_* файлов (по факту не менее 27, docs говорят 19) ->
  PRD-047.42b, инвентаризация/классификация dead_confirmed.
- LEGACY_PIPELINE_ENABLED -> PRD-047.43.
- ~19-128 collection/environment-зависимых падений -> PRD-047.45.
- docs/testing.md, S7, Panic/Medical Escalation, трейс-поля — без изменений.
```

---

## БЛОК Б — одна новая запись журнала (вставить ПОВЕРХ существующих)

```markdown
- 2026-07-16 v4.20 -> v4.21: PRD-047.42-APPLY-17 принят ACCEPTED.
  Первый пост-format кластер (prompt_constraint_append_and_debug_
  bookkeeping -> slice10, 48 строк). Сохранены дописывание user_prompt
  и асимметрия is-not-None/isinstance; порядок 18 ключей last_debug
  зафиксирован тестом (влияет на байты snapshot). Закрыта дыра
  покрытия: ветка дописывания не задевается snapshot-харнессом —
  добавлены прямые тесты обеих веток. Осталось 3 кластера _call_llm;
  следующий — runtime_settings_and_system_prompt_selection (APPLY-18).
```
