# ОБНОВЛЕНИЕ Части 4 мастер-плана после PRD-047.42-APPLY-18 (v4.21 → v4.22)

**Файл:** `docs/MASTER_STRATEGIC_PLAN_NEO_MindBot_v4_RU.md`
**Дата:** 2026-07-16
**Подготовил:** архитектор, после независимой верификации APPLY-18
(коммиты bac7e304 / 076d916f / 80864b7e) и владелец-решения #3.

---

## ИНСТРУКЦИЯ ДЛЯ ИСПОЛНИТЕЛЯ — строго ТРИ отдельных шага

```text
Шаг 1. В шапке документа (строка ~7) заменить "v4.21" на "v4.22"
  в строке "**Версия:** ...". Остальной текст шапки не менять.

Шаг 2 (БЛОК А). Заменить тело Части 4 — весь текст от строки
  "# ЧАСТЬ 4 — ГДЕ МЫ СЕЙЧАС  ← ЕДИНСТВЕННЫЙ ОБНОВЛЯЕМЫЙ РАЗДЕЛ"
  (включительно) до строки
  "Журнал правок (новая строка поверх существующих):"
  (НЕ включая её) — на текст Блока А ниже.
  САМ ЖУРНАЛ (всё после этой строки, до начала ЧАСТИ 5) НЕ ТРОГАТЬ.

Шаг 3 (БЛОК Б). Вставить ОДНУ новую запись журнала (Блок Б ниже)
  сразу ПОСЛЕ строки "Журнал правок (новая строка поверх существующих):"
  и пустой строки — ПЕРЕД самой верхней существующей записью
  (сейчас "- 2026-07-16 v4.20 -> v4.21: ..."). Существующие записи
  НЕ удалять, НЕ переформатировать, НЕ сокращать.

Коммит: "Update master plan part 4 after PRD-047.42-APPLY-18 (v4.22)".
Один коммит на все три шага. Перед пушем ОБЯЗАТЕЛЬНО: git diff и
глазами убедиться, что из журнала не исчезло НИ ОДНОЙ старой записи.
```

---

## БЛОК А — новое тело Части 4 (без журнала)

```markdown
# ЧАСТЬ 4 — ГДЕ МЫ СЕЙЧАС  ← ЕДИНСТВЕННЫЙ ОБНОВЛЯЕМЫЙ РАЗДЕЛ

Эпоха: 1 (Консолидация).
Последний принятый PRD: PRD-047.42-APPLY-18 (ACCEPTED, commits
  076d916f / 80864b7e; обновление плана до v4.21 — bac7e304).
  Верифицировано архитектором независимо через GitHub: пересчёт
  snapshot из живого кода, SHA1 user_prompt по всем трём сценариям
  (цепочка не менялась с APPLY-15), 128 passed по контрактным тестам
  серии + прямым тестам slice10/slice11, канонический широкий прогон —
  ровно 19 падений, список идентичен эталону, целостность журнала
  Части 4 подтверждена diff'ом.

ВЛАДЕЛЕЦ-РЕШЕНИЕ #3 (2026-07-16, действует): кластер provider_dispatch
  (единственный await create_agent_completion, ~11 строк одного
  оператора) НЕ выносится в отдельный модуль — остаётся в _call_llm.
  Основание: вынос одиночного вызова — структура без пользы, нарушает
  принцип "не добавлять структуру без письменного условия входа".
  Кластер закрыт решением "остаётся на месте". Вместо двух PRD —
  ОДИН финальный PRD-047.42-APPLY-19 (Вариант Б, выбран владельцем
  явно).

Статус декомпозиции _call_llm: серия format() (APPLY-11..16) закрыта;
  пост-format кластеры: prompt_constraint (slice10) — закрыт,
  runtime_settings_and_system_prompt (slice11) — закрыт,
  provider_dispatch — закрыт решением "остаётся на месте" (см.
  владелец-решение #3), response_unpack_cost_and_return — последний.
  writer_agent.py = 1465 строк.

Следующий PRD: PRD-047.42-APPLY-19 — ПОСЛЕДНИЙ кластер _call_llm:
  response_unpack_cost_and_return (slice12). Границы на конец сессии:
  строки 569-593 (25 строк; await 558-568 и return 594 НЕ входят).
  Пересчитать перед стартом. Особенности: вторая self-зависимость
  (_estimate_cost из защищённого writer_agent_fallback_state_mixin.py —
  callable'ом), тайминг duration_ms через time.perf_counter()
  (харнесс детерминирует его подменой атрибута ОБЩЕГО модуля time —
  helper обязан использовать "import time" + "time.perf_counter()",
  НЕ "from time import perf_counter"), 13-ключевой last_debug_patch.
  После приёмки APPLY-19 метод _call_llm считается ГОТОВЫМ.
Шлюз в Эпоху 2: НЕ пройден. Работаем по Сценарию А
  (владелец-решение #2 от 2026-07-14).

Корпус законов Fable: получен полностью (R01-R07), НЕ ратифицирован.

ВЛАДЕЛЕЦ-РЕШЕНИЕ (2026-07-13, действует): 4 файла — thread_manager.py
  (1482), orchestrator.py (1481), api/debug_routes.py (1380),
  writer_context_package.py (1376) — превышают порог DoD §5.6
  (~1200 строк), но их декомпозиция сознательно отложена владельцем на
  ОТДЕЛЬНЫЙ трек ПОСЛЕ реализации Эпохи 2 "умности" — не пересматривать
  без явного нового решения владельца.

ВЛАДЕЛЕЦ-РЕШЕНИЕ #2 (2026-07-14, действует): цель текущей рабочей
  линии — Сценарий А (полностью привести в порядок writer_agent.py:
  последний кластер _call_llm, затем картирование и, вероятно,
  декомпозиция _enforce_answer_compliance (610 строк) и
  _enforce_mvp_free_dialogue_compliance (225 строк)). Буквальный DoD
  §5.6 в конце этой линии закрыт НЕ будет — расхождение сознательное,
  зафиксированное, не blocker.

PRD-047.42-APPLY-18 закрыт (Stage 3b, второй пост-format кластер):
- кластер runtime_settings_and_system_prompt_selection (9 строк,
  границы 548-556) вынесен в writer_agent_call_llm_slice11.py;
- третья namesake-история dialogue_profile (переприсваивание) решена
  корректно: параметр внутрь + перепривязка снаружи; сводка всех трёх
  случаев зафиксирована в PRD APPLY-18 раздел 1;
- первая self-зависимость (_resolve_runtime_settings из защищённого
  lifecycle mixin) передана bound-callable'ом, mixin не тронут
  (hash-proof подтверждён архитектором);
- импорт-гигиена: WRITER_SYSTEM / WRITER_SYSTEM_MVP_FREE_DIALOGUE
  удалены после zero-match grep (перепроверено архитектором),
  WRITER_USER_TEMPLATE и импорты dialogue_policy на месте;
- прямые тесты фиксируют семантику переприсваивания, выбор обоих
  system_prompt и единственный keyword-вызов callable'а.

Защищённые файлы (канонический список архитектора) — 16:
  14 из transfer brief v3
  + writer_agent_call_llm_slice9.py
    (f69c7217137ddc23ea1c09e7ba6dd9a1886d0249)
  + writer_agent_call_llm_slice10.py
    (dd34d823f61c608c3bd8a2be0fd48763cec2c838).
  После приёмки APPLY-19: + slice11 (hash зафиксировать) = 17.

Отложено сознательно (не потеряно):
- core_required_fields (10 обязательных ctx[...] без дефолтов) — вне
  серии, отдельное решение после APPLY-19.
- Boundary mapping _enforce_answer_compliance (610 строк) и
  _enforce_mvp_free_dialogue_compliance (225 строк) — следующий шаг
  Сценария А после APPLY-19.
- thread_manager.py, orchestrator.py, api/debug_routes.py,
  writer_context_package.py — отдельный трек, после Эпохи 2.
- writer_contract.py (979 строк) — под порогом ~1200, отдельное решение.
- diagnostic_center_* файлов (по факту не менее 27, docs говорят 19) ->
  PRD-047.42b, инвентаризация/классификация dead_confirmed.
- LEGACY_PIPELINE_ENABLED -> PRD-047.43.
- ~19-128 collection/environment-зависимых падений -> PRD-047.45.
- docs/testing.md, S7, Panic/Medical Escalation, трейс-поля — без изменений.
```

---

## БЛОК Б — одна новая запись журнала (вставить ПОВЕРХ существующих)

```markdown
- 2026-07-16 v4.21 -> v4.22: PRD-047.42-APPLY-18 принят ACCEPTED
  (runtime_settings_and_system_prompt -> slice11; третий случай
  namesake dialogue_profile и первая self-зависимость решены чисто).
  ВЛАДЕЛЕЦ-РЕШЕНИЕ #3: provider_dispatch (одиночный await) НЕ
  выносится — остаётся в _call_llm; вместо двух PRD один финальный
  APPLY-19 (response_unpack_cost_and_return -> slice12). После его
  приёмки _call_llm считается готовым.
```
