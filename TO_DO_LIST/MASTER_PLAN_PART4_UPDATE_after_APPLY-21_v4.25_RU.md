# ОБНОВЛЕНИЕ Части 4 мастер-плана после PRD-047.42-APPLY-21 (v4.24 → v4.25)

**Файл:** `docs/MASTER_STRATEGIC_PLAN_NEO_MindBot_v4_RU.md`
**Дата:** 2026-07-16
**Подготовил:** архитектор, после независимой верификации APPLY-21
(коммиты ab7ec529 / 37a2c2f5 / f730754e) и находки при разведке
следующего среза.

---

## ИНСТРУКЦИЯ ДЛЯ ИСПОЛНИТЕЛЯ — строго ТРИ отдельных шага

```text
Шаг 1. В шапке документа заменить "v4.24" на "v4.25".
Шаг 2 (БЛОК А). Заменить тело Части 4 — от строки
  "# ЧАСТЬ 4 — ГДЕ МЫ СЕЙЧАС  ← ЕДИНСТВЕННЫЙ ОБНОВЛЯЕМЫЙ РАЗДЕЛ"
  (включительно) до строки
  "Журнал правок (новая строка поверх существующих):" (НЕ включая) —
  на Блок А ниже. ЖУРНАЛ НЕ ТРОГАТЬ.
Шаг 3 (БЛОК Б). Вставить ОДНУ новую запись журнала (Блок Б) поверх
  существующих. Существующие записи не удалять, не переформатировать.
Коммит: "Update master plan part 4 after PRD-047.42-APPLY-21 (v4.25)".
Перед пушем ОБЯЗАТЕЛЬНО: git diff + глазами убедиться, что из журнала
не исчезло НИ ОДНОЙ старой записи.
```

---

## БЛОК А — новое тело Части 4 (без журнала)

```markdown
# ЧАСТЬ 4 — ГДЕ МЫ СЕЙЧАС  ← ЕДИНСТВЕННЫЙ ОБНОВЛЯЕМЫЙ РАЗДЕЛ

Эпоха: 1 (Консолидация).
Последний принятый PRD: PRD-047.42-APPLY-21 (ACCEPTED, commits
  37a2c2f5 / f730754e; обновление плана до v4.24 — ab7ec529).
  Верифицировано архитектором независимо через GitHub: построчная
  сверка helper'а с оригиналом строк 587-706 (побайтовое совпадение
  выражений и порядка last_debug-записей); все 44 поля распакованы в
  writer_agent.py в исходном порядке; собственный пересчёт before/
  after снапшотов через build_before_snapshot()/build_after_snapshot()
  из живого кода (реконструкция historical-класса из git show ab7ec52
  — приём, годный для будущих PRD этой линии) — идентичны
  закоммиченным и друг другу, метка совпадает с константой; 132
  passed; канонический прогон — ровно 19, список идентичен эталону;
  18 защищённых файлов (список ниже) не изменились.

⚠️ НАХОДКА АРХИТЕКТОРА (важно для планирования следующих срезов):
  карта APPLY-20 занизила ширину семьи 1 (intake_and_obligation_
  prelude, заявлено R01-R03). Между R02 (707-711) и R03 (767-773)
  находится ВТОРАЯ волна из ~16 локальных переменных и 6 записей
  last_debug (answer_obligation, last_direct_question,
  last_offer_summary, offer_repair_context, concept_question,
  has_unsolicited_practice, has_question, asks_define_known_term,
  has_external_surveillance_frame, user_requests_no_question,
  user_requests_no_practice, user_repair_signal, user_step_request,
  canned_step_disallowed и др.) — эта подготовка карте не была видна
  отдельной строкой, "утонула" в ширине R03. R03 физически ВЛОЖЕН
  внутрь этой подготовки (не после неё), с третьей и четвёртой
  self-зависимостями (_set_final_answer_shape_debug,
  _build_gentle_close_reply). Механика простого dataclass-извлечения
  (mechanic c), использованная в slice1 прелюдии и во всей линии
  _call_llm, здесь НЕ подходит: нужна механика "извлечь-и-возможно-
  вернуть" (mechanic d, новая для проекта) — helper возвращает
  Optional[текст-раннего-возврата] + локальные (валидны только если
  раннего возврата не было) + last_debug_patch.
  Дополнительно: три маркерных tuple'а (_PRACTICE_MARKERS,
  _KNOWN_CONCEPT_CLARIFICATION_MARKERS, _EXTERNAL_SURVEILLANCE_
  MARKERS) определены МОДУЛЬНО внутри writer_agent.py (не в
  writer_agent_constants.py) — прямой импорт helper'ом из
  writer_agent.py создаст циклический импорт. Решение (закон З-3 —
  не плодить новую структуру): маркеры передаются в helper
  параметрами, НЕ переносятся в отдельный файл.
  Это не блокер и не откат APPLY-21 — прелюдия (slice1) принята и
  корректна для своего диапазона. Находка лишь уточняет объём и
  механику следующего PRD.

Следующий PRD: PRD-047.42-APPLY-22 — "вторая прелюдия + R03" одним
  срезом (границы пересчитать по свежему HEAD перед стартом; на
  момент этой находки — ориентировочно ~62 строки, начало сразу
  после R02, конец сразу после R03). Механика (d): helper возвращает
  frozen dataclass с полями early_return_text: str | None,
  last_debug_patch: dict, плюс локальные, нужные правилам R04+
  (валидны только когда early_return_text is None). Вызывающий код:
  если early_return_text is not None -> self.last_debug.update(...) и
  return early_return_text; иначе -> update(...) и распаковка
  локальных, поток продолжается в R04. Три self-метода
  (_set_final_answer_shape_debug х2 использования внутри среза,
  _build_gentle_close_reply) и три маркерных tuple'а — параметрами.
  Обязательное требование к тестам: явное покрытие ОБЕИХ ветвей R03
  (early_return_text is not None И is None) — это первая ветвящаяся
  механика в проекте, ловушка здесь наиболее вероятна.
Дальше по Сценарию А: следующие семьи по карте (Секция C, с учётом
  того, что реальные границы других семей тоже могут быть занижены
  картой аналогично находке этого PRD — при разведке каждой следующей
  семьи закладывать время на перепроверку истинных границ, а не
  слепо доверять диапазонам из APPLY-20). Вопрос расширения покрытия
  харнесса (сейчас 22/75) — решение архитектора по факту разведки
  каждой конкретной семьи, не общее заранее.
Шлюз в Эпоху 2: облегчённый (владелец-решение #4). Ориентир до шлюза:
  ~7-11 PRD (пересчитан вверх на 1 из-за находки этого PRD).

Корпус законов Fable: получен полностью (R01-R07), НЕ ратифицирован.

ВЛАДЕЛЕЦ-РЕШЕНИЕ (2026-07-13, действует): 4 больших файла — трек
  после Эпохи 2.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #2 (2026-07-14, действует): Сценарий А, DoD §5.6
  буквально не закрывается — сознательно, не blocker.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #3 (2026-07-16, исполнено APPLY-19): await
  create_agent_completion остаётся в _call_llm.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #4 (2026-07-16, действует): облегчённый шлюз Эпохи
  2 — только Сценарий А; PRD-047.42b/43/45 — отдельный трек после
  Эпохи 2.

PRD-047.42-APPLY-21 закрыт (первый срез декомпозиции
  _enforce_answer_compliance, прелюдия):
- строки 587-706 (44 локальных + патч last_debug) вынесены в
  writer_agent_enforce_slice1.py; text (583) и R01 (584-585) остались
  в методе; R02 (707-711) остался сразу после вызова — граница точная;
- порядок 44 полей датакласса и порядок распаковки в writer_agent.py
  совпадают с порядком присваиваний оригинала; last_debug-записи,
  перемежающиеся с локальными, воспроизведены в тех же точках потока;
- прямые тесты фиксируют порядок полей по Секции A карты и
  представительную выборку значений + порядок ключей патча;
- Snapshot-гигиена сработала второй раз подряд (метка == константе).

Защищённые файлы (канонический список архитектора) — 18:
  14 из transfer brief v3
  + slice9  (f69c7217137ddc23ea1c09e7ba6dd9a1886d0249)
  + slice10 (dd34d823f61c608c3bd8a2be0fd48763cec2c838)
  + slice11 (fa776248a89124c8b34d7d94134a448bee9a3cdc)
  + slice12 (8a9544b784f16bb3b3968d2c787964b684e85838).
  После приёмки APPLY-22: + writer_agent_enforce_slice1.py (hash
  зафиксировать из HEAD f730754e) + writer_agent_enforce_slice2.py
  (новый) = 20.

Отложено сознательно (не потеряно):
- Семьи R04+ _enforce_answer_compliance — после APPLY-22, границы
  пересчитывать перед каждой, с поправкой на возможное занижение картой.
- _enforce_mvp_free_dialogue_compliance (225 строк) — после.
- core_required_fields — вне серии, отдельное решение архитектора.
- 4 больших файла; PRD-047.42b; PRD-047.43; PRD-047.45 — отдельный
  трек ПОСЛЕ Эпохи 2 (владелец-решения 2026-07-13 и #4).
- writer_contract.py (979 строк) — под порогом, отдельное решение.
- docs/testing.md, S7, Panic/Medical Escalation, трейс-поля — без изменений.
```

---

## БЛОК Б — одна новая запись журнала (вставить ПОВЕРХ существующих)

```markdown
- 2026-07-16 v4.24 -> v4.25: PRD-047.42-APPLY-21 принят ACCEPTED —
  прелюдия _enforce_answer_compliance вынесена (44 локальных,
  slice1). НАХОДКА: карта APPLY-20 занизила ширину семьи 1 — между
  R02 и R03 обнаружена вторая волна из ~16 локальных с R03,
  физически вложенным внутрь неё, и двумя дополнительными self-
  зависимостями. Требуется новая механика (d) — "извлечь-и-возможно-
  вернуть" — впервые в проекте. Следующий шаг — APPLY-22, границы
  пересчитать заново перед стартом.
```
