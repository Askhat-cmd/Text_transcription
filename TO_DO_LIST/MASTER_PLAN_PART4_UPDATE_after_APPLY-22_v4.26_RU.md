# ОБНОВЛЕНИЕ Части 4 мастер-плана после PRD-047.42-APPLY-22 (v4.25 → v4.26)

**Файл:** `docs/MASTER_STRATEGIC_PLAN_NEO_MindBot_v4_RU.md`
**Дата:** 2026-07-16
**Подготовил:** архитектор, после независимой верификации APPLY-22
(коммиты c488e838 / c01b96cd / ff9489c2) и разведки семьи 2.

---

## ИНСТРУКЦИЯ ДЛЯ ИСПОЛНИТЕЛЯ — строго ТРИ отдельных шага

```text
Шаг 1. В шапке документа заменить "v4.25" на "v4.26".
Шаг 2 (БЛОК А). Заменить тело Части 4 — от строки
  "# ЧАСТЬ 4 — ГДЕ МЫ СЕЙЧАС  ← ЕДИНСТВЕННЫЙ ОБНОВЛЯЕМЫЙ РАЗДЕЛ"
  (включительно) до строки
  "Журнал правок (новая строка поверх существующих):" (НЕ включая) —
  на Блок А ниже. ЖУРНАЛ НЕ ТРОГАТЬ.
Шаг 3 (БЛОК Б). Вставить ОДНУ новую запись журнала (Блок Б) поверх
  существующих. Существующие записи не удалять, не переформатировать.
Коммит: "Update master plan part 4 after PRD-047.42-APPLY-22 (v4.26)".
Перед пушем ОБЯЗАТЕЛЬНО: git diff + глазами убедиться, что из журнала
не исчезло НИ ОДНОЙ старой записи.
```

---

## БЛОК А — новое тело Части 4 (без журнала)

```markdown
# ЧАСТЬ 4 — ГДЕ МЫ СЕЙЧАС  ← ЕДИНСТВЕННЫЙ ОБНОВЛЯЕМЫЙ РАЗДЕЛ

Эпоха: 1 (Консолидация).
Последний принятый PRD: PRD-047.42-APPLY-22 (ACCEPTED, commits
  c01b96cd / ff9489c2; план до v4.25 — c488e838). Верифицировано
  архитектором независимо: построчная сверка helper'а с оригиналом;
  ЛОВУШКА ПОРЯДКА (Особенность 1 PRD) лично воспроизведена архитектором
  прямой симуляцией true-ветки на реальном self.last_debug —
  final_answer_shape подтверждён 6-м ключом, не 1-м; собственный
  пересчёт before/after снапшотов идентичен и детерминирован, метка
  верна (третий раз подряд); 140 passed; канонический прогон — ровно
  19, список идентичен эталону; 19 защищённых файлов (список ниже) не
  тронуты; логи APPLY-20/21 неприкосновенны.

Семья 1 (intake_and_obligation_prelude, R01-R03) ПОЛНОСТЬЮ ЗАКРЫТА:
  text/R01 инлайн, slice1 (прелюдия, 44 локальных), R02 инлайн, slice2
  (вторая прелюдия + R03, механика (d) впервые).

РАЗВЕДКА СЕМЬИ 2 (архитектор, до выдачи PRD): хорошая новость — семья
  2 (obligation_specific_repairs_before_profile_split) оказалась ЧИЩЕ
  семьи 1: строки 666-753 (88 строк, современная нумерация) — это
  каскад из 6 самодостаточных if/return правил (R04 provide_one_
  bounded_practice с 2 вложенными подусловиями, R07/R08 literal_
  markdown_echo, R09-R16 — 4 обязательство-специфичных repair-правила)
  БЕЗ общей подготовки локальных между правилами (в отличие от находки
  APPLY-21). Семья 3 (mvp_free_branch_handoff) — единственный
  делегирующий self-вызов _enforce_mvp_free_dialogue_compliance(...)
  на строке 754; кандидат на решение "остаётся на месте" по аналогии
  с provider_dispatch (владелец-решение #3) — решение архитектора при
  подходе к этой границе, не блокирует семью 2.
  Внутри R04 использованы self._set_final_answer_shape_debug,
  self._defer_no_stub_repair, self._strip_optional_followup_invitation
  — но ВСЕ в пределах ОДНОГО самодостаточного правила без чужой
  последующей записи в last_debug, поэтому ловушка Особенности 1
  здесь не действует ТАК же остро, как в APPLY-22 — тем не менее по
  закону З-4 (маленький шаг там, где риск растёт с размером) семья 2
  режется НЕ одним PRD, а по правилам: R04 первым, отдельным PRD.

Следующий PRD: PRD-047.42-APPLY-23 — ТОЛЬКО правило R04 (provide_one_
  bounded_practice), строки 666-692 (27 строк, современная нумерация
  на момент этой записи; пересчитать перед стартом). Самодостаточный
  каскад с 3 self-вызовами и локальной практик-логикой (practice_
  anchor_present/practice_step_present/practice_multistep). R07-R16
  (61 строка) — следующим PRD после R04, отдельно.
Дальше по Сценарию А: R07-R16 (одним или несколькими PRD по факту
  разведки), решение по mvp_free_branch_handoff (вероятно "остаётся
  на месте"), затем семьи 4-6 по карте (с обязательной перепроверкой
  границ каждой — карта уже дважды показала занижение реальной
  ширины). Затем _enforce_mvp_free_dialogue_compliance (225 строк).
Шлюз в Эпоху 2: облегчённый (владелец-решение #4). Ориентир до шлюза:
  ~9-14 PRD (пересчитан вверх — семья 2 требует минимум 2 PRD вместо
  предполагаемого 1, вероятно и другие семьи так же).

Корпус законов Fable: получен полностью (R01-R07), НЕ ратифицирован.

ВЛАДЕЛЕЦ-РЕШЕНИЕ (2026-07-13): 4 больших файла — трек после Эпохи 2.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #2 (2026-07-14): Сценарий А, DoD §5.6 не закрывается
  буквально — сознательно, не blocker.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #3 (2026-07-16): await create_agent_completion
  остаётся в _call_llm.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #4 (2026-07-16): облегчённый шлюз Эпохи 2 — только
  Сценарий А; PRD-047.42b/43/45 — отдельный трек после Эпохи 2.

PRD-047.42-APPLY-22 закрыт (второй срез _enforce_answer_compliance,
  вторая прелюдия + R03, первая ветвящаяся механика):
- строки 628-700 вынесены в writer_agent_enforce_slice2.py;
  close_gently_triggered: bool отделяет true/false ветки (5 vs 6
  ключей патча); user_mechanism_request/answer_fit — Optional,
  валидны только в false-ветке;
- self._set_final_answer_shape_debug и self._build_gentle_close_reply
  остались в writer_agent.py, вызываются СТРОГО после
  self.last_debug.update(patch) — порядок байт снапшота сохранён,
  подтверждено интеграционным тестом Исполнителя И независимой прямой
  симуляцией архитектора;
- три маркерных tuple'а переданы параметрами (закон З-3, избежание
  циклического импорта из writer_agent.py).

Защищённые файлы (канонический список архитектора) — 19:
  14 из transfer brief v3
  + slice9  (f69c7217137ddc23ea1c09e7ba6dd9a1886d0249)
  + slice10 (dd34d823f61c608c3bd8a2be0fd48763cec2c838)
  + slice11 (fa776248a89124c8b34d7d94134a448bee9a3cdc)
  + slice12 (8a9544b784f16bb3b3968d2c787964b684e85838)
  + enforce_slice1 (eef347d07d2f3693fef1554a210cfa0e44526126).
  После приёмки APPLY-23: + writer_agent_enforce_slice2.py (hash
  зафиксировать из HEAD ff9489c2) + writer_agent_enforce_slice3.py
  (новый) = 21.

Отложено сознательно (не потеряно):
- R07-R16, mvp_free_branch_handoff, семьи 4-6 — после R04, границы
  пересчитывать перед каждым шагом без исключения.
- _enforce_mvp_free_dialogue_compliance (225 строк) — после.
- core_required_fields — вне серии.
- 4 больших файла; PRD-047.42b; PRD-047.43; PRD-047.45 — отдельный
  трек ПОСЛЕ Эпохи 2.
- writer_contract.py (979 строк) — под порогом, отдельное решение.
- docs/testing.md, S7, Panic/Medical Escalation, трейс-поля — без изменений.
```

---

## БЛОК Б — одна новая запись журнала (вставить ПОВЕРХ существующих)

```markdown
- 2026-07-16 v4.25 -> v4.26: PRD-047.42-APPLY-22 принят ACCEPTED —
  вторая прелюдия + R03 (механика (d), первая ветвящаяся в проекте).
  Ловушка порядка ключей лично воспроизведена и подтверждена
  архитектором прямой симуляцией. Семья 1 закрыта полностью. Разведка
  семьи 2: чище семьи 1 (нет скрытой подготовки локальных), но по
  закону маленького шага режется по правилам, не одним PRD. Следующий
  шаг — APPLY-23: только R04 (provide_one_bounded_practice).
```
