# ОБНОВЛЕНИЕ Части 4 мастер-плана после PRD-047.42-APPLY-23 (v4.26 → v4.27)

**Файл:** `docs/MASTER_STRATEGIC_PLAN_NEO_MindBot_v4_RU.md`
**Дата:** 2026-07-16
**Подготовил:** архитектор, после независимой верификации APPLY-23
(коммиты ad7875d8 / d7ef669a / b39ed432) и разведки R07-R16.

---

## ИНСТРУКЦИЯ ДЛЯ ИСПОЛНИТЕЛЯ — строго ТРИ отдельных шага

```text
Шаг 1. В шапке документа заменить "v4.26" на "v4.27".
Шаг 2 (БЛОК А). Заменить тело Части 4 — от строки
  "# ЧАСТЬ 4 — ГДЕ МЫ СЕЙЧАС  ← ЕДИНСТВЕННЫЙ ОБНОВЛЯЕМЫЙ РАЗДЕЛ"
  (включительно) до строки
  "Журнал правок (новая строка поверх существующих):" (НЕ включая) —
  на Блок А ниже. ЖУРНАЛ НЕ ТРОГАТЬ.
Шаг 3 (БЛОК Б). Вставить ОДНУ новую запись журнала (Блок Б) поверх
  существующих. Существующие записи не удалять, не переформатировать.
Коммит: "Update master plan part 4 after PRD-047.42-APPLY-23 (v4.27)".
Перед пушем ОБЯЗАТЕЛЬНО: git diff + глазами убедиться, что из журнала
не исчезло НИ ОДНОЙ старой записи.
```

---

## БЛОК А — новое тело Части 4 (без журнала)

```markdown
# ЧАСТЬ 4 — ГДЕ МЫ СЕЙЧАС  ← ЕДИНСТВЕННЫЙ ОБНОВЛЯЕМЫЙ РАЗДЕЛ

Эпоха: 1 (Консолидация).
Последний принятый PRD: PRD-047.42-APPLY-23 (ACCEPTED, commits
  d7ef669a / b39ed432; план до v4.26 — ad7875d8). Верифицировано
  архитектором независимо: helper дословно сверен с PRD; литеральный
  текст ответа сверен посимвольно с оригиналом через git show —
  идентичен; собственный пересчёт снапшотов детерминирован и
  идентичен закоммиченным; 144 passed; канонический прогон — ровно
  19, список идентичен эталону; 20 защищённых файлов не тронуты;
  логи APPLY-20/21/22 неприкосновенны. Честная переоценка границ
  Исполнителем (666-692 -> факт 666-696, форматирование литерала)
  принята как образец правильной прозрачности, не как проблема.

ТЕМП РАБОТЫ — РЕШЕНИЕ ВЛАДЕЛЬЦА (2026-07-16, устно в диалоге,
  зафиксировано архитектором): семьи правил без подтверждённой при
  разведке скрытой сложности (конкурирующие last_debug-записи между
  соседними правилами, вложенные self-зависимости с риском порядка
  байт) режутся ОДНИМ PRD целиком, а не по одному правилу. Мелкий шаг
  (закон З-4) остаётся только там, где разведка ПОДТВЕРДИЛА реальный
  риск — как было с R03/R04. Разведка перед каждым PRD обязательна в
  любом случае; решение о размере шага — по её честному результату.

РАЗВЕДКА R07-R16 (архитектор, до выдачи PRD): чистый случай —
  56 строк (современная нумерация), 5 независимых правил
  (R07/R08 literal_markdown_echo-репейр, R09/R10 acknowledge_style_
  preference, R11/R12 repair_and_answer_last_question, R13/R14
  answer_last_offer, R15/R16 answer_knowledge_question/answer_direct_
  question), НИКАКОЙ общей подготовки локальных между правилами,
  НИКАКОЙ конкуренции last_debug (единственная запись — в
  literal_markdown_echo, полностью самодостаточна: пишет и сразу
  возвращает). Все 4 repair-правила используют один и тот же self-
  вызов self._defer_no_stub_repair с разными signal/must_answer —
  безопасно для единого классификатора. Берём ВСЮ семью одним PRD.

Следующий PRD: PRD-047.42-APPLY-24 — R07-R16 одним срезом (границы
  пересчитать заново перед стартом; на момент разведки — строки
  690-745). Механика — классификатор (как APPLY-23), но с 6 исходами
  вместо 4: not_matched + literal_markdown_echo_mismatch (несёт
  last_debug-патч из 2 ключей и текст возврата) + 4 варианта *_repair
  (несут signal/must_answer для self._defer_no_stub_repair, который
  остаётся в writer_agent.py).
Дальше по Сценарию А: решение по mvp_free_branch_handoff (кандидат на
  "остаётся на месте" по аналогии с owner-решением #3) — архитектор
  решит при подходе к границе; затем семьи 4-6 карты APPLY-20 (каждая
  — разведка -> решение о размере шага по её результату, не заранее).
  Потом _enforce_mvp_free_dialogue_compliance (225 строк).
Шлюз в Эпоху 2: облегчённый (владелец-решение #4). Ориентир до шлюза
  при новом темпе: ~6-9 PRD (пересчитан вниз с учётом укрупнения).

Корпус законов Fable: получен полностью (R01-R07), НЕ ратифицирован.

ВЛАДЕЛЕЦ-РЕШЕНИЕ (2026-07-13): 4 больших файла — трек после Эпохи 2.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #2 (2026-07-14): Сценарий А, DoD §5.6 не закрывается
  буквально — сознательно, не blocker.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #3 (2026-07-16): await create_agent_completion
  остаётся в _call_llm.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #4 (2026-07-16): облегчённый шлюз Эпохи 2 — только
  Сценарий А; PRD-047.42b/43/45 — отдельный трек после Эпохи 2.

PRD-047.42-APPLY-23 закрыт (R04, provide_one_bounded_practice,
  первый срез семьи 2):
- строки 666-696 вынесены в writer_agent_enforce_slice3.py как чистый
  классификатор без self и без записи last_debug (4 исхода);
- все self-вызовы и литеральный текст ответа остались в writer_agent.py;
- упрощённая механика (без флага+патча из APPLY-22) обоснована и
  безопасна — в этом правиле нет конкуренции last_debug с соседями.

Защищённые файлы (канонический список архитектора) — 20:
  14 из transfer brief v3
  + slice9  (f69c7217137ddc23ea1c09e7ba6dd9a1886d0249)
  + slice10 (dd34d823f61c608c3bd8a2be0fd48763cec2c838)
  + slice11 (fa776248a89124c8b34d7d94134a448bee9a3cdc)
  + slice12 (8a9544b784f16bb3b3968d2c787964b684e85838)
  + enforce_slice1 (eef347d07d2f3693fef1554a210cfa0e44526126)
  + enforce_slice2 (ea62e30cb721ed514cc501129bfc3857982a401f).
  После приёмки APPLY-24: + writer_agent_enforce_slice3.py (hash
  зафиксировать из HEAD b39ed432) + writer_agent_enforce_slice4.py
  (новый) = 22.

Отложено сознательно (не потеряно):
- mvp_free_branch_handoff, семьи 4-6 — после R07-R16, разведка перед
  каждым шагом обязательна.
- _enforce_mvp_free_dialogue_compliance (225 строк) — после.
- core_required_fields — вне серии.
- 4 больших файла; PRD-047.42b; PRD-047.43; PRD-047.45 — отдельный
  трек ПОСЛЕ Эпохи 2.
- writer_contract.py (979 строк) — под порогом, отдельное решение.
- docs/testing.md, S7, Panic/Medical Escalation, трейс-поля — без изменений.
```

---

## БЛОК Б — одна новая запись журнала (вставить ПОВЕРХ существующих)

```markdown
- 2026-07-16 v4.26 -> v4.27: PRD-047.42-APPLY-23 принят ACCEPTED —
  R04 (provide_one_bounded_practice) вынесен чистым классификатором
  без self. ВЛАДЕЛЬЦЕМ принято решение о темпе: семьи без
  подтверждённой разведкой скрытой сложности режутся одним PRD, не
  по правилу. Разведка R07-R16 чистая (5 независимых правил, без
  конкуренции last_debug) — следующий PRD берёт всю семью целиком.
```
