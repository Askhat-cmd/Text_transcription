# ОБНОВЛЕНИЕ Части 4 мастер-плана после PRD-047.42-APPLY-24 (v4.27 → v4.28)

**Файл:** `docs/MASTER_STRATEGIC_PLAN_NEO_MindBot_v4_RU.md`
**Дата:** 2026-07-16
**Подготовил:** архитектор, после независимой верификации APPLY-24
(коммиты 2b17486e / fa935d5c / 696af665) и разбора находки о дрейфе
подсчёта правил.

---

## ИНСТРУКЦИЯ ДЛЯ ИСПОЛНИТЕЛЯ — строго ТРИ отдельных шага

```text
Шаг 1. В шапке документа заменить "v4.27" на "v4.28".
Шаг 2 (БЛОК А). Заменить тело Части 4 — от строки
  "# ЧАСТЬ 4 — ГДЕ МЫ СЕЙЧАС  ← ЕДИНСТВЕННЫЙ ОБНОВЛЯЕМЫЙ РАЗДЕЛ"
  (включительно) до строки
  "Журнал правок (новая строка поверх существующих):" (НЕ включая) —
  на Блок А ниже. ЖУРНАЛ НЕ ТРОГАТЬ.
Шаг 3 (БЛОК Б). Вставить ОДНУ новую запись журнала (Блок Б) поверх
  существующих. Существующие записи не удалять, не переформатировать.
Коммит: "Update master plan part 4 after PRD-047.42-APPLY-24 (v4.28)".
Перед пушем ОБЯЗАТЕЛЬНО: git diff + глазами убедиться, что из журнала
не исчезло НИ ОДНОЙ старой записи.
```

---

## БЛОК А — новое тело Части 4 (без журнала)

```markdown
# ЧАСТЬ 4 — ГДЕ МЫ СЕЙЧАС  ← ЕДИНСТВЕННЫЙ ОБНОВЛЯЕМЫЙ РАЗДЕЛ

Эпоха: 1 (Консолидация).
Последний принятый PRD: PRD-047.42-APPLY-24 (ACCEPTED, commits
  fa935d5c / 696af665; план до v4.27 — 2b17486e). Верифицировано
  архитектором независимо: helper дословно сверен с PRD; собственный
  пересчёт снапшотов детерминирован и идентичен закоммиченным (17
  cases); 150 passed по серии; красная линия соблюдена — файл теста
  APPLY-20 и все логи APPLY-20..23 физически не тронуты (git diff
  пуст); канонический pytest tests/ -k writer -q — ровно 19, список
  идентичен эталону.

СЕМЬЯ 2 (obligation_specific_repairs_before_profile_split, R04-R16)
  ПОЛНОСТЬЮ ЗАКРЫТА.

НАХОДКА И РЕШЕНИЕ АРХИТЕКТОРА (разобрано и подтверждено независимо):
  test_prd_047_42_apply_20_enforce_compliance_mapping.py::
  test_rule_count_matches_boundary_map_inventory падает (75 -> 67)
  вне канонического прогона. Причина воспроизведена архитектором
  трассировкой физического числа AST if-узлов по коммитам: APPLY-21/22
  /23 сохраняли 75, APPLY-24 впервые дал ровно 67 (-8, что равно
  10 схлопнутых if-узлов R07-R16 минус 2 диспетчерских if в
  writer_agent.py — арифметика Исполнителя подтверждена посчитанным
  фактом, не только объяснением).
  ДИАГНОЗ: тест по конструкции сравнивает ЖИВОЙ AST-подсчёт if-узлов
  текущего кода с константой 75, зафиксированной в момент картирования
  (до начала декомпозиции). Классификаторная механика (APPLY-23/24 и
  далее) ЗАКОНОМЕРНО и НЕИЗБЕЖНО схлопывает вложенные if в
  диспетчерские — это сама суть декомпозиции, не побочный эффект.
  Тест был спроектирован как разовая проверка целостности карты В
  МОМЕНТ ЕЁ СОЗДАНИЯ, а не как вечный инвариант, переживающий
  дальнейший рефакторинг — это ограничение конструкции теста
  архитектор не предвидел при приёмке APPLY-20.
  ПОСЛЕДСТВИЯ, ЕСЛИ НЕ ВМЕШАТЬСЯ: каждый следующий классификаторный
  срез будет ЕЩЁ БОЛЬШЕ отдалять счётчик от 75 без всякой
  диагностической ценности —шум множится, а не убывает.
  РЕШЕНИЕ АРХИТЕКТОРА (действует, не требует отдельного владелец-
  решения — техническое, в рамках стратегии): test_rule_count_
  matches_boundary_map_inventory выводится из категории "живой
  регрессионный тест" в категорию "исторический снимок момента
  APPLY-20" отдельным микро-PRD ПЕРЕД следующим содержательным срезом
  (не блокирует Сценарий А, но должно быть сделано в течение 1-2
  PRD, иначе шум накопится). Микро-PRD не переписывает и не удаляет
  архив APPLY-20 (красная линия соблюдается), а превращает именно эту
  ОДНУ ассерцию в информационную (например, снимает жёсткое ==75,
  документирует ожидаемый дрейф с ссылкой на ADR). До этого микро-PRD
  падение теста остаётся задокументированным, некритичным, вне
  канонического прогона -k writer — не блокирует дальнейшую работу.

Следующий PRD: (а) микро-PRD "retire rule-count invariant" (см. выше,
  небольшой, 1 файл теста, явное обоснование, не более получаса
  Исполнителя) — рекомендуется сделать следующим, чтобы не копить
  долг; ЛИБО (б) по желанию владельца — сразу к следующей семье
  (mvp_free_branch_handoff, строка ~746 текущей нумерации, вероятный
  кандидат на "остаётся на месте"), с микро-PRD чуть позже. Решение
  порядка — за владельцем, оба варианта не блокируют друг друга.
Дальше по Сценарию А: mvp_free_branch_handoff -> семьи 4-6 карты
  APPLY-20 (разведка перед каждой, размер шага по её результату) ->
  _enforce_mvp_free_dialogue_compliance (225 строк).
Шлюз в Эпоху 2: облегчённый (владелец-решение #4). Ориентир до шлюза
  при текущем темпе: ~5-8 содержательных PRD + 1 микро-PRD гигиены.

Корпус законов Fable: получен полностью (R01-R07), НЕ ратифицирован.

ВЛАДЕЛЕЦ-РЕШЕНИЕ (2026-07-13): 4 больших файла — трек после Эпохи 2.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #2 (2026-07-14): Сценарий А, DoD §5.6 не закрывается
  буквально — сознательно, не blocker.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #3 (2026-07-16): await create_agent_completion
  остаётся в _call_llm.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #4 (2026-07-16): облегчённый шлюз Эпохи 2 — только
  Сценарий А; PRD-047.42b/43/45 — отдельный трек после Эпохи 2.
ТЕМП РАБОТЫ (2026-07-16, зафиксировано в v4.27): семьи без
  подтверждённой разведкой скрытой сложности режутся одним PRD.

PRD-047.42-APPLY-24 закрыт (R07-R16, вся семья 2 одним укрупнённым
  срезом — первое применение нового темпа):
- 56 строк вынесены в writer_agent_enforce_slice4.py как чистый
  классификатор с 6 исходами, без self, без прямой записи last_debug;
- единственный self-вызов (_defer_no_stub_repair) и патч из 2 ключей
  для literal_markdown_echo остались в writer_agent.py, в точном
  исходном порядке;
- границы (690-745) совпали с PRD без переоценки — впервые в серии
  enforce-декомпозиции разведка архитектора оказалась абсолютно точной;
- честно задокументирована и架 разобрана находка о дрейфе AST-теста
  APPLY-20 (см. выше) — образцовый пример закона З-10 (честная дыра
  важнее красивого отчёта).

Защищённые файлы (канонический список архитектора) — 21:
  14 из transfer brief v3
  + slice9  (f69c7217137ddc23ea1c09e7ba6dd9a1886d0249)
  + slice10 (dd34d823f61c608c3bd8a2be0fd48763cec2c838)
  + slice11 (fa776248a89124c8b34d7d94134a448bee9a3cdc)
  + slice12 (8a9544b784f16bb3b3968d2c787964b684e85838)
  + enforce_slice1 (eef347d07d2f3693fef1554a210cfa0e44526126)
  + enforce_slice2 (ea62e30cb721ed514cc501129bfc3857982a401f)
  + enforce_slice3 (41ddfbf851c540d82109890c58a8c418827a97fa).
  После микро-PRD гигиены список не растёт (правки теста, не
  production-кода). После следующего содержательного среза:
  + writer_agent_enforce_slice4.py (hash зафиксировать из HEAD
  696af665) = 22.

Отложено сознательно (не потеряно):
- Микро-PRD "retire rule-count invariant" — рекомендован следующим.
- mvp_free_branch_handoff, семьи 4-6 — после, разведка перед каждым.
- _enforce_mvp_free_dialogue_compliance (225 строк) — после.
- core_required_fields — вне серии.
- 4 больших файла; PRD-047.42b; PRD-047.43; PRD-047.45 — отдельный
  трек ПОСЛЕ Эпохи 2.
- writer_contract.py (979 строк) — под порогом, отдельное решение.
- docs/testing.md, S7, Panic/Medical Escalation, трейс-поля — без изменений.
```

---

## БЛОК Б — одна новая запись журнала (вставить ПОВЕРХ существующих)

```markdown
- 2026-07-16 v4.27 -> v4.28: PRD-047.42-APPLY-24 принят ACCEPTED —
  семья 2 (R04-R16) полностью закрыта, R07-R16 взяты одним
  укрупнённым срезом (новый темп). НАХОДКА: тест APPLY-20 на подсчёт
  правил (==75) структурно несовместим с классификаторной механикой —
  живой AST-счётчик закономерно дрейфует при каждом схлопывании
  вложенных if в диспетчерские. Не регрессия, вне канонического
  прогона, красная линия соблюдена. Архитектор рекомендует небольшой
  микро-PRD, переводящий эту ассерцию в информационную, следующим
  шагом (не блокирует Сценарий А).
```
