# ОБНОВЛЕНИЕ Части 4 мастер-плана после PRD-047.42-APPLY-25 (v4.28 → v4.29)

**Файл:** `docs/MASTER_STRATEGIC_PLAN_NEO_MindBot_v4_RU.md`
**Дата:** 2026-07-16
**Подготовил:** архитектор, после независимой верификации APPLY-25
(коммиты dc1c9aa8 / b2c4c489 / 787b7f0d), технического решения по
mvp_free_branch_handoff и разведки остатка метода.

---

## ИНСТРУКЦИЯ ДЛЯ ИСПОЛНИТЕЛЯ — строго ТРИ отдельных шага

```text
Шаг 1. В шапке документа заменить "v4.28" на "v4.29".
Шаг 2 (БЛОК А). Заменить тело Части 4 — от строки
  "# ЧАСТЬ 4 — ГДЕ МЫ СЕЙЧАС  ← ЕДИНСТВЕННЫЙ ОБНОВЛЯЕМЫЙ РАЗДЕЛ"
  (включительно) до строки
  "Журнал правок (новая строка поверх существующих):" (НЕ включая) —
  на текст Блока А ниже. ЖУРНАЛ НЕ ТРОГАТЬ.
Шаг 3 (БЛОК Б). Вставить ОДНУ новую запись журнала (Блок Б) поверх
  существующих. Существующие записи не удалять, не переформатировать.
Коммит: "Update master plan part 4 after PRD-047.42-APPLY-25 (v4.29)".
Перед пушем ОБЯЗАТЕЛЬНО: git diff + глазами убедиться, что из журнала
не исчезло НИ ОДНОЙ старой записи.
```

---

## Новое тело Части 4 (без журнала)

```markdown
# ЧАСТЬ 4 — ГДЕ МЫ СЕЙЧАС  ← ЕДИНСТВЕННЫЙ ОБНОВЛЯЕМЫЙ РАЗДЕЛ

Эпоха: 1 (Консолидация).
Последний принятый PRD: PRD-047.42-APPLY-25 (ACCEPTED, commits
  b2c4c489 / 787b7f0d; план до v4.28 — dc1c9aa8). Верифицировано
  архитектором независимо: diff ограничен ровно одним файлом теста
  (подтверждено git diff); замена дословно совпадает со спецификацией
  (одна ассерция убрана, docstring добавлен); логи APPLY-20 не тронуты
  (красная линия); 139 passed; канонический прогон — ровно 19, список
  идентичен эталону.

ТЕХНИЧЕСКОЕ РЕШЕНИЕ АРХИТЕКТОРА (2026-07-16, в рамках стратегии, без
  отдельного владелец-решения — прямой прецедент): mvp_free_branch_
  handoff (строка 717, если dialogue_profile == DIALOGUE_PROFILE_MVP_
  FREE: return self._enforce_mvp_free_dialogue_compliance(...)) —
  ОСТАЁТСЯ НА МЕСТЕ. Единственный делегирующий self-вызов без
  внутренней логики, без записей last_debug — прямой аналог
  provider_dispatch в _call_llm (владелец-решение #3). Выносить в
  отдельный файл — чистая церемония, структура без пользы (закон З-3).
  Семья 3 карты APPLY-20 закрыта этим решением, PRD не требовался.

РАЗВЕДКА ОСТАТКА МЕТОДА (архитектор, до выдачи PRD): после mvp_free
  handoff остаётся 278 строк (756-1033, до return text — конец
  метода). Внутри — ровно ОДНА запись last_debug (строка 912,
  template_leakage_repair_deferred_to_gate) и только 3 self-метода
  (_defer_no_stub_repair, _resolve_one_step_or_no_practice_fallback,
  _set_final_answer_shape_debug+_strip_optional_followup_invitation —
  знакомая пара из R04), все — терминальные (сразу перед return).
  Естественная граница по комментарию кода на строке 822 ("Known
  concept answer-first path") делит остаток на:
    Block A (756-821, 66 строк): greeting/low-resource/safety/
      support/clarify_one_point/user_repair_signal — ~10-11 исходов,
      НОЛЬ last_debug-записей, один self-вызов в самом конце. Взят
      целиком в следующий PRD — чистый случай, полный аналог R07-R16.
    Block B (822-1033, 212 строк): "known concept answer-first" +
      question_policy-каскад + практик/active-line хвост, включая
      единственную last_debug-запись метода. НЕ разведан детально —
      решение по его размеру (одним PRD или дробить) откладывается
      до момента подхода к нему, разведка перед этим PRD обязательна
      (закон-принцип: не планировать вперёд то, что не прочитано).

Следующий PRD: PRD-047.42-APPLY-26 — Block A целиком (756-821,
  пересчитать границы перед стартом). Механика — классификатор,
  аналогичный APPLY-24, но с текстовыми исходами (часть текстов
  ответа повторяется у разных условий — переносить как есть, не
  дедуплицировать).
Дальше по Сценарию А: разведка и PRD(ы) по Block B -> boundary
  mapping + декомпозиция _enforce_mvp_free_dialogue_compliance
  (225 строк, отдельный метод).
Шлюз в Эпоху 2: облегчённый (владелец-решение #4). Ориентир до шлюза:
  ~4-6 PRD (Block A, Block B — 1-2 PRD по факту разведки,
  _enforce_mvp_free_dialogue_compliance — 1-2 PRD).

Корпус законов Fable: получен полностью (R01-R07), НЕ ратифицирован.

ВЛАДЕЛЕЦ-РЕШЕНИЕ (2026-07-13): 4 больших файла — трек после Эпохи 2.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #2 (2026-07-14): Сценарий А, DoD §5.6 не закрывается
  буквально — сознательно, не blocker.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #3 (2026-07-16): await create_agent_completion
  остаётся в _call_llm.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #4 (2026-07-16): облегчённый шлюз Эпохи 2 — только
  Сценарий А; PRD-047.42b/43/45 — отдельный трек после Эпохи 2.
ТЕМП РАБОТЫ (2026-07-16, v4.27): семьи без подтверждённой разведкой
  скрытой сложности режутся одним PRD.

PRD-047.42-APPLY-25 закрыт (гигиенический микро-PRD): жёсткая
  ассерция rule_count==75 в тесте APPLY-20 заменена на docstring с
  объяснением ожидаемого дрейфа при классификаторной декомпозиции;
  диагностические ассерции (самосогласованность, floor>=40) сохранены;
  логи APPLY-20 не тронуты; технический долг из находки APPLY-24
  погашен полностью.

Защищённые файлы (канонический список архитектора) — 22:
  14 из transfer brief v3
  + slice9  (f69c7217137ddc23ea1c09e7ba6dd9a1886d0249)
  + slice10 (dd34d823f61c608c3bd8a2be0fd48763cec2c838)
  + slice11 (fa776248a89124c8b34d7d94134a448bee9a3cdc)
  + slice12 (8a9544b784f16bb3b3968d2c787964b684e85838)
  + enforce_slice1 (eef347d07d2f3693fef1554a210cfa0e44526126)
  + enforce_slice2 (ea62e30cb721ed514cc501129bfc3857982a401f)
  + enforce_slice3 (41ddfbf851c540d82109890c58a8c418827a97fa)
  + enforce_slice4 (hash зафиксировать из HEAD 787b7f0d).
  После приёмки APPLY-26: + writer_agent_enforce_slice5.py (новый) = 23.

Отложено сознательно (не потеряно):
- Block B (822-1033, 212 строк) — разведка перед PRD, после Block A.
- _enforce_mvp_free_dialogue_compliance (225 строк) — после метода.
- core_required_fields — вне серии.
- 4 больших файла; PRD-047.42b; PRD-047.43; PRD-047.45 — отдельный
  трек ПОСЛЕ Эпохи 2.
- writer_contract.py (979 строк) — под порогом, отдельное решение.
- docs/testing.md, S7, Panic/Medical Escalation, трейс-поля — без изменений.
```

---

## БЛОК Б журнала — одна новая запись (вставить ПОВЕРХ существующих)

```markdown
- 2026-07-16 v4.28 -> v4.29: PRD-047.42-APPLY-25 принят ACCEPTED —
  технический долг rule_count-теста погашен. Техническое решение
  архитектора: mvp_free_branch_handoff остаётся на месте (прямой
  аналог owner-решения #3), семья 3 карты закрыта без PRD. Разведка
  остатка метода (278 строк): естественная граница делит его на
  Block A (66 строк, чистый) и Block B (212 строк, не разведан).
  Следующий шаг — APPLY-26, Block A целиком.
```
