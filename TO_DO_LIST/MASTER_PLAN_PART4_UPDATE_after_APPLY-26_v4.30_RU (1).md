# ОБНОВЛЕНИЕ Части 4 мастер-плана после PRD-047.42-APPLY-26 (v4.29 → v4.30)

**Файл:** `docs/MASTER_STRATEGIC_PLAN_NEO_MindBot_v4_RU.md`
**Дата:** 2026-07-16
**Подготовил:** архитектор, после независимой верификации APPLY-26
(коммиты 06fbbbdf / ad12bf8d / 312d476b). Подготовлено ДО передачи в
работу, вместе с трансфер-брифом — чтобы новый чат не унаследовал
типовой лаг "план на 1 PRD младше кода".

---

## ИНСТРУКЦИЯ ДЛЯ ИСПОЛНИТЕЛЯ — строго ТРИ отдельных шага

```text
Шаг 1. В шапке документа заменить "v4.29" на "v4.30".
Шаг 2 (БЛОК А). Заменить тело Части 4 — от строки
  "# ЧАСТЬ 4 — ГДЕ МЫ СЕЙЧАС  ← ЕДИНСТВЕННЫЙ ОБНОВЛЯЕМЫЙ РАЗДЕЛ"
  (включительно) до строки
  "Журнал правок (новая строка поверх существующих):" (НЕ включая) —
  на текст Блока А ниже. ЖУРНАЛ НЕ ТРОГАТЬ.
Шаг 3 (БЛОК Б). Вставить ОДНУ новую запись журнала (Блок Б) поверх
  существующих. Существующие записи не удалять, не переформатировать.
Коммит: "Update master plan part 4 after PRD-047.42-APPLY-26 (v4.30)".
Перед пушем ОБЯЗАТЕЛЬНО: git diff + глазами убедиться, что из журнала
не исчезло НИ ОДНОЙ старой записи.
```

---

## Новое тело Части 4 (без журнала)

```markdown
# ЧАСТЬ 4 — ГДЕ МЫ СЕЙЧАС  ← ЕДИНСТВЕННЫЙ ОБНОВЛЯЕМЫЙ РАЗДЕЛ

Эпоха: 1 (Консолидация).
Последний принятый PRD: PRD-047.42-APPLY-26 (ACCEPTED, commits
  ad12bf8d / 312d476b; план до v4.29 — 06fbbbdf). Верифицировано
  архитектором независимо: классификатор дословно сверен с PRD;
  writer_agent.py — вызов и все 15 текстовых литералов точны;
  собственный пересчёт снапшотов идентичен закоммиченным (17 cases);
  162 passed; канонический прогон — ровно 19, список идентичен
  эталону; 22 защищённых файла не тронуты; логи APPLY-20..25
  неприкосновенны. Честная находка Исполнителя подтверждена архитектором
  логическим разбором: исходы give_short_support_len_or_flags и
  give_short_support_markers структурно недостижимы (их условие —
  строгое подмножество условия give_short_support_primary) —
  унаследованный мёртвый код оригинала, не новый дефект, поведение
  не изменилось (не трогали, только задокументировали).

Семья 3 (mvp_free_branch_handoff) закрыта техническим решением
  архитектора ("остаётся на месте", без PRD). Block A остатка метода
  (756-821 на момент решения) ЗАКРЫТ.

writer_agent.py = 1276 строк. _enforce_answer_compliance начинается на
  строке 574. Block B — единственный неразведанный участок метода:
  строки 804-1015 (212 строк, современная нумерация), от комментария
  "# Known concept answer-first path" до "return text" (конец метода,
  перед def _enforce_mvp_free_dialogue_compliance на строке 1017).
  Внутри Block B — единственная во всём методе оставшаяся запись
  last_debug (template_leakage_repair_deferred_to_gate, была на строке
  912 до сдвига от APPLY-26 — пересчитать точно). НЕ разведан детально
  архитектором построчно — только внешние границы и список self-вызовов
  (_defer_no_stub_repair, _resolve_one_step_or_no_practice_fallback,
  _set_final_answer_shape_debug+_strip_optional_followup_invitation —
  знакомая пара из R04/APPLY-23) сняты на момент этой записи.

Следующий шаг: разведка Block B (построчно, с обычной сверкой на
  скрытую подготовку локальных между правилами и порядок last_debug-
  записи — по прецеденту APPLY-21/22, не доверять внешней чистоте
  на глаз) -> решение о размере шага (одним PRD или дробить) по
  результату -> PRD(ы) декомпозиции -> boundary mapping и декомпозиция
  _enforce_mvp_free_dialogue_compliance (225 строк, отдельный метод,
  начинается на строке 1017).
Шлюз в Эпоху 2: облегчённый (владелец-решение #4). Ориентир до шлюза
  на момент этой записи: ~4-6 PRD (Block B — 1-2 PRD по факту разведки,
  _enforce_mvp_free_dialogue_compliance — 1-2 PRD, возможен запас).

Корпус законов Fable: получен полностью (R01-R07), НЕ ратифицирован.

ВЛАДЕЛЕЦ-РЕШЕНИЕ (2026-07-13): 4 больших файла — трек после Эпохи 2.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #2 (2026-07-14): Сценарий А, DoD §5.6 не закрывается
  буквально — сознательно, не blocker.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #3 (2026-07-16): await create_agent_completion
  остаётся в _call_llm.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #4 (2026-07-16): облегчённый шлюз Эпохи 2 — только
  Сценарий А; PRD-047.42b/43/45 — отдельный трек после Эпохи 2.
ТЕМП РАБОТЫ (2026-07-16, v4.27): семьи без подтверждённой разведкой
  скрытой сложности режутся одним PRD.

PRD-047.42-APPLY-26 закрыт (Block A, 15 исходов, семья 3 закрыта
  техническим решением): чистый классификатор, единственный self-вызов
  остался в writer_agent.py, повторяющиеся текстовые литералы НЕ
  дедуплицированы (сохранены раздельно по группам исходов, как в
  оригинале); найден и задокументирован унаследованный мёртвый код
  (2 недостижимых исхода) — не исправлялся, только зафиксирован.

Защищённые файлы (канонический список архитектора) — 23:
  14 из transfer brief v3
  + slice9  (f69c7217137ddc23ea1c09e7ba6dd9a1886d0249)
  + slice10 (dd34d823f61c608c3bd8a2be0fd48763cec2c838)
  + slice11 (fa776248a89124c8b34d7d94134a448bee9a3cdc)
  + slice12 (8a9544b784f16bb3b3968d2c787964b684e85838)
  + enforce_slice1 (eef347d07d2f3693fef1554a210cfa0e44526126)
  + enforce_slice2 (ea62e30cb721ed514cc501129bfc3857982a401f)
  + enforce_slice3 (41ddfbf851c540d82109890c58a8c418827a97fa)
  + enforce_slice4 (13abc089f91b90c7077c98a002543ebf132eda3f)
  + enforce_slice5 (hash зафиксировать из HEAD 312d476b перед стартом
    следующего PRD).

Отложено сознательно (не потеряно):
- Block B (804-1015, 212 строк) — построчная разведка обязательна
  перед PRD, не доверять внешней чистоте.
- _enforce_mvp_free_dialogue_compliance (225 строк) — после Block B.
- core_required_fields — вне серии.
- 4 больших файла; PRD-047.42b; PRD-047.43; PRD-047.45 — отдельный
  трек ПОСЛЕ Эпохи 2.
- writer_contract.py (979 строк) — под порогом, отдельное решение.
- docs/testing.md, S7, Panic/Medical Escalation, трейс-поля — без изменений.
```

---

## БЛОК Б журнала — одна новая запись (вставить ПОВЕРХ существующих)

```markdown
- 2026-07-16 v4.29 -> v4.30: PRD-047.42-APPLY-26 принят ACCEPTED —
  Block A закрыт (15 исходов, найден и задокументирован унаследованный
  мёртвый код). Семья 3 закрыта техническим решением архитектора.
  Единственный оставшийся неразведанный участок _enforce_answer_
  compliance — Block B (212 строк). Этот апдейт подготовлен и передан
  ВМЕСТЕ с трансфер-брифом сессии, чтобы новый чат стартовал без
  типового лага "план на 1 PRD младше кода".
```
