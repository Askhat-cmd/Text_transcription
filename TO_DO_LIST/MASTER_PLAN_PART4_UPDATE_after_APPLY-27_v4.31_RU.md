# Обновление Части 4 мастер-плана — после PRD-047.42-APPLY-27 (v4.31)

Формат передачи — ДВА БЛОКА, применяются раздельно:
- **Блок А** — полное новое тело раздела "ЧАСТЬ 4 — ГДЕ МЫ СЕЙЧАС" (без
  журнала). Заменить им весь текст раздела ЧАСТЬ 4 от строки "Эпоха: 1
  (Консолидация)." до строки "Отложено сознательно (не потеряно): ...
  докс/testing.md, S7, Panic/Medical Escalation, трейс-поля — без
  изменений." включительно.
- **Блок Б** — ОДНА новая строка журнала. Вставить НАД существующей
  первой записью журнала (журнал остаётся "новая строка поверх
  существующих" — старые записи НЕ трогать, НЕ удалять).

Версия в шапке документа (после строки "**Версия:**") также обновить:
v4.30 -> v4.31.

---

## БЛОК А (заменить весь текст раздела ЧАСТЬ 4, кроме журнала)

Эпоха: 1 (Консолидация).
Последний принятый PRD: PRD-047.42-APPLY-27 (ACCEPTED, commits
  52f071a1 / c4a447a7; план до v4.30 — 2dfc9dfb). Верифицировано
  архитектором независимо: классификатор дословно сверен с PRD (19
  тегов, включая not_matched как сигнал провала дальше по методу, без
  соответствующей ветки на call site); writer_agent.py call site
  байт-в-байт совпадает с PRD; группы 7-12 (остаток Block B) физически
  не задеты; собственный пересчёт снапшотов напрямую импортом
  build_before_snapshot()/build_after_snapshot() идентичен
  закоммиченным (17 cases, before==after — 0 расхождений); порядок
  ключей last_debug для единственной оставшейся прямой записи метода
  (template_leakage_repair_deferred_to_gate) подтверждён — 17-я позиция
  из 18, перед final_answer_shape, до/после идентично; 29 новых тестов
  (26 прямых + 3 контрактных) и весь контрактный набор серии (145
  тестов) — зелёные; канонический прогон pytest tests/ -k writer -q —
  ровно 19 падений, список байт-в-байт идентичен списку ДО этого PRD
  (регрессии нет); 23 защищённых файла не тронуты; логи APPLY-20..26
  неприкосновенны. Дополнительно: архитектор прогнал дифференциальный
  фаззинг-тест (50 000 случайных комбинаций предложенной логики против
  извлечённого дословно оригинального кода строк 804-896) ДО передачи
  PRD Исполнителю — 0 расхождений; задокументировано в docs/DECISIONS.md
  как ADR-126 (написан Исполнителем, содержательно точен).

writer_agent.py = 1261 строка. _enforce_answer_compliance начинается на
  строке 576.

Block B ЧАСТИЧНО закрыт. Часть 1 (была 804-896, 93 строки, группы 1-6:
  known-concept-prefirst path, тройной question-policy-каскад,
  repair_misalignment, practice-forbidden template-leakage) закрыта
  PRD-047.42-APPLY-27. Единственная во всём методе прямая запись
  last_debug закрыта вместе с этой частью.

Часть 2 Block B (текущая нумерация HEAD c4a447a7: строки 883-1000, 118
  строк, группы 7-12) — структурно разведана архитектором заранее (см.
  раздел 5 PRD-047.42-APPLY-27), НЕ разведана детально построчно до
  уровня итогового PRD. Известно на сегодня:
  - группа 7 (883-892): maybe-return, один исход (mechanism_explanation_
    repair — третье по счёту появление этого сигнала в методе);
  - группа 8 (894-900): maybe-return, один исход (one_step);
  - группа 9 (902-941): САМЫЙ структурно сложный кусок всего метода.
    Maybe-return с 4 внутренними локальными (list_like, first_item,
    sentence_parts, has_step_marker) и 4 точками возврата, включая один
    исход с вычисленным текстом из regex-матча (first_item.group(1).
    strip()), не self-вызов и не last_debug, но требует аккуратной
    сборки return_text внутри helper'а. 3 из 4 точек возврата вызывают
    один и тот же self-метод (_resolve_one_step_or_no_practice_
    fallback) с одинаковыми аргументами;
  - группа 10 (943-962): ВСЕГДА возвращает при входе — 4 исхода,
    последний безусловный default внутри блока;
  - группа 11 (964-979): maybe-return, вызывает МОДУЛЬНУЮ (не self)
    функцию starts_with_mechanical_revoicing(text) — уже импортирована
    в writer_agent.py, безопасна для прямого использования внутри
    helper'а. Один внутренний локальный (parts), один исход с
    вычисленным текстом;
  - группа 12 (981-999): maybe-return, 3 исхода — третье по счёту
    появление known_concept_correlation/neurostalking/self_realization
    сигналов в методе;
  - строка 1000 "return text" — безусловный финальный fallback всего
    метода _enforce_answer_compliance, не трогается.
  last_debug: группы 7-12 не содержат ни одной прямой записи —
  единственная запись метода уже закрыта в APPLY-27. Технически проще
  APPLY-27 по этому конкретному риску, сложнее по объёму локальных
  переменных (группа 9). НЕ доверять этому Заделу буквально —
  построчная разведка перед PRD обязательна заново (карта APPLY-20
  дважды подтверждённо занижала границы; тот же принцип применим к
  любой предварительной прикидке, включая эту).

Следующий шаг: построчная разведка групп 7-12 (перепроверить границы и
  скрытую структуру заново) -> PRD-047.42-APPLY-28 -> Block B закрыт
  полностью -> boundary mapping и декомпозиция _enforce_mvp_free_
  dialogue_compliance (225 строк на момент APPLY-26, пересчитать точно,
  отдельный метод) — не картирован вообще, своя отдельная разведка с
  нуля.
Шлюз в Эпоху 2: облегчённый (владелец-решение #4). Ориентир до шлюза на
  момент этой записи: ~3-5 PRD (Block B — 1 PRD остался (APPLY-28),
  _enforce_mvp_free_dialogue_compliance — 1-2 PRD, возможен запас).

Корпус законов Fable: получен полностью (R01-R07), НЕ ратифицирован.

ВЛАДЕЛЕЦ-РЕШЕНИЕ (2026-07-13): 4 больших файла — трек после Эпохи 2.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #2 (2026-07-14): Сценарий А, DoD §5.6 не закрывается
  буквально — сознательно, не blocker.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #3 (2026-07-16): await create_agent_completion
  остаётся в _call_llm.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #4 (2026-07-16): облегчённый шлюз Эпохи 2 — только
  Сценарий А; PRD-047.42b/43/45 — отдельный трек после Эпохи 2.
ТЕМП РАБОТЫ (2026-07-16, v4.27): семьи/блоки без подтверждённой
  разведкой скрытой сложности режутся одним PRD.
ТЕХНИЧЕСКОЕ РЕШЕНИЕ АРХИТЕКТОРА (2026-07-23, задокументировано как
  ADR-126): блок, смешивающий "всегда-возврат" и "может-провалиться-
  дальше" механики и заметно длиннее предыдущего батчированного среза,
  дробится по естественному структурному шву, а не произвольно
  пополам и не форсируется в один PRD — прямое уточнение ТЕМПА РАБОТЫ
  v4.27 для случая структурно неоднородного блока (не отменяет
  принцип "резать одним PRD там, где семья/блок структурно чиста").

PRD-047.42-APPLY-27 закрыт (Block B часть 1, группы 1-6, 18 значимых
  исходов + not_matched как сигнал провала): чистый классификатор,
  расширенный явным исходом not_matched (без ветки на call site —
  управление естественно уходит в ещё не вынесенный код); все 4
  self-метода остались исключительно в writer_agent.py; два физически
  разных условия для known_concept_correlation/neurostalking сохранены
  как 4 разных внутренних тега, объединены только на уровне вывода на
  call site (прямой прецедент APPLY-26); единственная оставшаяся прямая
  last_debug-запись метода закрыта патчем, применяемым ПЕРЕД self-
  вызовом, порядок байт подтверждён идентичным до/после.

Защищённые файлы (канонический список архитектора) — 24:
  14 из transfer brief v3
  + slice9  (f69c7217137ddc23ea1c09e7ba6dd9a1886d0249)
  + slice10 (dd34d823f61c608c3bd8a2be0fd48763cec2c838)
  + slice11 (fa776248a89124c8b34d7d94134a448bee9a3cdc)
  + slice12 (8a9544b784f16bb3b3968d2c787964b684e85838)
  + enforce_slice1 (eef347d07d2f3693fef1554a210cfa0e44526126)
  + enforce_slice2 (ea62e30cb721ed514cc501129bfc3857982a401f)
  + enforce_slice3 (41ddfbf851c540d82109890c58a8c418827a97fa)
  + enforce_slice4 (13abc089f91b90c7077c98a002543ebf132eda3f)
  + enforce_slice5 (ed697cdec60ef2cfa8282cd646fa812a387937ca)
  + enforce_slice6 (7336a62b8cc4580e1d5ea702fcdb07f1024b7a41 — снят
    архитектором сразу после приёмки APPLY-27, чтобы закрыть типовой
    лаг "хэш зафиксировать перед стартом следующего PRD" ДО начала
    APPLY-28).

Отложено сознательно (не потеряно):
- Block B часть 2 (883-1000, 118 строк, группы 7-12) — построчная
  разведка обязательна перед PRD, не доверять предварительной
  структурной прикидке буквально.
- _enforce_mvp_free_dialogue_compliance (225 строк на момент последней
  оценки) — после Block B, своя отдельная разведка с нуля.
- core_required_fields — вне серии.
- 4 больших файла; PRD-047.42b; PRD-047.43; PRD-047.45 — отдельный
  трек ПОСЛЕ Эпохи 2.
- writer_contract.py (979 строк) — под порогом, отдельное решение.
- docs/testing.md, S7, Panic/Medical Escalation, трейс-поля — без изменений.

---

## БЛОК Б (одна новая строка журнала — вставить над существующей первой записью)

- 2026-07-23 v4.30 -> v4.31: PRD-047.42-APPLY-27 принят ACCEPTED —
  Block B часть 1 закрыта (группы 1-6, 93 строки, 18 значимых исходов +
  not_matched). Единственная во всём методе прямая last_debug-запись
  закрыта вместе с этим срезом, порядок байт подтверждён архитектором
  независимо (включая до/после сравнение позиции ключа). Техническое
  решение архитектора о дроблении Block B на 2 PRD по структурному шву
  задокументировано как ADR-126. Хэш writer_agent_enforce_slice6.py
  зафиксирован архитектором сразу после приёмки (закрывает типовой лаг
  до старта следующего PRD). Следующий шаг — APPLY-28 (Block B часть 2,
  группы 7-12, самый структурно сложный кусок метода — группа 9 с 4
  внутренними локальными).
