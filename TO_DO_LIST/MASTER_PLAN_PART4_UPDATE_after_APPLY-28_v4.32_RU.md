# Обновление Части 4 мастер-плана — после PRD-047.42-APPLY-28 (v4.32)

Формат передачи — ДВА БЛОКА, применяются раздельно:
- **Блок А** — полное новое тело раздела "ЧАСТЬ 4 — ГДЕ МЫ СЕЙЧАС" (без
  журнала). Заменить им весь текст раздела ЧАСТЬ 4 от строки "Эпоха: 1
  (Консолидация)." до строки "Отложено сознательно (не потеряно): ...
  docs/testing.md, S7, Panic/Medical Escalation, трейс-поля — без
  изменений." включительно.
- **Блок Б** — ОДНА новая строка журнала. Вставить НАД существующей
  первой записью журнала (старые записи НЕ трогать).

Версия в шапке документа: v4.31 -> v4.32.

---

## БЛОК А (заменить весь текст раздела ЧАСТЬ 4, кроме журнала)

Эпоха: 1 (Консолидация).
Последний принятый PRD: PRD-047.42-APPLY-28 (ACCEPTED, commits
  2ee0c71d / 3fa58c75; план до v4.31 — 430f8195). Верифицировано
  архитектором независимо: классификатор дословно сверен с PRD (17
  тегов, включая not_matched); writer_agent.py call site байт-в-байт
  совпадает с PRD; строка "return text" (физический конец метода)
  подтверждена нетронутой; собственный пересчёт снапшотов напрямую
  импортом build_before_snapshot()/build_after_snapshot() идентичен
  закоммиченным (17 cases, before==after — 0 расхождений); специальный
  edge-case теста группы 9 (list_like=True, first_item=None, text="-\n")
  подтверждён на месте и корректен; 26 новых тестов (24 прямых + 2
  контрактных) и весь контрактный набор серии (147 тестов) — зелёные;
  канонический прогон pytest tests/ -k writer -q — ровно 19 падений,
  список байт-в-байт идентичен списку ДО этого PRD (регрессии нет); 24
  защищённых файла не тронуты; логи APPLY-20..27 неприкосновенны.
  Дополнительно: архитектор прогнал дифференциальный фаззинг-тест
  (40 000 случайных комбинаций против извлечённого дословно
  оригинального кода строк 883-999) плюс отдельный целевой тест ровно
  на этот edge-case ДО передачи PRD Исполнителю — 0 расхождений;
  задокументировано в docs/DECISIONS.md как ADR-127 (написан
  Исполнителем, содержательно точен).

**MILESTONE: декомпозиция _enforce_answer_compliance ЗАВЕРШЕНА
ПОЛНОСТЬЮ.** Метод (был крупнейшим в writer_agent.py) полностью
покрыт извлечёнными классификаторами/helper'ами по семи срезам
(APPLY-21, 22, 23, 24, 26, 27, 28), одним техническим решением
"остаётся на месте" (mvp_free_branch_handoff, ADR-125) и одним
гигиеническим микро-PRD (APPLY-25). Block B (оба среза, APPLY-27+28)
закрыт полностью.

writer_agent.py = 1238 строк. _enforce_answer_compliance — строки
  577-977 (метод целиком декомпозирован до классификаторов + call site;
  строка 977 "return text" — финальный fallback, не выносится).

Следующий шаг: НЕ ещё один срез этого метода — boundary mapping и
  декомпозиция _enforce_mvp_free_dialogue_compliance (строки 979-1203
  на HEAD 3fa58c75, 225 строк, отдельный метод со своей сигнатурой —
  ~35 именованных keyword-параметров, НЕ (self, response_text,
  contract) как у предыдущего метода). Метод НЕ картирован вообще —
  своя отдельная разведка с нуля, не переносить предположения о
  структуре или механиках из _enforce_answer_compliance (карта APPLY-20
  дважды подтверждённо занижала фактическую структуру — тот же принцип
  осторожности применим к любой новой карте).
Шлюз в Эпоху 2: облегчённый (владелец-решение #4). Ориентир до шлюза на
  момент этой записи: ~2-4 PRD (_enforce_mvp_free_dialogue_compliance —
  1-2 PRD по факту разведки, возможен запас).

Корпус законов Fable: получен полностью (R01-R07), НЕ ратифицирован.

ВЛАДЕЛЕЦ-РЕШЕНИЕ (2026-07-13): 4 больших файла — трек после Эпохи 2.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #2 (2026-07-14): Сценарий А, DoD §5.6 не закрывается
  буквально — сознательно, не blocker.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #3 (2026-07-16): await create_agent_completion
  остаётся в _call_llm.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #4 (2026-07-16): облегчённый шлюз Эпохи 2 — только
  Сценарий А; PRD-047.42b/43/45 — отдельный трек после Эпохи 2.
ТЕМП РАБОТЫ (2026-07-16, v4.27): семьи/блоки без подтверждённой
  разведкой скрытой сложности режутся одним PRD.
ТЕХНИЧЕСКОЕ РЕШЕНИЕ АРХИТЕКТОРА (2026-07-23, ADR-126): структурно
  неоднородный блок дробится по естественному шву, а не форсируется в
  один PRD (уточнение ТЕМПА РАБОТЫ v4.27).
ТЕХНИЧЕСКОЕ РЕШЕНИЕ АРХИТЕКТОРА (2026-07-23, ADR-127): вложенное
  "может-провалиться внутри может-провалиться" переносится в helper
  буквально (вложенный if без else на обоих уровнях), без упрощения в
  одно условие — упрощение меняет поведение на edge-case с частичным
  совпадением. Новый именованный паттерн для будущих срезов при
  встрече аналогичной структуры.

PRD-047.42-APPLY-28 закрыт (Block B часть 2, группы 7-12, 16 значимых
  исходов + not_matched): чистый классификатор; not_matched без ветки
  на call site — управление уходит прямо к "return text" (физический
  конец метода, не тронут); модульная функция
  starts_with_mechanical_revoicing вызывается напрямую внутри helper'а
  (не self, безопасно); новый под-паттерн "maybe-return внутри
  maybe-return" (группа 9) воспроизведён буквально и защищён отдельным
  edge-case тестом; все self-методы остались исключительно в
  writer_agent.py.

Защищённые файлы (канонический список архитектора) — 25:
  14 из transfer brief v3
  + slice9  (f69c7217137ddc23ea1c09e7ba6dd9a1886d0249)
  + slice10 (dd34d823f61c608c3bd8a2be0fd48763cec2c838)
  + slice11 (fa776248a89124c8b34d7d94134a448bee9a3cdc)
  + slice12 (8a9544b784f16bb3b3968d2c787964b684e85838)
  + enforce_slice1 (eef347d07d2f3693fef1554a210cfa0e44526126)
  + enforce_slice2 (ea62e30cb721ed514cc501129bfc3857982a401f)
  + enforce_slice3 (41ddfbf851c540d82109890c58a8c418827a97fa)
  + enforce_slice4 (13abc089f91b90c7077c98a002543ebf132eda3f)
  + enforce_slice5 (ed697cdec60ef2cfa8282cd646fa812a387937ca)
  + enforce_slice6 (7336a62b8cc4580e1d5ea702fcdb07f1024b7a41)
  + enforce_slice7 (5c7ce58e79684714d9082ae02e5ce6f46baa2915 — снят
    архитектором сразу после приёмки APPLY-28; это ПОСЛЕДНИЙ файл
    декомпозиции _enforce_answer_compliance — следующий защищённый
    файл появится только вместе с первым срезом
    _enforce_mvp_free_dialogue_compliance).

Отложено сознательно (не потеряно):
- _enforce_mvp_free_dialogue_compliance (979-1203, 225 строк) —
  boundary mapping с нуля, своя отдельная разведка, не картирован
  вообще.
- core_required_fields — вне серии.
- 4 больших файла; PRD-047.42b; PRD-047.43; PRD-047.45 — отдельный
  трек ПОСЛЕ Эпохи 2.
- writer_contract.py (979 строк) — под порогом, отдельное решение.
- docs/testing.md, S7, Panic/Medical Escalation, трейс-поля — без изменений.

---

## БЛОК Б (одна новая строка журнала — вставить над существующей первой записью)

- 2026-07-23 v4.31 -> v4.32: PRD-047.42-APPLY-28 принят ACCEPTED —
  Block B часть 2 закрыта (группы 7-12, 117 строк, 16 значимых исходов
  + not_matched), Block B закрыт ПОЛНОСТЬЮ. **MILESTONE: декомпозиция
  _enforce_answer_compliance завершена полностью** (7 срезов + 1
  техническое решение "на месте" + 1 гигиенический PRD). Новый
  под-паттерн "maybe-return внутри maybe-return" (группа 9) воспроизведён
  буквально и защищён отдельным edge-case тестом, задокументирован как
  ADR-127. Хэш writer_agent_enforce_slice7.py зафиксирован архитектором
  сразу после приёмки — последний защищённый файл этой декомпозиции.
  Следующий шаг — НЕ ещё один срез, а boundary mapping с нуля
  _enforce_mvp_free_dialogue_compliance (979-1203, 225 строк, другая
  сигнатура — не переносить предположения о структуре).
