# Обновление Части 4 мастер-плана — после PRD-047.42-APPLY-29 и APPLY-30 (v4.33)

Формат — ДВА БЛОКА: Блок А заменяет весь текст раздела ЧАСТЬ 4 (без
журнала); Блок Б — одна новая строка журнала (вставить над первой
существующей записью). Версия в шапке: v4.32 -> v4.33.

---

## БЛОК А (заменить весь текст раздела ЧАСТЬ 4, кроме журнала)

Эпоха: 1 (Консолидация).
Последний принятый PRD: PRD-047.42-APPLY-30 (ACCEPTED, commits
  8c0878ba / 3bbe6571; APPLY-29 — 27861f49 / c8bd2491; план до v4.32 —
  05c9de95). Оба PRD верифицированы архитектором независимо в одном
  цикле: оба helper'а байт-в-байт совпадают с PRD (включая вычисляемое,
  не литеральное значение answer_fit_repair_applied в APPLY-30); call
  site в writer_agent.py байт-в-байт совпадает, перенос мутации text/
  lowered_text подтверждён (переприсвоение сразу после вызова
  классификатора, до проверки outcome); собственный пересчёт снапшотов
  напрямую импортом идентичен закоммиченным (17/17 cases в каждом PRD,
  before==after — 0 расхождений); специальные тесты на мутацию
  (APPLY-29) и на вычисляемое значение last_debug (APPLY-30, оба bool)
  подтверждены на месте и корректны; 45 новых тестов и весь контрактный
  набор серии (151 тест) — зелёные; канонический прогон pytest tests/
  -k writer -q — ровно 19 падений, список байт-в-байт идентичен списку
  ДО обоих PRD вместе (регрессии нет); 25 защищённых файлов не тронуты;
  логи APPLY-20..29 неприкосновенны. Задокументировано как ADR-128
  (перенос мутации через классификатор) и ADR-129 (единственное
  обоснованное исключение "self может быть чистой функцией" — явно НЕ
  создаёт слепой прецедент для будущих self-методов).

**MILESTONE #2: декомпозиция `_enforce_mvp_free_dialogue_compliance`
ЗАВЕРШЕНА ПОЛНОСТЬЮ.** Вместе с MILESTONE #1 (APPLY-28,
`_enforce_answer_compliance`) — **ОБА крупных метода writer_agent.py
полностью декомпозированы.** writer_agent.py = 1217 строк.

Следующий шаг — НЕ технический выбор архитектора. По итогам обоих
milestone'ов требуется:
1. Сверка Сценария А (DoD §5.6) целиком с фактическим состоянием файла
   — были ли по пути найдены новые основания пересмотреть облегчённый
   критерий шлюза (владелец-решение #4) или он остаётся в силе как есть.
2. Обсуждение с владельцем: открывать ли шлюз в Эпоху 2 сейчас, или
   есть дополнительные критерии/паузы, которые владелец хочет внести
   до перехода.
Это стратегическая развилка, не решается архитектором единолично —
ждём владельца.

Корпус законов Fable: получен полностью (R01-R07), НЕ ратифицирован.

ВЛАДЕЛЕЦ-РЕШЕНИЕ (2026-07-13): 4 больших файла — трек после Эпохи 2.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #2 (2026-07-14): Сценарий А, DoD §5.6 не закрывается
  буквально — сознательно, не blocker.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #3 (2026-07-16): await create_agent_completion
  остаётся в _call_llm.
ВЛАДЕЛЕЦ-РЕШЕНИЕ #4 (2026-07-16): облегчённый шлюз Эпохи 2 — только
  Сценарий А; PRD-047.42b/43/45 — отдельный трек после Эпохи 2.
  **ГОТОВ К ПРОВЕРКЕ**: оба крупных метода закрыты, критерий формально
  достижим — финальное решение об открытии шлюза за владельцем (см.
  выше).
ТЕМП РАБОТЫ (2026-07-16, v4.27; уточнено ADR-126, 2026-07-23): семьи/
  блоки без подтверждённой разведкой скрытой сложности режутся одним
  PRD; структурно неоднородные и заметно более длинные блоки — по
  естественному шву.
ТЕХНИЧЕСКОЕ РЕШЕНИЕ АРХИТЕКТОРА (ADR-127, 2026-07-23): вложенное
  "может-провалиться внутри может-провалиться" переносится буквально.
ТЕХНИЧЕСКОЕ РЕШЕНИЕ АРХИТЕКТОРА (ADR-128, 2026-07-23): мутация общей
  локальной переменной, переживающая непрошедшую группу, переносится
  через результат классификатора безусловно (updated_*-поля), с
  переприсвоением на call site ДО проверки outcome — новый эталонный
  паттерн для будущих срезов с общими мутируемыми локальными.
ТЕХНИЧЕСКОЕ РЕШЕНИЕ АРХИТЕКТОРА (ADR-129, 2026-07-23): self-метод
  может быть импортирован в helper как чистая функция, которую он
  тонко оборачивает, — но только после явной проверки отсутствия
  доступа к self, и только как разовое обоснованное исключение, НЕ
  как общее правило для будущих self-методов.
ТЕМП РАБОТЫ (2026-07-23, ускорение по просьбе владельца): для
  структурно простых участков — без отдельного boundary-mapping PRD
  (разведка + дизайн + PRD готовятся в одном заходе архитектором);
  облегчённая дифференциальная проверка (десятки тысяч, не
  многие-многие десятки тысяч комбинаций) для типовых механик;
  независимая пост-приёмочная верификация (хэши, снапшоты, тесты,
  канонический прогон) — БЕЗ сокращений, не подлежит ускорению.

PRD-047.42-APPLY-29 закрыт (mvp-метод Part 1, группы A-J, 16 значимых
  исходов + not_matched): впервые в обеих декомпозициях — перенос
  мутации text/lowered_text через классификатор (ADR-128);
  offer_repair_context подтверждён унаследованным мёртвым кодом,
  задокументирован, не тронут.
PRD-047.42-APPLY-30 закрыт (mvp-метод Part 2, группы K-P + финальный
  fallback метода, 8 значимых исходов + not_matched=физический конец
  метода): единственный случай вычисляемого (не литерального) значения
  last_debug-патча во всей декомпозиции; единственное обоснованное
  исключение "self как чистая функция" (ADR-129).

Защищённые файлы (канонический список архитектора) — 27:
  25 из предыдущей записи (см. v4.32)
  + mvp_slice1 (06e849aff414ec40872c4fbb0fe6a81df235a8a5 — снят
    архитектором сразу после приёмки APPLY-30)
  + mvp_slice2 (0eb6634a807ad6e78e249aa24af516f75c8ed869 — снят
    архитектором сразу после приёмки APPLY-30).

Отложено сознательно (не потеряно):
- Открытие шлюза в Эпоху 2 — ждёт решения владельца (см. выше).
- core_required_fields — вне серии.
- 4 больших файла; PRD-047.42b; PRD-047.43; PRD-047.45 — отдельный
  трек ПОСЛЕ Эпохи 2 (условие "после" зависит от решения о шлюзе).
- writer_contract.py (979 строк) — под порогом, отдельное решение.
- docs/testing.md, S7, Panic/Medical Escalation, трейс-поля — без изменений.

---

## БЛОК Б (одна новая строка журнала — вставить над существующей первой записью)

- 2026-07-23 v4.32 -> v4.33: PRD-047.42-APPLY-29 и PRD-047.42-APPLY-30
  приняты ACCEPTED — `_enforce_mvp_free_dialogue_compliance`
  декомпозирован полностью (Part 1: группы A-J, перенос мутации text/
  lowered_text через классификатор, ADR-128; Part 2: группы K-P +
  финал метода, единственное вычисляемое значение last_debug-патча,
  единственное обоснованное исключение self-как-чистая-функция,
  ADR-129). **MILESTONE #2, вместе с MILESTONE #1 (APPLY-28) — оба
  крупных метода writer_agent.py полностью декомпозированы.** Оба
  хэша (mvp_slice1, mvp_slice2) зафиксированы архитектором сразу после
  приёмки. Следующий шаг — НЕ очередной срез: сверка Сценария А целиком
  и решение владельца об открытии шлюза в Эпоху 2 (стратегическая
  развилка, не единоличный технический выбор архитектора).
