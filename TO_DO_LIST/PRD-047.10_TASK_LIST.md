# PRD-047.10 TASK LIST

## 1. Source Gates
- [x] Verify `main` branch and clean/expected workspace state.
- [x] Confirm PRD-047.9 baseline (`status=passed`) and required runtime files exist.
- [x] Validate docs continuity (`PROJECT_STATE`, `ROADMAP`) points to PRD-047.10.
- [x] Save gate snapshot to `TO_DO_LIST/logs/PRD-047.10/source_gates.json`.

## 2. Runtime Policy Implementation
- [x] Extend unified `dialogue_policy` with `human_like_answer_policy` for `mvp_free_dialogue`.
- [x] Add/extend detectors for explicit direct answer, dissatisfaction/sarcasm, summary request, and practical application requests.
- [x] Add `constraint_resolution` metadata (advisory/overruled constraints) for trace/admin visibility.
- [x] Keep `safe_guided` conservative and backward-compatible.

## 3. Writer Prompt/Behavior Calibration
- [x] Inject Human-like Writer Autonomy block into writer prompt context.
- [x] Ensure conflicting hard constraints are softened in MVP when user explicitly requests depth/format.
- [x] Ensure response shape mapping works for key cases (anger truth, direct concrete answer, sarcasm repair, full summary, explain/apply concept).
- [x] Keep one-step and short-support behavior for explicit requests.

## 4. Planner/Active-line/Contract Alignment
- [x] Keep planner/diagnostic/active-line advisory in MVP profile.
- [x] Preserve minimal safety baseline and no new blocking/rewriter logic.
- [x] Ensure no new runtime path and no new LLM agent.

## 5. Admin + Trace Visibility
- [x] Extend `/api/admin/runtime/effective` payload to include new human-like policy and constraint resolution fields.
- [x] Add/align web admin typing/UI rendering if payload schema changed.
- [x] Expose trace-safe metadata pointers for policy enablement and overrule behavior.

## 6. Dataset + Runner
- [x] Add PRD-047.10 eval dataset with required critical live cases.
- [x] Add/extend runner `scripts/run_prd_047_10_human_like_eval.py` with `dry/direct/live` modes.
- [x] Generate `human_like_eval_dry/direct/live` artifacts and `human_like_eval_live.md`.
- [x] Generate sanitized `sample_answer_review.md`.

## 7. Tests
- [x] Add/update unit tests for policy, detectors, constraint resolution, and behavior mapping.
- [x] Add/update prompt contract tests for MVP autonomy block and softened constraints.
- [x] Add/update integration tests for admin/runtime policy payload and unified-path invariants.
- [x] Run targeted pytest set and record output in `TO_DO_LIST/logs/PRD-047.10/test_command_output.txt`.

## 8. No-Mutation + Hygiene
- [x] Generate `no_mutation_proof.json` with PRD-047.10 required flags.
- [x] Generate `artifact_encoding_hygiene.json`.
- [x] Ensure no secrets/raw provider payload/db/cache committed.

## 9. Reports + Docs
- [x] Create `TO_DO_LIST/reports/PRD-047.10_IMPLEMENTATION_REPORT.md`.
- [x] Create `TO_DO_LIST/reports/PRD-047.10_NEXT_PRD_RECOMMENDATION.md`.
- [x] Update `docs/PROJECT_STATE.md`, `docs/ROADMAP.md`, `docs/PRD_INDEX.md`.
- [x] Update `docs/DECISIONS.md` and profile docs if architecture/policy decision changed.

## 10. Delivery
- [x] Main commit and push to `origin/main`.
- [x] Post-push metadata sync (task/report/PRD index commit hash + push status).
- [x] Micro-commit and micro-push for metadata sync.
