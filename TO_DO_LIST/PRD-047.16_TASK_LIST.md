# PRD-047.16 Task List

## 1. Source Gates
- [x] Read `TO_DO_LIST/PRD-047.16_Mechanism_Aware_KB_Preparation_Chunk_Metadata_v1_RU.md`.
- [x] Verify git status and recent history include `PRD-047.15-HF2-R2`.
- [x] Audit current `Bot_data_base` and `bot_psychologist` knowledge/governance/retrieval modules.
- [x] Inventory real local processed blocks / registry / Chroma presence.
- [x] Save `source_audit.json` and `source_audit.md`.

## 2. Metadata Foundation
- [ ] Add `MechanismAwareChunkMetadata v1` schema and controlled vocabularies in existing `Bot_data_base/knowledge_governance` layer.
- [ ] Add backward-compatible adapter from legacy chunk/block metadata to normalized mechanism-aware metadata.
- [ ] Preserve unknown legacy fields in `legacy_metadata` / `extra_metadata`.
- [ ] Add invariant validation for `chunk_type`, `allowed_use`, `quote_policy`, `visibility`, `depth_level`, and practice/safety rules.

## 3. Dry-Run Audit Pipeline
- [ ] Add read-only audit runner for fixture mode and real-sample mode.
- [ ] Generate schema snapshot, normalized sample preview, distribution, completeness, anti-heuristic, and no-mutation artifacts.
- [ ] Keep outputs sanitized: no full raw chunk text, no DB writes, no Chroma writes, no runtime path changes.

## 4. Tests
- [ ] Add schema validation tests for core chunk types and invalid combinations.
- [ ] Add adapter tests for legacy preservation, sanitized preview, and no-crash optional-field gaps.
- [ ] Add dry-run runner tests for fixture mode artifact creation.
- [ ] Add anti-heuristic scan test for new modules.
- [ ] Run targeted regression tests and capture commands/results in `test_command_output.txt`.

## 5. Docs And Delivery
- [ ] Update `docs/PROJECT_STATE.md`, `docs/ROADMAP.md`, `docs/PRD_INDEX.md`, and `docs/DECISIONS.md`.
- [ ] Create implementation report and next PRD recommendation.
- [ ] Commit implementation and push to `origin/main`.
- [ ] Update task list / PRD metadata after push.
- [ ] Commit and push metadata micro-sync.
- [ ] Verify clean tree and `HEAD == origin/main`.
