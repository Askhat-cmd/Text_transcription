# PRD-047.42-APPLY-11 Task List

## Result
- Completed in main commit `0c35d2f`.
- Main push status: `pushed_to_origin_main`.

## Scope
- Extract `_call_llm` argument families `unified_dialogue_policy`, `dialogue_act_and_offer_state`, `dialogue_style_and_answer_obligation`, `diagnostic_card_and_writer_move`, and `context_budget_and_profile` from `bot_psychologist/bot_agent/multiagent/agents/writer_agent.py`.
- Preserve byte-identical behavior for the accepted 3-scenario `_call_llm` snapshot, including full `last_debug` and exact `user_prompt`.
- Keep `conversation_context=formatted_context` inline and unchanged.

## Implementation Stages
1. Tasking and source alignment
- Read PRD, current live `_call_llm` block, APPLY-6/7/9/10 runners and contracts.
- Confirm current live boundaries and dependency assumptions before editing.

2. Extraction
- Add `bot_psychologist/bot_agent/multiagent/agents/writer_agent_call_llm_slice4.py`.
- Introduce `CallLLMSlice4PolicyAndDialogueStateInputs`.
- Move the 39 accepted prompt-argument expressions into `_extract_call_llm_slice4_policy_and_dialogue_state(ctx, context_meta, dialogue_profile)`.
- Replace the inline `WRITER_USER_TEMPLATE.format(...)` kwargs in `writer_agent.py` with explicit `slice4_inputs.<field>` references while keeping `conversation_context=formatted_context` in place.

3. Tests and runners
- Add direct helper tests for slice 4, including the intentional non-`str()` behavior of `writer_move_instruction_summary`.
- Add `TO_DO_LIST/tools/run_prd_047_42_apply_11_call_llm_slice4.py`.
- Add contract tests for the new runner/reports.
- Re-run prior contract suites for APPLY-6, APPLY-7, APPLY-9, and APPLY-10.

4. Reports and docs
- Generate before/after snapshot artifacts under `TO_DO_LIST/logs/PRD-047.42-APPLY-11/`.
- Generate `no_mutation_proof.md`, `extraction_log.md`, `implementation_report.md`, `next_recommendation.md`.
- Update `docs/PROJECT_STATE.md`, `docs/ROADMAP.md`, `docs/PRD_INDEX.md`, `docs/DECISIONS.md`.
- Mark completion metadata in the PRD file itself.

5. Delivery
- Commit main implementation.
- Push to `main`.
- Apply completion metadata/doc tail if needed and micro-push.
- Verify `HEAD == origin/main` and clean working tree.

## Test Plan
- `python -m pytest bot_psychologist/tests/multiagent/test_writer_agent_call_llm_slice4.py -q`
- `python -m pytest bot_psychologist/tests/contract/test_prd_047_42_apply_11_call_llm_slice4.py -q`
- `python -m pytest bot_psychologist/tests/contract/test_prd_047_42_apply_6_call_llm_boundary_mapping.py -q`
- `python -m pytest bot_psychologist/tests/contract/test_prd_047_42_apply_7_call_llm_slice1.py -q`
- `python -m pytest bot_psychologist/tests/contract/test_prd_047_42_apply_9_call_llm_slice2.py -q`
- `python -m pytest bot_psychologist/tests/contract/test_prd_047_42_apply_10_call_llm_slice3.py -q`
- `python TO_DO_LIST/tools/run_prd_047_42_apply_11_call_llm_slice4.py --snapshot-only --snapshot-path TO_DO_LIST/logs/PRD-047.42-APPLY-11/call_llm_snapshot_before.json`
- `python TO_DO_LIST/tools/run_prd_047_42_apply_11_call_llm_slice4.py --snapshot-only --snapshot-path TO_DO_LIST/logs/PRD-047.42-APPLY-11/call_llm_snapshot_after.json --match-before TO_DO_LIST/logs/PRD-047.42-APPLY-11/call_llm_snapshot_before.json`
- Compare `call_llm_snapshot_before.json` vs `call_llm_snapshot_after.json` byte-for-byte.
- Compare extracted `user_prompt` values line-by-line and byte-for-byte before/after.

## Checks
- No semantic changes to `WRITER_USER_TEMPLATE`, prompt ordering, or defaults.
- `conversation_context=formatted_context` stays inline and unmoved.
- `writer_move_instruction_summary` remains `ctx.get(...) or "нет"` without new `str()`.
- No new runtime paths, flags, agents, DB mutations, or prompt-model changes.
- `no_mutation_proof.md` must show `0 changed paths` across protected files except `writer_agent.py`.
- Prior accepted helper/mixin/admin/contract files remain unchanged.
