# PRD-047.42-APPLY-17 Task List

## Result
- Completed.
- Verdict: `accepted_with_warning`.
- The required `_call_llm` snapshot and exact `user_prompt` stayed byte-identical.
- The PRD-required canonical `pytest tests/ -k writer -q` baseline of `19` known failures was reproduced on a clean isolated worktree with the APPLY-17 source/test files copied in.
- The owner workspace itself reports `14` failures because `5` environment-sensitive writer tests pass under the full local sibling-workspace context; this is recorded honestly as a delivery warning, not as slice-10 behavior drift.

## Scope
- Extract `_call_llm` cluster `prompt_constraint_append_and_debug_bookkeeping` from `writer_agent.py`.
- Preserve byte-identical `_call_llm` snapshot and exact `user_prompt` text for the accepted 3-scenario harness.
- Preserve insertion order of the `last_debug` keys via one ordered patch dict and one `self.last_debug.update(...)`.
- Keep `start_ts = time.perf_counter()` and the following runtime-settings/provider/response clusters out of scope.

## Implementation Stages
1. Source alignment
- Re-read the live `_call_llm` lines `531-578` and confirm the slice boundaries against the current file.
- Confirm the three critical invariants from the PRD: `user_prompt` mutation branch, ordered `last_debug_patch`, and passthrough-by-reference for `overruled_constraints`.
- Confirm whether `format_prompt_constraint_section_v1` becomes unused in `writer_agent.py` after extraction.

2. Extraction
- Add `bot_psychologist/bot_agent/multiagent/agents/writer_agent_call_llm_slice10.py`.
- Introduce `CallLLMSlice10PromptConstraintAndDebugBookkeepingResult`.
- Move the accepted cluster into `_apply_call_llm_slice10_prompt_constraint_and_debug_bookkeeping(...)`.
- Replace the inline block in `writer_agent.py` with one helper call, explicit `user_prompt` reassignment, and one `self.last_debug.update(...)`.
- Remove the direct `format_prompt_constraint_section_v1` import from `writer_agent.py` only if no other usages remain.

3. Tests and runner
- Add direct helper tests in `bot_psychologist/tests/multiagent/test_writer_agent_call_llm_slice10.py`.
- Add contract runner test in `bot_psychologist/tests/contract/test_prd_047_42_apply_17_call_llm_slice10.py`.
- Add `TO_DO_LIST/tools/run_prd_047_42_apply_17_call_llm_slice10.py`.
- Capture `call_llm_snapshot_before.json` and `call_llm_snapshot_after.json`.
- Generate `user_prompt_equivalence.md`, `no_mutation_proof.md`, `extraction_log.md`, `implementation_report.md`, and `next_recommendation.md`.

4. Verification
- Run the direct slice-10 helper tests.
- Run the slice-10 contract runner tests.
- Run `py_compile` for the touched Python files.
- Re-run the clean-tree historical contract suite for APPLY-6 + APPLY-7 + APPLY-9 + APPLY-10 + APPLY-11 + APPLY-12 + APPLY-13 + APPLY-14 + APPLY-15 + APPLY-16 + APPLY-17.
- Run the canonical broad writer subset exactly as specified by the PRD and record the real outcome honestly.

5. Docs and delivery
- Update `docs/PROJECT_STATE.md`, `docs/ROADMAP.md`, `docs/PRD_INDEX.md`, and `docs/DECISIONS.md`.
- Update the PRD completion metadata and mark the task list result.
- Commit the main implementation and push to `main`.
- Re-run the historical contract suite on clean tree if needed, then micro-push metadata/docs finalization.
- Verify `HEAD == origin/main` and clean working tree.

## Test Plan
- `python -m pytest bot_psychologist/tests/multiagent/test_writer_agent_call_llm_slice10.py -q`
- `python -m pytest bot_psychologist/tests/contract/test_prd_047_42_apply_17_call_llm_slice10.py -q`
- `python TO_DO_LIST/tools/run_prd_047_42_apply_17_call_llm_slice10.py --snapshot-only --snapshot-path TO_DO_LIST/logs/PRD-047.42-APPLY-17/call_llm_snapshot_after.json --match-before TO_DO_LIST/logs/PRD-047.42-APPLY-17/call_llm_snapshot_before.json --comparison-report TO_DO_LIST/logs/PRD-047.42-APPLY-17/user_prompt_equivalence.md`
- `python -m pytest bot_psychologist/tests/contract/test_prd_047_42_apply_6_call_llm_boundary_mapping.py bot_psychologist/tests/contract/test_prd_047_42_apply_7_call_llm_slice1.py bot_psychologist/tests/contract/test_prd_047_42_apply_9_call_llm_slice2.py bot_psychologist/tests/contract/test_prd_047_42_apply_10_call_llm_slice3.py bot_psychologist/tests/contract/test_prd_047_42_apply_11_call_llm_slice4.py bot_psychologist/tests/contract/test_prd_047_42_apply_12_call_llm_slice5.py bot_psychologist/tests/contract/test_prd_047_42_apply_13_call_llm_slice6.py bot_psychologist/tests/contract/test_prd_047_42_apply_14_call_llm_slice7.py bot_psychologist/tests/contract/test_prd_047_42_apply_15_call_llm_slice8.py bot_psychologist/tests/contract/test_prd_047_42_apply_16_call_llm_slice9.py bot_psychologist/tests/contract/test_prd_047_42_apply_17_call_llm_slice10.py -q`
- `pytest tests/ -k writer -q`

## Checks
- `user_prompt` mutation branch is preserved exactly, including `\n\n` join behavior.
- `last_debug_patch` contains exactly the original `18` keys in the original insertion order.
- `overruled_constraints` is passed through by reference, not copied.
- `format_prompt_constraint_section_v1` import removal is done only if the file has no remaining usage.
- No changes to `prompt_constraint_section.py`, runtime settings, provider dispatch, response unpack/return, `_enforce_*`, or the deferred god-files.
- `no_mutation_proof.md` must show `0 changed paths` across the protected files from APPLY-16.
