# PRD-047.42-APPLY-19 Task List

## Result
- Completed.
- Verdict: `accepted_with_warning`.
- The final `_call_llm` response-unpack/cost/debug-tail snapshot stayed byte-identical against the accepted APPLY-18 baseline.
- Direct slice-12 helper tests passed `3/3`, new contract runner tests passed `3/3`, and `py_compile` passed for the touched Python files.
- The clean-tree historical APPLY-6..19 contract rerun passed `37/37` after the main implementation commit landed on `main`.
- The canonical isolated clean-worktree `pytest tests/ -k writer -q` baseline reports `19 failed, 213 passed, 2010 deselected, 190 warnings`; this is the PRD acceptance baseline and not a slice-12 regression.
- The owner workspace canonical `pytest tests/ -k writer -q` run reports `14 failed, 218 passed, 2010 deselected, 346 warnings`; this is recorded honestly as an environment warning and not as slice-12 behavior drift.

## Scope
- Extract the final `_call_llm` cluster `response_unpack_cost_and_return` from `writer_agent.py`.
- Preserve byte-identical snapshots, byte-identical `user_prompt`, and deterministic `duration_ms=123` under the accepted harness.
- Preserve the exact `13`-key `last_debug` patch order and the single keyword call into `self._estimate_cost`.
- Leave `provider_dispatch`, `return llm_response`, `_enforce_*`, prompts, DB/runtime/model/safety surfaces, and protected prior slices out of scope.

## Implementation Stages
1. Source alignment
- Re-read the live `_call_llm` tail and confirm the exact cluster boundaries.
- Confirm the three PRD-critical invariants: module-level `time.perf_counter`, callable handoff for `_estimate_cost`, and the exact `13` debug keys in insertion order.
- Confirm `import time` in `writer_agent.py` must stay because `start_ts = time.perf_counter()` remains inline.

2. Extraction
- Add `bot_psychologist/bot_agent/multiagent/agents/writer_agent_call_llm_slice12.py`.
- Introduce `CallLLMSlice12ResponseUnpackAndCostResult`.
- Move the response unpack, token tuple extraction, cost estimation, duration calculation, and ordered debug patch into `_apply_call_llm_slice12_response_unpack_cost_and_bookkeeping(...)`.
- Replace the inline block in `writer_agent.py` with one helper call, one explicit `llm_response` reassignment, and one `self.last_debug.update(...)`.

3. Tests and runner
- Add direct helper tests in `bot_psychologist/tests/multiagent/test_writer_agent_call_llm_slice12.py`.
- Add contract runner tests in `bot_psychologist/tests/contract/test_prd_047_42_apply_19_call_llm_slice12.py`.
- Add `TO_DO_LIST/tools/run_prd_047_42_apply_19_call_llm_slice12.py`.
- Capture `call_llm_snapshot_before.json` and `call_llm_snapshot_after.json`.
- Generate `user_prompt_equivalence.md`, `no_mutation_proof.md`, `extraction_log.md`, `implementation_report.md`, and `next_recommendation.md`.

4. Verification
- Run the direct slice-12 helper tests.
- Run the slice-12 contract runner tests.
- Run `py_compile` for touched Python files.
- Re-run the clean-tree historical contract suite for APPLY-6 + APPLY-7 + APPLY-9 + APPLY-10 + APPLY-11 + APPLY-12 + APPLY-13 + APPLY-14 + APPLY-15 + APPLY-16 + APPLY-17 + APPLY-18 + APPLY-19.
- Run the canonical broad writer subset `pytest tests/ -k writer -q` and record the real outcome honestly.

5. Docs and delivery
- Update `docs/PROJECT_STATE.md`, `docs/ROADMAP.md`, `docs/PRD_INDEX.md`, and `docs/DECISIONS.md`.
- Update PRD completion metadata and mark this task list complete.
- Commit the main implementation and push to `main`.
- Re-run the historical contract suite on a clean tree if needed, then micro-push delivery metadata.
- Verify `HEAD == origin/main` and clean working tree.

## Test Plan
- `python -m pytest bot_psychologist/tests/multiagent/test_writer_agent_call_llm_slice12.py -q`
- `python -m pytest bot_psychologist/tests/contract/test_prd_047_42_apply_19_call_llm_slice12.py -q`
- `python TO_DO_LIST/tools/run_prd_047_42_apply_19_call_llm_slice12.py --snapshot-only --snapshot-path TO_DO_LIST/logs/PRD-047.42-APPLY-19/call_llm_snapshot_before.json`
- `python TO_DO_LIST/tools/run_prd_047_42_apply_19_call_llm_slice12.py --snapshot-only --snapshot-path TO_DO_LIST/logs/PRD-047.42-APPLY-19/call_llm_snapshot_after.json --match-before TO_DO_LIST/logs/PRD-047.42-APPLY-19/call_llm_snapshot_before.json --comparison-report TO_DO_LIST/logs/PRD-047.42-APPLY-19/user_prompt_equivalence.md`
- `python -m pytest bot_psychologist/tests/contract/test_prd_047_42_apply_6_call_llm_boundary_mapping.py bot_psychologist/tests/contract/test_prd_047_42_apply_7_call_llm_slice1.py bot_psychologist/tests/contract/test_prd_047_42_apply_9_call_llm_slice2.py bot_psychologist/tests/contract/test_prd_047_42_apply_10_call_llm_slice3.py bot_psychologist/tests/contract/test_prd_047_42_apply_11_call_llm_slice4.py bot_psychologist/tests/contract/test_prd_047_42_apply_12_call_llm_slice5.py bot_psychologist/tests/contract/test_prd_047_42_apply_13_call_llm_slice6.py bot_psychologist/tests/contract/test_prd_047_42_apply_14_call_llm_slice7.py bot_psychologist/tests/contract/test_prd_047_42_apply_15_call_llm_slice8.py bot_psychologist/tests/contract/test_prd_047_42_apply_16_call_llm_slice9.py bot_psychologist/tests/contract/test_prd_047_42_apply_17_call_llm_slice10.py bot_psychologist/tests/contract/test_prd_047_42_apply_18_call_llm_slice11.py bot_psychologist/tests/contract/test_prd_047_42_apply_19_call_llm_slice12.py -q`
- `pytest tests/ -k writer -q`

## Checks
- `time.perf_counter()` is used through the module import, not a direct function import.
- `estimate_cost` is called exactly once, by keyword arguments `tokens_prompt=` and `tokens_completion=`.
- `last_debug_patch` contains exactly `["model", "api_mode", "temperature", "max_tokens", "timeout", "llm_response", "tokens_prompt", "tokens_completion", "tokens_total", "estimated_cost_usd", "duration_ms", "error", "fallback_used"]` in that order.
- `llm_response` is the original `result.text` object reference, not `str(result.text)`.
- No changes to `writer_agent_fallback_state_mixin.py`, prior slice modules, prompts, contract files, admin modules, or deferred `_enforce_*` surfaces.
- `no_mutation_proof.md` must show `0 changed paths` across the protected files and explicitly include `writer_agent_fallback_state_mixin.py`.
