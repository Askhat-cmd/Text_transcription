# PRD-047.42-APPLY-20 TASK LIST

## Scope

- PRD: `PRD-047.42-APPLY-20`
- Objective: read-only boundary mapping for `WriterAgent._enforce_answer_compliance(...)`.
- Hard rule: `0` production-code changes in `bot_psychologist/**` runtime files.
- Allowed surfaces: `TO_DO_LIST/tools`, `TO_DO_LIST/logs`, `bot_psychologist/tests/contract`, docs sync, PRD/task metadata.

## Implementation Stages

### Stage 1. Context + safety rails

- Read the full PRD and current `_enforce_answer_compliance` source window.
- Reuse prior mapping/snapshot runner patterns from `APPLY-6`.
- Record protected-file set for `no_mutation_proof`, including:
  - canonical `17` protected files from the prior accepted chain,
  - `writer_agent_call_llm_slice12.py`,
  - whole `writer_agent.py`.

### Stage 2. Task artifacts

- Create `TO_DO_LIST/tools/run_prd_047_42_apply_20_enforce_compliance_mapping.py`.
- Create `bot_psychologist/tests/contract/test_prd_047_42_apply_20_enforce_compliance_mapping.py`.
- Create log directory `TO_DO_LIST/logs/PRD-047.42-APPLY-20/`.

### Stage 3. Boundary map

- Build `enforce_compliance_boundary_map.md` with:
  - Section A: prelude/local-variable table,
  - Section B: numbered rule cascade in execution order,
  - Section C: proposed continuous cluster families,
  - Section D: external dependency inventory.
- Ensure every numbered rule has:
  - line span,
  - condition summary,
  - text action summary,
  - early-return flag,
  - locals read.

### Stage 4. Deterministic snapshot harness

- Build `build_normalized_snapshot()` as importable API.
- Add `NORMALIZED_TIMESTAMP` and generate files only after final timestamp is fixed.
- Create `12-20` named cases `(response_text, contract) -> output`.
- Store `enforce_compliance_snapshot.json` with inputs, outputs, and metadata.

### Stage 5. Coverage journal

- Instrument rule coverage in-memory only; do not edit `writer_agent.py`.
- Produce `rule_coverage_log.md` mapping `rule -> cases`.
- Include explicit `НЕПОКРЫТЫЕ ПРАВИЛА` section with honest reasons.

### Stage 6. Verification + reports

- Produce `no_mutation_proof.md`.
- Produce `implementation_report.md`.
- Produce `next_recommendation.md`.
- Run clean-tree historical contract rerun for `APPLY-6..19`.
- Run canonical isolated `pytest tests/ -k writer -q` and record the exact known-failure background.

### Stage 7. Docs + delivery

- Update `docs/PROJECT_STATE.md`.
- Update `docs/ROADMAP.md`.
- Update `docs/PRD_INDEX.md`.
- Update `docs/DECISIONS.md`.
- Update PRD completion metadata in `PRD-047.42-APPLY-20_Enforce_Compliance_Boundary_Mapping_v1_RU.md`.
- Commit main delivery.
- Push `main`.
- Mark completion as requested in PRD and do metadata micro-push.
- Final check: `HEAD == origin/main`, working tree clean.

## Tests

- `pytest bot_psychologist/tests/contract/test_prd_047_42_apply_20_enforce_compliance_mapping.py -q`
- Historical rerun: all contract tests `APPLY-6..19` plus new `APPLY-20`
- Canonical isolated writer subset: `pytest tests/ -k writer -q`

## Checks

- `git diff --name-only -- <protected files>` must be empty.
- Snapshot must be deterministic on double build.
- Snapshot `generated_at_utc` must equal `NORMALIZED_TIMESTAMP`.
- Rule count in map must equal rule count in snapshot metadata.
- No production diff in `bot_psychologist/bot_agent/**`.

## Reports

- `TO_DO_LIST/logs/PRD-047.42-APPLY-20/enforce_compliance_boundary_map.md`
- `TO_DO_LIST/logs/PRD-047.42-APPLY-20/enforce_compliance_snapshot.json`
- `TO_DO_LIST/logs/PRD-047.42-APPLY-20/rule_coverage_log.md`
- `TO_DO_LIST/logs/PRD-047.42-APPLY-20/no_mutation_proof.md`
- `TO_DO_LIST/logs/PRD-047.42-APPLY-20/implementation_report.md`
- `TO_DO_LIST/logs/PRD-047.42-APPLY-20/next_recommendation.md`

## Cleanup

- No leftover temp instrumentation.
- No stray before/after artifacts outside PRD log folder.
- No unstaged/untracked runtime changes at delivery.
