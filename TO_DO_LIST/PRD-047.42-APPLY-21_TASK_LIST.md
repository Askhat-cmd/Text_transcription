# PRD-047.42-APPLY-21 TASK LIST

## Scope

- PRD: `PRD-047.42-APPLY-21`
- Objective: extract `_enforce_answer_compliance` prelude lines `587-706` into a frozen dataclass helper with a single `last_debug_patch`.
- Hard boundaries:
  - keep line `583` and rule `R01` (`584-585`) in `writer_agent.py`,
  - keep `R02` start at `707` and everything below untouched,
  - do not move `legacy_constraints_suppressed` at `712+`.
- Allowed surfaces: `writer_agent.py`, new helper module, new runner, tests, PRD logs/reports, docs sync, PRD/task metadata.

## Implementation Stages

### Stage 1. Boundary confirmation

- Reconfirm fresh HEAD line window `583-712`.
- Prove that `587-706` is exactly `ctx = contract.to_prompt_context()` through `gate_failed_checks`.
- Grep-confirm: no `self.last_debug` reads inside `587-706`, writes only.
- Record exact `44` locals from APPLY-20 Section A in assignment order.

### Stage 2. Task artifacts

- Create `bot_psychologist/bot_agent/multiagent/agents/writer_agent_enforce_slice1.py`.
- Create `TO_DO_LIST/tools/run_prd_047_42_apply_21_enforce_slice1.py`.
- Create `bot_psychologist/tests/contract/test_prd_047_42_apply_21_enforce_slice1.py`.
- Create `bot_psychologist/tests/multiagent/test_writer_agent_enforce_slice1.py`.
- Create log directory `TO_DO_LIST/logs/PRD-047.42-APPLY-21/`.

### Stage 3. Prelude helper extraction

- Add frozen dataclass `EnforceSlice1PreludeResult` with exactly `44` fields plus `last_debug_patch` last.
- Move code from `587-706` literally into `_extract_enforce_slice1_prelude(...)`.
- Replace each `self.last_debug[...] = ...` in-range with ordered `last_debug_patch[...] = ...`.
- Keep detector imports sourced from the same modules as `writer_agent.py`.

### Stage 4. WriterAgent integration

- Import helper into `writer_agent.py`.
- Replace only lines `587-706` with:
  - helper call,
  - explicit unpack of all `44` locals in field order,
  - one `self.last_debug.update(slice1_prelude.last_debug_patch)`.
- Do not use `locals()` or dynamic unpacking.
- Clean imports only after per-symbol zero-match grep.

### Stage 5. Runner + snapshots

- Reuse APPLY-20 harness by import, not copy-paste.
- Build before/after normalized snapshots for the same `17` cases.
- Set `NORMALIZED_TIMESTAMP` before writing artifacts.
- Persist deterministic comparison reports and `user_prompt`/`last_debug` parity evidence where applicable.

### Stage 6. Verification + reports

- Produce `no_mutation_proof.md`.
- Produce `implementation_report.md`.
- Produce `next_recommendation.md`.
- Record grep proofs:
  - no `self.last_debug` reads in-range,
  - zero-match import cleanup checks.
- Keep APPLY-20 logs untouched and mention this explicitly.

### Stage 7. Docs + delivery

- Update `docs/PROJECT_STATE.md`.
- Update `docs/ROADMAP.md`.
- Update `docs/PRD_INDEX.md`.
- Update `docs/DECISIONS.md`.
- Update PRD completion metadata in `PRD-047.42-APPLY-21_Enforce_Slice1_Prelude_v1_RU.md`.
- Commit main delivery.
- Push `main`.
- Mark completion as requested in PRD and do metadata micro-push.
- Final check: `HEAD == origin/main`, working tree clean.

## Tests

- `pytest bot_psychologist/tests/multiagent/test_writer_agent_enforce_slice1.py -q`
- `pytest bot_psychologist/tests/contract/test_prd_047_42_apply_21_enforce_slice1.py -q`
- Historical rerun: contract tests `APPLY-6..20` plus new `APPLY-21`
- Canonical isolated writer subset: `pytest tests/ -k writer -q`

## Checks

- Snapshot before/after must be byte-identical across all `17` cases.
- Dataclass field count must be `45` total members: `44` locals + `last_debug_patch`.
- Local unpack list in `writer_agent.py` must match dataclass order exactly.
- `git diff --name-only -- <protected files>` must exclude all protected paths except intended `writer_agent.py`.
- `generated_at_utc` must equal runner `NORMALIZED_TIMESTAMP`.
- `APPLY-20` map and snapshot files must remain unchanged.

## Reports

- `TO_DO_LIST/logs/PRD-047.42-APPLY-21/enforce_slice1_snapshot_before.json`
- `TO_DO_LIST/logs/PRD-047.42-APPLY-21/enforce_slice1_snapshot_after.json`
- `TO_DO_LIST/logs/PRD-047.42-APPLY-21/no_mutation_proof.md`
- `TO_DO_LIST/logs/PRD-047.42-APPLY-21/implementation_report.md`
- `TO_DO_LIST/logs/PRD-047.42-APPLY-21/next_recommendation.md`
- `TO_DO_LIST/logs/PRD-047.42-APPLY-21/grep_proof.md`
- `TO_DO_LIST/logs/PRD-047.42-APPLY-21/test_output.txt`

## Cleanup

- No stray temp files outside `TO_DO_LIST/logs/PRD-047.42-APPLY-21/`.
- No untracked runtime artifacts at delivery.
- No accidental edits below `R02` or in prior APPLY log folders.
