# PRD-047.42-APPLY-22 — Execution tasks / tests / checks (исполнитель: agent)

Источник: `TO_DO_LIST/PRD-047.42-APPLY-22_Enforce_Slice2_SecondPrelude_CloseGently_v1_RU.md`.
Рабочий файл, ведётся во время реализации, не часть мастер-плана.

## Задачи

- [x] Шаг 1 — пересчитать границы по HEAD f730754e: подтвердить 628-700 = от
  `self.last_debug["legacy_constraints_suppressed"]` до
  `self.last_debug["answer_fit_evaluator"]`; R02 (623-627) над срезом,
  R04 (702) под срезом без изменений. **Подтверждено чтением файла — совпадает.**
- [ ] Шаг 2 — создать `writer_agent_enforce_slice2.py` (mechanic d,
  `EnforceSlice2SecondPreludeResult`, точный код из тела ПРД §2).
- [ ] Шаг 3 — заменить строки 628-700 в `writer_agent.py` на вызов helper'а +
  `self.last_debug.update(...)` + условный `if close_gently_triggered` блок +
  распаковку локальных, порядок строго по ПРД.
- [ ] Шаг 4 — импорт helper'а в `writer_agent.py` (после импорта slice1).
- [ ] Шаг 5 — runner `TO_DO_LIST/tools/run_prd_047_42_apply_22_enforce_slice2.py`
  по образцу APPLY-21 runner'а: historical HEAD = f730754e, 17 cases,
  build_before_snapshot()/build_after_snapshot(), NORMALIZED_TIMESTAMP.
- [ ] Шаг 6a — `bot_psychologist/tests/contract/test_prd_047_42_apply_22_enforce_slice2.py`.
- [ ] Шаг 6b — `bot_psychologist/tests/multiagent/test_writer_agent_enforce_slice2.py`:
  - close_gently_triggered=True: патч РОВНО 5 ключей в порядке, без
    final_answer_shape/answer_fit_evaluator, user_mechanism_request/answer_fit=None;
  - close_gently_triggered=False: патч РОВНО 6 ключей, answer_fit_evaluator последний,
    user_mechanism_request/answer_fit заполнены;
  - интеграционный тест: `list(self.last_debug.keys())` заканчивается на
    `final_answer_shape` ШЕСТЫМ в true-ветке (защита от ловушки Особенности 1);
  - маркеры-параметры: пустой tuple practice_markers -> has_unsolicited_practice=False.
- [ ] Шаг 7 — before/after снапшоты по 17 cases, побайтовая идентичность,
  включая порядок ключей self.last_debug.
- [ ] Шаг 8 — все контрактные тесты APPLY-6..22 зелёные (чистое дерево / реран).
- [ ] Шаг 9 — `no_mutation_proof.md`: 18 защищённых + slice1
  (hash из HEAD f730754e) + логи APPLY-20/21 неприкосновенны.
- [ ] Обновить docs (DECISIONS.md, PRD_INDEX.md, PROJECT_STATE.md, ROADMAP.md)
  по образцу предыдущих APPLY-N записей.
- [ ] Канонический изолированный прогон `pytest tests/ -k writer -q` — ровно 19
  известных падений (или зафиксировать честное расхождение, как в APPLY-21).
- [ ] Заполнить completion metadata в PRD-файле (§7).
- [ ] Основной коммит + push в main.
- [ ] Микро-коммит с отметкой чек-листа/completion metadata + push.

## Красные линии (из ПРД §3)

- НЕ передавать `self._set_final_answer_shape_debug` / `self._build_gentle_close_reply`
  в helper.
- НЕ объединять true/false патчи в один универсальный список из 6 ключей.
- НЕ переносить маркерные tuple'ы в отдельный файл констант — только параметрами.
- НЕ вычислять `user_mechanism_request`/`answer_fit` заранее в true-ветке.
- НЕ трогать R02 (623-627) и R04 (702+).
- НЕ запускать run_prd-скрипты прошлых PRD как скрипты.

## Acceptance (из ПРД §4)

1. before/after снапшоты по 17 cases побайтово идентичны, включая порядок
   ключей self.last_debug для обеих веток.
2. Прямые тесты покрывают обе ветки патча + порядок final_answer_shape (6-м) +
   маркеры-параметры.
3. Контрактные тесты APPLY-6..21 зелёные без изменений.
4. no_mutation_proof: 0 изменений по 18 защищённым + slice1 + логам APPLY-20/21.
5. Канонический изолированный прогон — ровно 19 известных падений, те же тесты.
