# PRD-047.42-APPLY-23 — Execution tasks / tests / checks (исполнитель: agent)

Источник: `TO_DO_LIST/PRD-047.42-APPLY-23_Enforce_Slice3_BoundedPractice_R04_v1_RU.md`.
Рабочий файл, ведётся во время реализации, не часть мастер-плана.

## Пересчёт границ (Шаг 1) — ВАЖНОЕ РАСХОЖДЕНИЕ, НЕ БЛОКЕР

ПРД заявляет строки "666-692" (27 строк) с концом на
`return self._strip_optional_followup_invitation(text) or text` и R07 на 694.
Фактическая проверка по живому HEAD (ff9489c2/ad7875d, writer_agent.py не
менялся между ними):
- R04 реально занимает строки **666-696** (31 строка): текст условий,
  self-вызовов и литерального возврата совпадает С ПРД Step 2/3 ПОСИМВОЛЬНО;
- разница в 4 строки — потому что литеральный ответ "Одна короткая практика..."
  в текущем форматировании разбит на 3 строковых литерала (686-688), а не
  занимает одну строку, как явно предполагал подсчёт в ПРД;
- R07 (`if literal_markdown_echo:`) реально на строке **698** (не 694), сразу
  после одной пустой строки — без изменений, как и требует ПРД;
- grep подтвердил: `practice_anchor_present`/`practice_step_present`/
  `practice_multistep` нигде не читаются после строки 681 (все использования
  — внутри самого среза).

Вывод: это НЕ структурное расхождение (расширять срез не нужно) — текстовые
маркеры границ ("if answer_obligation == ..." ... "return self._strip_optional_
followup_invitation(text) or text" ... затем "if literal_markdown_echo:")
совпадают с ПРД однозначно. Реализация ведётся по фактическим строкам 666-696,
что зафиксировано здесь как честная переоценка границ (в духе находок
APPLY-21/APPLY-22), а не как STOP.

## Задачи

- [x] Шаг 1 — пересчитать границы по живому HEAD (см. выше, расхождение
  задокументировано, не блокер).
- [x] Шаг 2 — создать `writer_agent_enforce_slice3.py` (чистый классификатор,
  `EnforceSlice3BoundedPracticeResult`, 4 исхода, БЕЗ self-вызовов и БЕЗ
  записи last_debug).
- [x] Шаг 3 — заменить строки 666-696 в `writer_agent.py` на вызов
  классификатора + условный блок по `slice3_result.outcome` (3 self/return
  ветки + not_matched = ничего не делать, поток продолжается к R07),
  литеральный текст ответа копируется дословно.
- [x] Шаг 4 — импорт helper'а в `writer_agent.py` (после импорта slice2).
- [x] Шаг 5 — runner `TO_DO_LIST/tools/run_prd_047_42_apply_23_enforce_slice3.py`
  по образцу APPLY-22: HISTORICAL_HEAD = ff9489c2, 17 cases,
  build_before_snapshot()/build_after_snapshot(), NORMALIZED_TIMESTAMP.
- [x] Шаг 6a — `bot_psychologist/tests/contract/test_prd_047_42_apply_23_enforce_slice3.py`.
- [x] Шаг 6b — `bot_psychologist/tests/multiagent/test_writer_agent_enforce_slice3.py`:
  - все 4 исхода классификатора отдельными case'ами
    (not_matched, be_strong, defer_repair, strip_followup);
  - подтверждение чистоты (нет self, нет last_debug, идемпотентность —
    двойной вызов с одинаковыми аргументами даёт одинаковый результат).
- [x] Шаг 7 — before/after снапшоты по 17 cases, побайтовая идентичность
  (включая case `bounded_practice_be_strong`).
- [x] Шаг 8 — все контрактные тесты APPLY-6..22 зелёные (чистое дерево / реран).
- [x] Шаг 9 — `no_mutation_proof.md`: 20 защищённых (19 из v4.26 +
  writer_agent_enforce_slice2.py, hash из HEAD ff9489c2) + логи
  APPLY-20/21/22 неприкосновенны.
- [x] grep-подтверждение в отчёте: три внутренние переменные helper'а нигде
  не читаются после исходных строк R04.
- [x] Обновить docs (DECISIONS.md, PRD_INDEX.md, PROJECT_STATE.md, ROADMAP.md).
- [x] Канонический изолированный прогон `pytest tests/ -k writer -q` — ровно 19
  известных падений.
- [x] Заполнить completion metadata в PRD-файле (§7).
- [x] Основной коммит + push в main.
- [x] Микро-коммит с отметкой чек-листа/completion metadata + push.

## Красные линии (из ПРД §3)

- НЕ переносить `self._set_final_answer_shape_debug`,
  `self._strip_optional_followup_invitation` или литеральный текст ответа
  в helper.
- НЕ добавлять self-параметры в helper — чистая функция.
- НЕ трогать R07 (698+) и далее.
- НЕ выносить `practice_anchor_present`/`practice_step_present`/
  `practice_multistep` наружу, если grep подтвердит непубличность.
- НЕ менять литеральный текст ответа ни на символ.
- НЕ запускать run_prd-скрипты прошлых PRD как скрипты.

## Acceptance (из ПРД §4)

1. before/after снапшоты по 17 cases побайтово идентичны.
2. Прямые тесты покрывают все 4 исхода классификатора + отсутствие побочных
   эффектов.
3. Контрактные тесты APPLY-6..22 зелёные без изменений.
4. no_mutation_proof: 0 изменений по 20 защищённым + логам APPLY-20/21/22.
5. grep-подтверждение непубличности трёх внутренних переменных — в отчёте.
6. Канонический изолированный прогон — ровно 19 известных падений.

## Итог (после реализации)

- Основной implementation commit: `d7ef669`, запушен в `origin/main`.
- Прямые тесты (8) и контрактные тесты (3) — зелёные.
- Чистое дерево, изолированный worktree от `d7ef669`: APPLY-6..23 контрактный
  реран — 128/128 (было 125/125 до APPLY-23, +3 новых контрактных теста).
- Канонический изолированный прогон `pytest tests/ -k writer -q` (worktree):
  `19 failed, 228 passed, 2021 deselected, 190 warnings` — ровно 19, тот же
  набор, что в APPLY-20/21/22 (рост passed/deselected объясняется только
  новыми APPLY-23 тестами).
- Рабочее пространство владельца: `14 failed, 233 passed, 2021 deselected,
  346 warnings` — тот же известный набор из 14, что и в APPLY-21/22;
  зафиксировано как честное предупреждение, не регрессия.
- docs (DECISIONS.md ADR-123, PRD_INDEX.md, PROJECT_STATE.md, ROADMAP.md)
  обновлены по образцу APPLY-22.
- Completion metadata в PRD-файле (§7) заполнена.
