# PRD-047.42-APPLY-24 — _enforce_answer_compliance slice 4: R07-R16 (пять obligation-repair правил) — ПЕРВЫЙ УКРУПНЁННЫЙ СРЕЗ v1 RU

**Проект:** Bot Psychologist / Neo MindBot
**Репозиторий:** `Askhat-cmd/Text_transcription`
**Локальный путь владельца:** `C:\My_practice\Text_transcription`
**Дата:** 2026-07-16
**Статус:** draft / ready for IDE-agent implementation
**Основание:** разведка подтвердила чистоту семьи (см. v4.27) — по
решению владельца о темпе, семья режется одним PRD. Границы по
HEAD = b39ed432 (writer_agent.py = 1323 строки).

---

## 0. Короткое решение

```text
Выносим ВСЮ семью: строки 690-745 (56 строк) — от
"if literal_markdown_echo:" до последнего "return self._defer_no_
stub_repair(...)" правила R15/R16 включительно. Строка 746
(if dialogue_profile == DIALOGUE_PROFILE_MVP_FREE) остаётся СРАЗУ под
срезом без изменений.

Почему одним куском (закон З-4 не требует дробления здесь): все 5
правил читают уже существующие локальные, НЕ создают новых
общих зависимостей друг для друга, НЕ конкурируют за last_debug
(единственная запись — в literal_markdown_echo, полностью
самодостаточна: пишет 2 ключа и сразу возвращает). Единственный
self-вызов (self._defer_no_stub_repair) используется 4 раза с разными
signal/must_answer — безопасно для одного классификатора с 6 исходами.

Механика — та же, что в APPLY-23: чистый классификатор без self и без
прямой записи last_debug для repair-правил; для literal_markdown_echo
классификатор возвращает патч (2 ключа) + готовый текст возврата —
это тоже безопасно, так как это ЕДИНСТВЕННАЯ last_debug-работа во
всей семье, ничего до/после неё не конкурирует.

Новый модуль: writer_agent_enforce_slice4.py.
Новый PRD-инструмент: run_prd_047_42_apply_24_enforce_slice4.py.
```

---

## 1. Особенность — прочитать перед реализацией

```text
6 исходов классификатора, ровно в порядке правил оригинала:
  "not_matched" — ни одно правило не сработало, поток идёт дальше к
    строке 746 (mvp_free handoff), НИЧЕГО не пишется в last_debug;
  "literal_markdown_echo_mismatch" — R07/R08: normalized_response !=
    normalized_requested. Несёт last_debug_patch = ровно
    {"format_request_repair_applied": True,
     "final_answer_shape": "literal_markdown_echo"} (в этом порядке —
    это порядок оригинала) и return_text = normalized_requested;
  "acknowledge_style_preference_repair" — R09/R10: signal=
    "style_preference_direct_answer_repair", must_answer=
    "known_concept_question";
  "repair_and_answer_last_question_repair" — R11/R12: signal=
    "repair_answer_last_question_repair", must_answer=target (target =
    last_direct_question or user_message — вычисляется ВНУТРИ
    классификатора, как в оригинале);
  "answer_last_offer_repair" — R13/R14: signal="answer_last_offer_
    repair", must_answer=last_offer_summary or last_direct_question or
    "last_assistant_offer";
  "answer_knowledge_or_direct_repair" — R15/R16: signal="knowledge_
    direct_answer_repair", must_answer="known_concept_question".

Порядок проверки условий внутри классификатора ОБЯЗАН точно повторять
порядок правил в оригинале (R07 первым, R15/R16 последним) — это
каскад, и первое сработавшее условие останавливает проверку
остальных, как это делают return'ы в оригинале.
```

---

## 2. Технический план для Исполнителя

```text
Шаг 1. Пересчитать границы по свежему HEAD: подтвердить 690-745 —
  ровно от "if literal_markdown_echo:" до конца последнего правила
  R15/R16, строка 746 (mvp_free handoff) сразу под срезом без
  изменений. Расхождение — STOP, в отчёт (по образцу честной
  переоценки в APPLY-23 — расхождение в пределах форматирования не
  blocker, структурное расхождение — blocker).

Шаг 2. Создать writer_agent_enforce_slice4.py:

from __future__ import annotations

from dataclasses import dataclass
from typing import Any, Literal, Optional


Slice4Outcome = Literal[
    "not_matched",
    "literal_markdown_echo_mismatch",
    "acknowledge_style_preference_repair",
    "repair_and_answer_last_question_repair",
    "answer_last_offer_repair",
    "answer_knowledge_or_direct_repair",
]


@dataclass(frozen=True)
class EnforceSlice4Result:
    outcome: Slice4Outcome
    last_debug_patch: Optional[dict[str, Any]] = None
    return_text: Optional[str] = None
    defer_signal: Optional[str] = None
    defer_must_answer: Optional[str] = None


def _classify_enforce_slice4_obligation_repairs_and_echo(
    *,
    text: str,
    user_message: str,
    lowered_text: str,
    answer_obligation: str,
    literal_markdown_echo: str,
    concept_question: bool,
    last_direct_question: str,
    last_offer_summary: str,
    offer_repair_context: str,
) -> EnforceSlice4Result:
    if literal_markdown_echo:
        normalized_requested = literal_markdown_echo.strip()
        normalized_response = text.strip()
        if normalized_response != normalized_requested:
            return EnforceSlice4Result(
                outcome="literal_markdown_echo_mismatch",
                last_debug_patch={
                    "format_request_repair_applied": True,
                    "final_answer_shape": "literal_markdown_echo",
                },
                return_text=normalized_requested,
            )

    if answer_obligation == "acknowledge_style_preference_then_answer" and (
        "расскажи больше" in lowered_text or len(text) < 140
    ):
        if concept_question:
            return EnforceSlice4Result(
                outcome="acknowledge_style_preference_repair",
                defer_signal="style_preference_direct_answer_repair",
                defer_must_answer="known_concept_question",
            )

    if answer_obligation == "repair_and_answer_last_question" and (
        "сейчас полезнее прямое объяснение механизма" in lowered_text or len(text) < 180
    ):
        target = last_direct_question or user_message
        if "нейросталкинг" in target.lower():
            return EnforceSlice4Result(
                outcome="repair_and_answer_last_question_repair",
                defer_signal="repair_answer_last_question_repair",
                defer_must_answer=target,
            )

    if answer_obligation == "answer_last_offer" and (
        any(marker in lowered_text for marker in ("подтверди", "если хочешь", "могу так сделать"))
        or any(marker in lowered_text for marker in ("предлагаю такой план", "хотите, чтобы", "сначала"))
        or "после подтверждения" in lowered_text
        or ("могу так сделать" in last_offer_summary.lower() and len(text) < 500)
        or (
            any(color in offer_repair_context for color in ("красн", "оранж", "зелен"))
            and not all(color in lowered_text for color in ("красн", "оранж", "зелен"))
        )
    ):
        if any(color in offer_repair_context for color in ("красн", "оранж", "зелен")):
            return EnforceSlice4Result(
                outcome="answer_last_offer_repair",
                defer_signal="answer_last_offer_repair",
                defer_must_answer=last_offer_summary or last_direct_question or "last_assistant_offer",
            )

    if answer_obligation in {"answer_knowledge_question", "answer_direct_question"} and (
        "сейчас полезнее прямое объяснение механизма" in lowered_text or len(text) < 140
    ):
        if concept_question:
            return EnforceSlice4Result(
                outcome="answer_knowledge_or_direct_repair",
                defer_signal="knowledge_direct_answer_repair",
                defer_must_answer="known_concept_question",
            )

    return EnforceSlice4Result(outcome="not_matched")

  Каждое условие — точная копия соответствующего правила оригинала
  (строки 690-745). Порядок проверок не менять.

Шаг 3. В writer_agent.py заменить строки 690-745 на:

        slice4_result = _classify_enforce_slice4_obligation_repairs_and_echo(
            text=text,
            user_message=user_message,
            lowered_text=lowered_text,
            answer_obligation=answer_obligation,
            literal_markdown_echo=literal_markdown_echo,
            concept_question=concept_question,
            last_direct_question=last_direct_question,
            last_offer_summary=last_offer_summary,
            offer_repair_context=offer_repair_context,
        )
        if slice4_result.outcome == "literal_markdown_echo_mismatch":
            self.last_debug.update(slice4_result.last_debug_patch)
            return slice4_result.return_text
        if slice4_result.outcome in {
            "acknowledge_style_preference_repair",
            "repair_and_answer_last_question_repair",
            "answer_last_offer_repair",
            "answer_knowledge_or_direct_repair",
        }:
            return self._defer_no_stub_repair(
                signal=slice4_result.defer_signal,
                text=text,
                must_answer=slice4_result.defer_must_answer,
            )

  Строка 746 (if dialogue_profile == DIALOGUE_PROFILE_MVP_FREE)
  остаётся сразу под этим блоком, без изменений.

Шаг 4. Импорт helper'а в writer_agent.py (после импорта slice3).

Шаг 5. Runner run_prd_047_42_apply_24_enforce_slice4.py по образцу
  APPLY-23 (реконструкция historical-класса из git show HISTORICAL_
  HEAD = b39ed432, build_before_snapshot/build_after_snapshot
  импортируемые, NORMALIZED_TIMESTAMP до генерации файлов).

Шаг 6. Тесты:
  a) tests/contract/test_prd_047_42_apply_24_enforce_slice4.py;
  b) tests/multiagent/test_writer_agent_enforce_slice4.py — прямые,
     ОБЯЗАТЕЛЬНО покрывают все 6 исходов отдельными case'ами
     (not_matched; literal_markdown_echo_mismatch с точной проверкой
     последовательности 2 ключей патча; все 4 *_repair-исхода с
     проверкой правильных signal/must_answer, включая вычисление
     target для repair_and_answer_last_question), плюс подтверждение
     отсутствия побочных эффектов (чистая функция).

Шаг 7. before/after снапшоты — побайтовая идентичность по всем 17
  cases.

Шаг 8. Все контрактные тесты APPLY-6..23 (чистое дерево).

Шаг 9. no_mutation_proof.md: 21 защищённых (20 из v4.27 + enforce_
  slice3 hash зафиксировать из HEAD b39ed432) + логи APPLY-20..23
  неприкосновенны.
```

---

## 3. Не делаем

```text
- НЕ переносим self._defer_no_stub_repair в helper — остаётся в
  writer_agent.py, вызывается ОДИН раз с параметрами из результата.
- НЕ меняем порядок проверки правил и порядок ключей патча
  literal_markdown_echo.
- НЕ трогаем строку 746 (mvp_free handoff) и далее.
- НЕ добавляем self-параметры в helper.
- НЕ запускаем run_prd-скрипты прошлых PRD как скрипты (красная линия).
```

---

## 4. Acceptance criteria

```text
ACCEPTED, если:
1. before/after снапшоты по 17 cases побайтово идентичны.
2. Прямые тесты покрывают все 6 исходов, включая точный порядок 2
   ключей патча literal_markdown_echo и корректность target-вычисления.
3. Все контрактные тесты APPLY-6..23 зелёные без изменений.
4. no_mutation_proof: 0 изменений по 21 защищённым + логам APPLY-20..23.
5. Канонический изолированный прогон pytest tests/ -k writer -q:
   ровно 19 известных падений, те же тесты.

BLOCKED, если: снапшот разошёлся хоть в байте; порядок проверки правил
или ключей патча изменился; любой protected file или лог прошлого
PRD изменился.
```

---

## 5. Задел на следующий PRD

```text
После R07-R16 — граница mvp_free_branch_handoff (строка 746,
единственный делегирующий self-вызов). Архитектор решит на месте:
вероятно "остаётся на месте" (по аналогии с owner-решением #3 про
provider_dispatch), поскольку это одна строка без внутренней логики.
Затем семьи 4-6 карты APPLY-20 — каждая получает отдельную разведку
перед PRD; размер шага решается по её результату (одним PRD, если
чисто, дробится — если найдена скрытая сложность).
```

---

## 6. Simple explanation (для Асхата)

```text
Что делаем: пять правил редактора ответов одним куском — как ты и
просил, не мельчим, раз разведка показала, что здесь нет скрытых
ловушек. Все пять — независимые проверки "если бот должен был сделать
X, а обстоятельства изменились — переделать ответ" (например: если
пользователь просил дословный формат, а бот его не выдержал — вернуть
точный текст; если пользователь просит объяснение механизма вместо
общих слов — переделать ответ).

Что делать прямо сейчас:
1. Передать Исполнителю файл обновления Части 4 (v4.27) — там же
   зафиксировано твоё решение про темп работы.
2. Затем передать этот файл PRD-047.42-APPLY-24.
3. Когда будет "готово" — прислать оба хэша, проверю независимо.
```

---

## 7. Completion metadata (заполняется после приёмки)

```text
Статус исполнения: NOT_STARTED.
Основной implementation commit: —
Main push status: —
```
