# PRD-047.42-APPLY-25 — Execution tasks / tests / checks (исполнитель: agent)

Источник: `TO_DO_LIST/PRD-047.42-APPLY-25_Retire_RuleCount_Invariant_MicroPRD_v1_RU.md`.
Гигиенический микро-PRD. Рабочий файл, не часть мастер-плана.

## Задачи

- [ ] Шаг 1 — открыть `bot_psychologist/tests/contract/test_prd_047_42_apply_20_
  enforce_compliance_mapping.py`, найти `test_rule_count_matches_boundary_
  map_inventory`.
- [ ] Шаг 2 — убрать РОВНО одну ассерцию `assert len(rules) == 75`, добавить
  docstring с объяснением дрейфа (75->67 в APPLY-24, классификаторная
  механика закономерно схлопывает if-узлы). Две другие ассерции
  (`payload["metadata"]["rule_count"] == len(rules)`, `len(rules) >= 40`)
  остаются без изменений. Две другие функции файла НЕ трогать.
- [ ] Шаг 3 — прогнать сам файл: все 3 теста зелёные.
- [ ] Шаг 4 — прогнать все контрактные тесты APPLY-6..24 (чистое дерево) —
  подтвердить отсутствие побочных эффектов, включая то, что теперь ВСЕ
  зелёные (не 130/1, а 131/131).
- [ ] Шаг 5 — канонический `pytest tests/ -k writer -q` — подтвердить ровно
  19 известных падений, тот же список (число не должно измениться —
  этот тест и раньше не входил в `-k writer`).
- [ ] Шаг 6 — `no_mutation_proof.md` для этого PRD: (а) protected-файлы v4.28
  (21) не изменились; (б) `TO_DO_LIST/logs/PRD-047.42-APPLY-20/` не изменился
  (0 diff) — прямое доказательство соблюдения красной линии; (в) весь git
  diff PRD ограничен ОДНИМ файлом теста.
- [ ] Шаг 7 — короткий `implementation_report.md`.
- [ ] Шаг 8 — docs sync (по желанию исполнителя, необязателен для микро-PRD) —
  сделать короткую запись в PROJECT_STATE.md/ROADMAP.md для целостности
  истории.
- [ ] Заполнить completion metadata в PRD-файле (§7).
- [ ] Основной коммит + push в main.
- [ ] Микро-коммит с отметкой чек-листа/completion metadata + push.

## Не делаем (из ПРД §3)

- НЕ трогаем `TO_DO_LIST/logs/PRD-047.42-APPLY-20/` (карта, снапшот,
  coverage-log, отчёты) — красная линия.
- НЕ трогаем две другие функции теста в этом файле.
- НЕ трогаем production-код (`writer_agent*.py`).
- НЕ меняем `run_prd_047_42_apply_20_enforce_compliance_mapping.py`.
- НЕ переименовываем и не удаляем функцию теста целиком.

## Acceptance (из ПРД §4)

1. `git diff` ограничен ровно одним файлом:
   `bot_psychologist/tests/contract/test_prd_047_42_apply_20_enforce_
   compliance_mapping.py`.
2. Все 3 теста файла зелёные.
3. Все контрактные тесты APPLY-6..24 зелёные без изменений (кроме самого
   починенного теста).
4. Канонический `pytest tests/ -k writer -q`: ровно 19, тот же список.
5. `no_mutation_proof` подтверждает: 0 diff по protected-файлам, 0 diff по
   `TO_DO_LIST/logs/PRD-047.42-APPLY-20/`.
