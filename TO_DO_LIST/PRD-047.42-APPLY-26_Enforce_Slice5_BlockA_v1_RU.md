# PRD-047.42-APPLY-26 — _enforce_answer_compliance slice 5: Block A (greeting/support/safety/clarify_one_point/user_repair_signal) v1 RU

**Проект:** Bot Psychologist / Neo MindBot
**Репозиторий:** `Askhat-cmd/Text_transcription`
**Локальный путь владельца:** `C:\My_practice\Text_transcription`
**Дата:** 2026-07-16
**Статус:** draft / ready for IDE-agent implementation
**Основание:** разведка подтвердила чистоту (см. v4.29) — берём Block A
целиком. Границы по HEAD = 787b7f0d (после APPLY-25).

---

## 0. Короткое решение

```text
Выносим Block A: строки 756-821 (66 строк) — от комментария
"# Greeting path..." до конца правила user_repair_signal включительно.
Строка 822 (комментарий "# Known concept answer-first path", начало
Block B) остаётся СРАЗУ под срезом без изменений.

Механика — классификатор (как APPLY-24), 15 исходов. ОСОБЕННОСТЬ
этого среза: несколько условий возвращают ОДИНАКОВЫЙ по тексту
литерал (например, "Я рядом. Сейчас не нужно ничего разбирать...."
встречается у трёх разных условий) — это НЕ дедуплицируется, каждое
условие в writer_agent.py держит свой литерал отдельно, дословно как
в оригинале. Один исход (clarify_one_point с >1 вопросом) несёт
ВЫЧИСЛЕННЫЙ текст, а не константу — классификатор вычисляет его сам
и кладёт в return_text. Единственный self-вызов (user_repair_signal)
остаётся в writer_agent.py, параметры не зависят ни от чего внутри
классификатора кроме уже известных text/user_message.

Новый модуль: writer_agent_enforce_slice5.py.
Новый PRD-инструмент: run_prd_047_42_apply_26_enforce_slice5.py.
```

---

## 1. Технический план для Исполнителя

```text
Шаг 1. Пересчитать границы по свежему HEAD: подтвердить 756-821 —
  ровно от "# Greeting path..." до конца блока user_repair_signal,
  строка 822 (комментарий Block B) сразу под срезом без изменений.
  Расхождение (в пределах форматирования — не blocker, по прецеденту
  APPLY-23; структурное — STOP, в отчёт).

Шаг 2. Создать writer_agent_enforce_slice5.py:

from __future__ import annotations

from dataclasses import dataclass
from typing import Optional, Literal

from .writer_agent_constants import _contains_any


Slice5Outcome = Literal[
    "not_matched",
    "greeting_path",
    "low_resource_no_practice",
    "thanks_close",
    "safety_priority_has_question",
    "give_short_support_primary",
    "give_short_support_len_or_flags",
    "stabilize_safety_len_or_question",
    "stabilize_safety_markers",
    "close_gently",
    "give_short_support_markers",
    "clarify_one_point_zero_questions",
    "clarify_one_point_multi_questions",
    "clarify_one_point_long",
    "user_repair_signal",
]


@dataclass(frozen=True)
class EnforceSlice5Result:
    outcome: Slice5Outcome
    return_text: Optional[str] = None


def _classify_enforce_slice5_block_a(
    *,
    text: str,
    user_message: str,
    lowered_user: str,
    lowered_text: str,
    practice_allowed: bool,
    should_answer_directly: bool,
    is_greeting: bool,
    has_unsolicited_practice: bool,
    has_question: bool,
    active_line_intent: str,
    planner_safety_priority: bool,
    planner_next_move: str,
    planner_answer_shape: str,
    user_repair_signal: bool,
    low_resource_no_practice_markers: tuple[str, ...],
) -> EnforceSlice5Result:
    if not practice_allowed and not should_answer_directly and (is_greeting or has_unsolicited_practice):
        return EnforceSlice5Result(outcome="greeting_path")

    if _contains_any(lowered_user, low_resource_no_practice_markers):
        if has_unsolicited_practice or len(text) > 280 or "?" in text:
            return EnforceSlice5Result(outcome="low_resource_no_practice")

    if active_line_intent == "thanks_close" and (
        has_unsolicited_practice
        or _contains_any(lowered_text, ("шаг", "давай сделаем", "предложу еще"))
    ):
        return EnforceSlice5Result(outcome="thanks_close")

    if planner_safety_priority and has_question:
        return EnforceSlice5Result(outcome="safety_priority_has_question")

    if planner_next_move == "give_short_support" or planner_answer_shape == "short_support":
        return EnforceSlice5Result(outcome="give_short_support_primary")

    if planner_next_move == "give_short_support" and (len(text) > 260 or has_question or has_unsolicited_practice):
        return EnforceSlice5Result(outcome="give_short_support_len_or_flags")

    if planner_next_move == "stabilize_safety" and (len(text) > 320 or has_question):
        return EnforceSlice5Result(outcome="stabilize_safety_len_or_question")

    if planner_next_move == "stabilize_safety" and _contains_any(
        lowered_text,
        ("механизм", "прогнозирован", "контрол", "паттерн", "драйвер", "до начала действия"),
    ):
        return EnforceSlice5Result(outcome="stabilize_safety_markers")

    if planner_next_move == "close_gently" and (
        has_question
        or has_unsolicited_practice
        or _contains_any(lowered_text, ("новый шаг", "давай продолжим", "в следующий раз разберем"))
    ):
        return EnforceSlice5Result(outcome="close_gently")

    if planner_next_move == "give_short_support" and _contains_any(
        lowered_text,
        ("механизм", "теория", "стратегия", "прогнозирован", "контрол", "паттерн"),
    ):
        return EnforceSlice5Result(outcome="give_short_support_markers")

    if planner_next_move == "clarify_one_point":
        question_count = text.count("?")
        if question_count == 0:
            return EnforceSlice5Result(outcome="clarify_one_point_zero_questions")
        if question_count > 1:
            first = text.split("?")[0].strip()
            return EnforceSlice5Result(
                outcome="clarify_one_point_multi_questions",
                return_text=(first + "?").strip(),
            )
        if len(text) > 320:
            return EnforceSlice5Result(outcome="clarify_one_point_long")

    if user_repair_signal:
        return EnforceSlice5Result(outcome="user_repair_signal")

    return EnforceSlice5Result(outcome="not_matched")

  Каждое условие — точная копия строк 756-821. Порядок проверок не
  менять. low_resource_no_practice_markers — параметр (модульная
  константа _LOW_RESOURCE_NO_PRACTICE_MARKERS передаётся вызывающим
  кодом, по прецеденту APPLY-22 — избегаем циклического импорта, если
  константа определена в writer_agent.py; если она уже импортируется
  из writer_agent_constants.py — импортировать helper'ом напрямую
  оттуда и параметр не заводить; проверить перед реализацией и
  выбрать правильный вариант).

Шаг 3. В writer_agent.py заменить строки 756-821 на:

        slice5_result = _classify_enforce_slice5_block_a(
            text=text,
            user_message=user_message,
            lowered_user=lowered_user,
            lowered_text=lowered_text,
            practice_allowed=practice_allowed,
            should_answer_directly=should_answer_directly,
            is_greeting=is_greeting,
            has_unsolicited_practice=has_unsolicited_practice,
            has_question=has_question,
            active_line_intent=active_line_intent,
            planner_safety_priority=planner_safety_priority,
            planner_next_move=planner_next_move,
            planner_answer_shape=planner_answer_shape,
            user_repair_signal=user_repair_signal,
            low_resource_no_practice_markers=_LOW_RESOURCE_NO_PRACTICE_MARKERS,
        )
        if slice5_result.outcome == "greeting_path":
            return (
                "Привет. Рад знакомству. "
                "Можем спокойно начать: принеси любой вопрос или тему, которую хочешь разобрать."
            )
        if slice5_result.outcome in {"low_resource_no_practice", "give_short_support_primary", "give_short_support_markers"}:
            return "Я рядом. Сейчас не нужно ничего разбирать. Можно просто немного снизить внутреннее давление."
        if slice5_result.outcome in {"thanks_close", "close_gently"}:
            return "Пожалуйста. Рад, что стало чуть яснее."
        if slice5_result.outcome == "safety_priority_has_question":
            return "Я рядом. Сейчас важнее чуть стабилизироваться и снизить внутреннюю перегрузку."
        if slice5_result.outcome == "give_short_support_len_or_flags":
            return "Я рядом. Сейчас не нужно всё разбирать. Можно просто немного снизить внутреннее давление."
        if slice5_result.outcome == "stabilize_safety_len_or_question":
            return "Я рядом. Сейчас важнее короткая опора здесь-и-сейчас, без перегруза."
        if slice5_result.outcome == "stabilize_safety_markers":
            return "Я рядом. Сейчас важнее чуть стабилизироваться и снизить внутреннюю перегрузку. Без разбора, только опора здесь-и-сейчас."
        if slice5_result.outcome == "clarify_one_point_zero_questions":
            return "Если выбрать один узел прямо сейчас, что больше всего сжимает тебя в этой ситуации?"
        if slice5_result.outcome == "clarify_one_point_multi_questions":
            return slice5_result.return_text
        if slice5_result.outcome == "clarify_one_point_long":
            return "Похоже, это сильно выматывает. Если взять один конкретный эпизод, где это ощущается острее всего?"
        if slice5_result.outcome == "user_repair_signal":
            return self._defer_no_stub_repair(
                signal="user_repair_signal",
                text=text,
                must_answer=user_message,
            )

  ВАЖНО: сгруппированные if-условия (например, три исхода с одним
  текстом "Я рядом. Сейчас не нужно ничего разбирать...") — это
  осознанное объединение НА УРОВНЕ ВЫВОДА ТЕКСТА, а не подмена логики:
  классификатор всё равно различает их как отдельные исходы (для
  тестируемости и трассировки), просто в writer_agent.py у них
  оказывается общий return. Литеральные тексты копируются ПОСИМВОЛЬНО
  из оригинала — сверить каждую строку с исходником перед коммитом.
  Строка 822 (комментарий Block B) остаётся сразу под этим блоком.

Шаг 4. Импорт helper'а в writer_agent.py (после импорта slice4).

Шаг 5. Runner run_prd_047_42_apply_26_enforce_slice5.py по образцу
  APPLY-24 (реконструкция historical-класса из git show HISTORICAL_
  HEAD = 787b7f0d, build_before_snapshot/build_after_snapshot
  импортируемые, NORMALIZED_TIMESTAMP до генерации файлов).

Шаг 6. Тесты:
  a) tests/contract/test_prd_047_42_apply_26_enforce_slice5.py;
  b) tests/multiagent/test_writer_agent_enforce_slice5.py — прямые,
     ОБЯЗАТЕЛЬНО покрывают все 15 исходов отдельными case'ами,
     включая точное вычисление return_text для
     clarify_one_point_multi_questions (несколько разных text-входов
     с разным количеством "?"), и порядок проверки условий (например,
     case, где ОБА условия give_short_support_primary И give_short_
     support_len_or_flags формально истинны одновременно — обязан
     сработать ПЕРВЫЙ по порядку, primary).

Шаг 7. before/after снапшоты — побайтовая идентичность по всем 17
  cases.

Шаг 8. Все контрактные тесты APPLY-6..25 (чистое дерево).

Шаг 9. no_mutation_proof.md: 22 защищённых (список v4.29; enforce_
  slice4 hash зафиксировать из HEAD 787b7f0d) + логи APPLY-20..25
  неприкосновенны.
```

---

## 2. Не делаем

```text
- НЕ дедуплицируем повторяющиеся текстовые литералы в единую
  константу — переносим ровно как в оригинале, раздельными return'ами
  по группам исходов.
- НЕ меняем порядок проверки условий — порядок определяет, какое
  правило "выигрывает" при формальном совпадении нескольких условий.
- НЕ трогаем self._resolve_one_step_or_no_practice_fallback — он не
  используется в Block A (появляется только в Block B).
- НЕ трогаем строку 822 и далее (Block B).
- НЕ запускаем run_prd-скрипты прошлых PRD как скрипты (красная линия).
```

---

## 3. Acceptance criteria

```text
ACCEPTED, если:
1. before/after снапшоты по 17 cases побайтово идентичны.
2. Прямые тесты покрывают все 15 исходов, включая вычисление
   return_text и порядок разрешения формально-пересекающихся условий.
3. Все контрактные тесты APPLY-6..25 зелёные без изменений.
4. no_mutation_proof: 0 изменений по 22 защищённым + логам APPLY-20..25.
5. Канонический изолированный прогон pytest tests/ -k writer -q:
   ровно 19 известных падений, те же тесты.

BLOCKED, если: снапшот разошёлся хоть в байте; текст любого литерала
отличается хоть на символ от оригинала; порядок проверки условий
изменился; любой protected file или лог прошлого PRD изменился.
```

---

## 4. Задел на следующий PRD

```text
После Block A — разведка Block B (822-1033, 212 строк): "known
concept answer-first path" + question_policy-каскад + практик/active-
line хвост, включая единственную запись last_debug метода (строка
912). Решение о размере шага (одним PRD или дробить) — по результату
разведки, не заранее.
```

---

## 5. Simple explanation (для Асхата)

```text
Что делаем: ещё один большой, но чистый кусок редактора ответов —
разные варианты "короткой поддержки" в зависимости от состояния
разговора (приветствие, усталость пользователя, кризисный режим,
благодарность и прощание, уточняющий вопрос). Одиннадцать разных
условий, некоторые из них ведут к одинаковому тексту ответа — это не
ошибка, так было в оригинале, я это сохранил.

Что делать прямо сейчас:
1. Передать Исполнителю файл обновления Части 4 (v4.29) — там же
   зафиксировано техническое решение "оставить как есть" по одному
   из кусков (без необходимости твоего решения — я имею право
   принимать такие технические решения сам, по прямому прецеденту).
2. Затем передать этот файл PRD-047.42-APPLY-26.
3. Когда будет "готово" — прислать оба хэша, проверю независимо.
```

---

## 6. Completion metadata (заполняется после приёмки)

```text
Статус исполнения: COMPLETED_ACCEPTED.
Основной implementation commit: ad12bf8
Main push status: pushed_to_origin_main
```
