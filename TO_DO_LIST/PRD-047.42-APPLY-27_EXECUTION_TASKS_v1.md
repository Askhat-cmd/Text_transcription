# PRD-047.42-APPLY-27 — Execution tasks / tests / checks (исполнитель: agent)

Источник: `TO_DO_LIST/PRD-047.42-APPLY-27_Enforce_Slice6_BlockB_Part1_v1_RU.md`.
Рабочий файл, ведётся во время реализации, не часть мастер-плана.

## Пересчёт границ (Шаг 1)

Проверено по живому HEAD (2dfc9dfb): строки **804-896** — ровно от
комментария `# Known concept answer-first path: enforce direct internal
meaning framing before generic question-policy rewrites.` до
`return self._strip_optional_followup_invitation(text) or text`
(практик-запрет template_leakage). Строка 897 пустая, строка **898** —
начало group 7 (`if (\n (planner_next_move == "deepen_mechanism"...`),
сразу под срезом без изменений. Полное совпадение с ПРД, без переоценки.
Хэш `writer_agent_enforce_slice5.py` подтверждён:
`ed697cdec60ef2cfa8282cd646fa812a387937ca` — совпадает с ПРД §7.

## Задачи

- [x] Шаг 1 — пересчитать границы по живому HEAD: подтверждено 804-896,
  898 = начало group 7, без изменений; хэш slice5 подтверждён.
- [ ] Шаг 2 — создать `writer_agent_enforce_slice6.py` (классификатор с 19
  тегами исходов, включая `not_matched`, БЕЗ self-вызовов; last_debug_patch
  только для `practice_forbidden_unsolicited_repair`; return_text только
  для `no_question_default_strip`).
- [ ] Шаг 3 — заменить строки 804-896 в `writer_agent.py` на вызов
  классификатора + диспетчеризация по `outcome` (объединение на call site
  для known_concept_prefirst_correlation/no_question_known_concept_
  correlation и neurostalking-пары); `not_matched` — БЕЗ ветки, управление
  проваливается в group 7 (строка 898), без изменений.
- [ ] Шаг 4 — импорт helper'а в `writer_agent.py` (после импорта slice5).
- [ ] Шаг 5 — runner `TO_DO_LIST/tools/run_prd_047_42_apply_27_enforce_slice6.py`
  по образцу APPLY-26: HISTORICAL_HEAD = 2dfc9dfb, 17 cases,
  build_before_snapshot()/build_after_snapshot(), NORMALIZED_TIMESTAMP.
- [ ] Шаг 6a — `bot_psychologist/tests/contract/test_prd_047_42_apply_27_enforce_slice6.py`.
- [ ] Шаг 6b — `bot_psychologist/tests/multiagent/test_writer_agent_enforce_slice6.py`:
  - все 18 значимых исходов отдельными case'ами (включая ОБА пути к
    known_concept_correlation и ОБА пути к known_concept_neurostalking
    как разные теги);
  - `not_matched`: case, где ни одно из 6 условий групп не срабатывает;
  - вычисление `return_text` для `no_question_default_strip` на нескольких
    text с разным числом "?";
  - порядок ключей last_debug для `practice_forbidden_unsolicited_repair`:
    интеграционный тест воспроизводит call site
    (`self.last_debug.update(patch)`, затем `_set_final_answer_shape_debug`)
    и подтверждает `template_leakage_repair_deferred_to_gate` идёт ПЕРЕД
    `final_answer_shape`;
  - минимум один приоритетный case внутри группы 2 (`give_direct_step` И
    `answer_shape=="short_support"` одновременно -> обязан сработать
    `no_question_one_step`, не `no_question_short_support`).
- [ ] Шаг 7 — before/after снапшоты по 17 cases, побайтовая идентичность,
  включая порядок ключей last_debug для case'ов с template_leakage.
- [ ] Шаг 8 — все контрактные тесты APPLY-6..26 зелёные (чистое дерево / реран).
- [ ] Шаг 9 — `no_mutation_proof.md`: 23 защищённых (список v4.30, включая
  writer_agent_enforce_slice5.py = ed697cdec60ef2cfa8282cd646fa812a387937ca)
  + логи APPLY-20..26 неприкосновенны.
- [ ] Обновить docs (DECISIONS.md, PRD_INDEX.md, PROJECT_STATE.md, ROADMAP.md).
- [ ] Канонический изолированный прогон `pytest tests/ -k writer -q` — ровно
  19 известных падений.
- [ ] Проверить: строки 898-1015 (группы 7-12) физически не изменены.
- [ ] Заполнить completion metadata в PRD-файле (§8).
- [ ] Основной коммит + push в main.
- [ ] Микро-коммит с отметкой чек-листа/completion metadata + push.

## Не делаем (из ПРД §3)

- НЕ трогаем группы 7-12 (строки 898-1015) — следующий PRD (APPLY-28).
- НЕ передаём ни один self-метод внутрь helper'а ни в каком виде.
- НЕ пишем self.last_debug внутри helper'а напрямую — только через
  last_debug_patch.
- НЕ объединяем known_concept_prefirst_correlation и no_question_known_
  concept_correlation (и neurostalking-пару) в один тег внутри
  классификатора — только на call site.
- НЕ дедуплицируем текстовые литералы.
- НЕ меняем порядок проверки условий.
- НЕ выносим маркерные tuple групп 3/5 в module-level константы —
  inline-литералы, как в оригинале.
- НЕ запускаем run_prd-скрипты прошлых PRD как скрипты.

## Acceptance (из ПРД §4)

1. before/after снапшоты по 17 cases побайтово идентичны, включая порядок
   ключей last_debug для template_leakage_repair_deferred_to_gate.
2. Прямые тесты покрывают все 18 значимых исходов, `not_matched`,
   вычисление return_text, порядок ключей last_debug, приоритетный case
   группы 2.
3. Контрактные тесты APPLY-6..26 зелёные без изменений.
4. no_mutation_proof: 0 изменений по 23 защищённым + логам APPLY-20..26.
5. Канонический изолированный прогон — ровно 19 известных падений.
6. Строки 898-1015 физически не изменены ни на символ.
