# PRD-047.42-APPLY-28 — Execution tasks / tests / checks (исполнитель: agent)

Источник: `TO_DO_LIST/PRD-047.42-APPLY-28_Enforce_Slice7_BlockB_Part2_v1_RU.md`.
Рабочий файл, ведётся во время реализации, не часть мастер-плана.

## Пересчёт границ (Шаг 1)

Проверено по живому HEAD (c4a447a7): строки **883-999** — ровно от
`if (\n            (planner_next_move == "deepen_mechanism"...` до
последнего `return self._defer_no_stub_repair(...)` группы 12
(`known_concept_self_realization_repair`). Строка **1000** —
`return text` (безусловный финал МЕТОДА) сразу под срезом, БЕЗ
изменений — это конец метода, а не часть выносимого блока. Полное
совпадение с ПРД, без переоценки. Хэш `writer_agent_enforce_slice6.py`
подтверждён: `7336a62b8cc4580e1d5ea702fcdb07f1024b7a41` — совпадает с
ПРД §9.

## Задачи

- [x] Шаг 1 — пересчитать границы по живому HEAD: подтверждено 883-999,
  1000 = `return text` (конец метода), без изменений; хэш slice6
  подтверждён.
- [ ] Шаг 2 — создать `writer_agent_enforce_slice7.py` (классификатор с
  17 тегами исходов, включая `not_matched`, БЕЗ self-вызовов; вызывает
  МОДУЛЬНУЮ starts_with_mechanical_revoicing напрямую; group 9 — вложенный
  if БЕЗ else внутри if БЕЗ else — edge-case list_like=True/first_item=None
  обязан провалиться в sentence_parts, а не выйти из группы).
- [ ] Шаг 3 — заменить строки 883-999 в `writer_agent.py` на вызов
  классификатора + диспетчеризация по `outcome`; `not_matched` — БЕЗ
  ветки, управление проваливается прямо к существующей `return text`
  (строка 1000, физически конец метода, НЕ трогать).
- [ ] Шаг 4 — импорт helper'а в `writer_agent.py` (после импорта slice6).
- [ ] Шаг 5 — runner `TO_DO_LIST/tools/run_prd_047_42_apply_28_enforce_slice7.py`
  по образцу APPLY-27: HISTORICAL_HEAD = c4a447a7, 17 cases,
  build_before_snapshot()/build_after_snapshot(), NORMALIZED_TIMESTAMP.
- [ ] Шаг 6a — `bot_psychologist/tests/contract/test_prd_047_42_apply_28_enforce_slice7.py`.
- [ ] Шаг 6b — `bot_psychologist/tests/multiagent/test_writer_agent_enforce_slice7.py`:
  - все 16 значимых исходов отдельными case'ами;
  - `not_matched`: case, где ни одна из 6 групп не срабатывает, и
    интеграционный тест воспроизводит call-site диспетчеризацию,
    подтверждает итоговый `return == text`;
  - EDGE-CASE группы 9 (Особенность 1): `text="-\n"` (list_like=True,
    first_item=None) с условиями, входящими в группу 9 НЕ через group 8
    (например user_step_request=True, planner_answer_shape!="one_step",
    planner_next_move!="give_direct_step") -> ОБЯЗАН провалиться в
    sentence_parts, не вернуть list_like-ветку, не упасть с исключением;
  - вычисление return_text для `first_item_extraction_g9`
    (`"- пункт один\nвторая строка"`) и `revoicing_strip_g11` (два
    предложения через starts_with_mechanical_revoicing);
  - минимум один приоритетный case: `planner_answer_shape="one_step"`
    формально попадающий и в группу 8, и в группу 9 -> группа 8 обязана
    сработать первой.
- [ ] Шаг 7 — before/after снапшоты по 17 cases, побайтовая идентичность.
- [ ] Шаг 8 — все контрактные тесты APPLY-6..27 зелёные (чистое дерево / реран).
- [ ] Шаг 9 — `no_mutation_proof.md`: 24 защищённых (список v4.31, включая
  writer_agent_enforce_slice6.py = 7336a62b8cc4580e1d5ea702fcdb07f1024b7a41)
  + логи APPLY-20..27 неприкосновенны.
- [ ] Обновить docs (DECISIONS.md, PRD_INDEX.md, PROJECT_STATE.md, ROADMAP.md).
- [ ] Канонический изолированный прогон `pytest tests/ -k writer -q` — ровно
  19 известных падений.
- [ ] Проверить: строка 1000 `return text` физически не изменена, не
  продублирована, не удалена.
- [ ] Заполнить completion metadata в PRD-файле (§7).
- [ ] Основной коммит + push в main.
- [ ] Микро-коммит с отметкой чек-листа/completion metadata + push.

## Не делаем (из ПРД §3)

- НЕ трогаем строку 1000 `return text` — конец метода.
- НЕ добавляем явную ветку `if outcome == "not_matched":` на call site.
- НЕ передаём ни один self-метод внутрь helper'а ни в каком виде.
- НЕ упрощаем вложенную структуру группы 9 (list_like -> first_item ->
  return) в единое условие — изменит поведение на edge-case.
- НЕ объединяем 4 разных "one_step"-тега ВНУТРИ классификатора.
- НЕ дедуплицируем повторяющиеся сигналы с их предыдущими появлениями
  в APPLY-27.
- НЕ запускаем run_prd-скрипты прошлых PRD как скрипты.

## Acceptance (из ПРД §4)

1. before/after снапшоты по 17 cases побайтово идентичны.
2. Прямые тесты покрывают все 16 значимых исходов, `not_matched` с
   правильной итоговой диспетчеризацией в `return text`, EDGE-CASE
   группы 9, вычисление return_text для двух вычисляемых исходов,
   приоритетный case (группа 8 против группы 9).
3. Контрактные тесты APPLY-6..27 зелёные без изменений.
4. no_mutation_proof: 0 изменений по 24 защищённым + логам APPLY-20..27.
5. Канонический изолированный прогон — ровно 19 известных падений.
6. Строка 1000 `return text` физически не изменена ни на символ.
