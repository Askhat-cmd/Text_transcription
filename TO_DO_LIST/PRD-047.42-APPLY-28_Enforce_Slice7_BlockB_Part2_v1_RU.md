# PRD-047.42-APPLY-28 — _enforce_answer_compliance slice 7: Block B часть 2 (mechanism-repair / one-step-каскад с извлечением первого пункта списка / active-line practice-suppression / revoicing / known-concept финал) v1 RU

**Проект:** Bot Psychologist / Neo MindBot
**Репозиторий:** `Askhat-cmd/Text_transcription`
**Локальный путь владельца:** `C:\My_practice\Text_transcription`
**Дата:** 2026-07-23
**Статус:** draft / ready for IDE-agent implementation
**Основание:** независимая верификация PRD-047.42-APPLY-27 (ACCEPTED,
commits 52f071a1/c4a447a7) архитектором завершена; построчная разведка
второй половины Block B (группы 7-12) выполнена в этой же сессии по
свежему HEAD = c4a447a7 (writer_agent.py = 1261 строка).

---

## 0. Короткое решение

```text
Закрываем Block B ПОЛНОСТЬЮ. Строки 883-999 (117 строк, группы 7-12) —
от "if (\n (planner_next_move == "deepen_mechanism"..." до конца
последнего known-concept-условия группы 12. Строка 1000 "return text"
(безусловный финальный fallback ВСЕГО метода _enforce_answer_
compliance) НЕ выносится — остаётся буквально на месте, это конец
метода, а не часть выносимого блока.

Механика — та же, что и в APPLY-27: классификатор, расширенный явным
исходом "not_matched" для "может-не-сработать" групп. Отличие от
APPLY-27: здесь not_matched ведёт не к ещё не вынесенному коду (такого
кода после этого PRD в методе больше нет), а к явному return text —
на call site "not_matched" не требует отдельной ветки, потому что
return text и так стоит последней безусловной строкой сразу после всех
проверок исхода, ровно как это было в оригинале.

Самый сложный узел всего Block B — группа 9 (902-941 на текущей
нумерации): "может-не-сработать" ВНУТРИ "может-не-сработать" —
list_like матчится, но first_item может НЕ смэтчиться (отдельный regex
без capture-группы гарантированно найдёт что-то только если после
маркера списка есть непустой не-\n остаток) — в этом случае код
проваливается ДАЛЬШЕ, к проверке sentence_parts, а не выходит из
группы. Это НЕ описано ни в одном прошлом PRD этой серии — новый
под-паттерн внутри уже знакомой механики "извлечь-и-возможно-вернуть".
Помимо этого группа 9 создаёт 4 внутренних локальных (list_like,
first_item, sentence_parts, has_step_marker), из которых наружу нужен
только результат вычисления return_text для одной ветки
(first_item.group(1).strip()) — остальные три локальных не покидают
helper.

Группа 11 вызывает МОДУЛЬНУЮ (не self) функцию
starts_with_mechanical_revoicing(text) — уже импортирована в
writer_agent.py из ..active_line, безопасна для прямого использования
внутри helper'а (не self, циклического импорта нет).

last_debug: группы 7-12 НЕ содержат ни одной прямой записи —
единственная запись метода уже закрыта в APPLY-27. Технически этот PRD
ПРОЩЕ по этому конкретному риску, но требует максимальной аккуратности
на группе 9.

ДОПОЛНИТЕЛЬНАЯ ПРОВЕРКА АРХИТЕКТОРА (по прецеденту APPLY-27):
предложенный классификатор прогнан дифференциальным фаззинг-тестом
против ПОДЛИННОГО кода строк 883-999 (извлечён из репозитория как
есть, self-методы подменены маркерами, модульная функция
starts_with_mechanical_revoicing — настоящая) — 40 000 случайных
комбинаций, 0 расхождений. ОТДЕЛЬНО протестирован edge-case группы 9
(list_like=True, first_item=None — например text="-\n") — подтверждено,
что код корректно проваливается в проверку sentence_parts вместо
некорректного возврата или исключения.

Новый модуль: writer_agent_enforce_slice7.py.
Новый PRD-инструмент: run_prd_047_42_apply_28_enforce_slice7.py.
```

---

## 1. Особенности — прочитать перед реализацией

```text
ОСОБЕННОСТЬ 1 — новый под-паттерн "maybe-return внутри maybe-return"
(группа 9, строки 902-907). Оригинал:
    if list_like:
        first_item = re.search(r"(?:[-*•]|\d+[.)])\s+(.+)", text)
        if first_item:
            return first_item.group(1).strip()
    sentence_parts = [...]
    ...
Если list_like=True, но first_item НЕ смэтчился (например текст —
маркер списка с одним пробелом/табом/переводом строки и без содержимого
после), код НЕ возвращает и НЕ прерывается — он молча продолжает к
вычислению sentence_parts, как будто list_like было False. helper
обязан воспроизвести это ТОЧНО: вложенный if БЕЗ else, оба уровня
"может провалиться". Тест на этот edge-case ОБЯЗАТЕЛЕН (Шаг 6б).

ОСОБЕННОСТЬ 2 — "not_matched" на этом срезе ведёт к концу метода, а не
к ещё не вынесенному коду. В отличие от APPLY-27 (где not_matched
означал "продолжай к группе 7"), здесь после group 12 в оригинале
СРАЗУ идёт "return text" — последняя строка метода. На call site это
означает: после всех if-проверок исхода просто оставить `return text`
как последнюю строку функции БЕЗ явной проверки на "not_matched" — та
же семантика, но здесь это заодно и физический конец метода
_enforce_answer_compliance, а не временная граница PRD.

ОСОБЕННОСТЬ 3 — starts_with_mechanical_revoicing(text) (группа 11) —
МОДУЛЬНАЯ функция (from ..active_line import
starts_with_mechanical_revoicing), НЕ self-метод. Разрешено вызывать
её напрямую внутри helper'а — не self, не last_debug, чистая функция.
helper импортирует её из того же места (..active_line), что и
writer_agent.py — циклического импорта нет (active_line.py не
импортирует writer_agent_enforce_slice7).

ОСОБЕННОСТЬ 4 — 4 из 5 "one_step"-подобных исходов группы 9 плюс
исход группы 8 (итого 4 разных тега: one_step_g8,
sentence_parts_one_step_g9, question_marker_one_step_g9,
no_step_marker_one_step_g9) вызывают ОДИН и тот же self-метод с
ОДИНАКОВЫМИ аргументами — остаются РАЗНЫМИ тегами внутри классификатора
(прямой прецедент Особенности 3 из APPLY-27 — для тестируемости и
трассировки), объединяются только на call site в один return.

ОСОБЕННОСТЬ 5 — известные повторы сигналов. mechanism_explanation_repair
(группа 7) — ТРЕТЬЕ по счёту появление этого сигнала в методе (первое —
APPLY-27 группа 4, второе — APPLY-27 группа 7, то есть уже объединённая
часть 1). known_concept_correlation/neurostalking/self_realization
(группа 12) — ТРЕТЬЕ по счёту появление этих сигналов (первое и
второе — оба в APPLY-27, части 1). Это НЕ дедуплицируется между PRD —
каждое физическое условие оригинала остаётся отдельным кодовым путём
со своим тегом внутри своего среза.
```

---

## 2. Технический план для Исполнителя

```text
Шаг 1. Пересчитать границы по свежему HEAD перед стартом: подтвердить,
  что строки 883-999 (в терминах текущего HEAD) — это ровно от
  "if (\n            (planner_next_move == \"deepen_mechanism\"..." до
  последнего "return self._defer_no_stub_repair(...)" группы 12
  (known_concept_self_realization_repair), строка 1000 "return text"
  СРАЗУ под срезом остаётся БЕЗ ИЗМЕНЕНИЙ (это конец метода). Расхождение
  в пределах форматирования — не blocker; структурное — STOP, в отчёт.

Шаг 2. Создать writer_agent_enforce_slice7.py:

from __future__ import annotations

import re
from dataclasses import dataclass, field
from typing import Any, Literal, Optional

from ..active_line import starts_with_mechanical_revoicing
from .writer_agent_constants import _contains_any


EnforceBlockBPart2Outcome = Literal[
    "not_matched",
    "mechanism_explanation_repair_g7",
    "one_step_g8",
    "first_item_extraction_g9",
    "sentence_parts_one_step_g9",
    "question_marker_one_step_g9",
    "no_step_marker_one_step_g9",
    "safety_grounding_g10",
    "active_line_correction_repair_g10",
    "active_line_mechanism_repair_g10",
    "practice_suppression_meaning_repair_g10",
    "active_line_revoicing_correction_repair_g11",
    "active_line_revoicing_mechanism_repair_g11",
    "revoicing_strip_g11",
    "known_concept_correlation_repair_g12",
    "known_concept_neurostalking_repair_g12",
    "known_concept_self_realization_repair_g12",
]


@dataclass(frozen=True)
class EnforceBlockBPart2Result:
    outcome: EnforceBlockBPart2Outcome
    return_text: Optional[str] = None


def _classify_enforce_block_b_part2(
    *,
    text: str,
    user_message: str,
    lowered_user: str,
    lowered_text: str,
    planner_next_move: str,
    planner_question_policy: str,
    planner_answer_shape: str,
    planner_practice_policy: str,
    user_mechanism_request: bool,
    user_requests_no_question: bool,
    user_requests_no_practice: bool,
    has_question: bool,
    has_unsolicited_practice: bool,
    user_step_request: bool,
    active_line_intent: str,
    active_line_practice_suppression: bool,
    active_line_should_offer_practice: bool,
    active_line_repair_mode: bool,
    active_line_revoicing_allowed: bool,
) -> EnforceBlockBPart2Result:
    if (
        (planner_next_move == "deepen_mechanism" or user_mechanism_request)
        and (planner_question_policy == "none" or user_requests_no_question)
        and (len(text) > 700 or has_question or has_unsolicited_practice or user_requests_no_practice)
    ):
        return EnforceBlockBPart2Result(outcome="mechanism_explanation_repair_g7")

    if planner_answer_shape == "one_step" or planner_next_move == "give_direct_step":
        return EnforceBlockBPart2Result(outcome="one_step_g8")

    if planner_answer_shape == "one_step" or user_step_request or active_line_intent == "ask_for_direct_step":
        list_like = bool(re.search(r"(^|\n)\s*(?:[-*•]|\d+[.)])\s+", text))
        if list_like:
            first_item = re.search(r"(?:[-*•]|\d+[.)])\s+(.+)", text)
            if first_item:
                return EnforceBlockBPart2Result(
                    outcome="first_item_extraction_g9",
                    return_text=first_item.group(1).strip(),
                )
        sentence_parts = [part.strip() for part in re.split(r"(?<=[.!?])\s+", text) if part.strip()]
        if len(sentence_parts) > 2:
            return EnforceBlockBPart2Result(outcome="sentence_parts_one_step_g9")
        if planner_question_policy == "none" and _contains_any(
            lowered_text,
            (
                "хочешь",
                "хочется",
                "можешь",
                "уточни",
                "попробу",
                "какой",
                "что выбрать",
            ),
        ):
            return EnforceBlockBPart2Result(outcome="question_marker_one_step_g9")
        has_step_marker = _contains_any(lowered_text, ("сделай", "начни", "открой", "выбери", "напиши", "шаг"))
        if not has_step_marker:
            return EnforceBlockBPart2Result(outcome="no_step_marker_one_step_g9")

    if active_line_practice_suppression and not active_line_should_offer_practice and has_unsolicited_practice:
        if planner_next_move == "stabilize_safety" or planner_answer_shape == "safety_grounding":
            return EnforceBlockBPart2Result(outcome="safety_grounding_g10")
        if active_line_intent == "correction_of_bot" or active_line_repair_mode:
            return EnforceBlockBPart2Result(outcome="active_line_correction_repair_g10")
        if active_line_intent == "understand_mechanism":
            return EnforceBlockBPart2Result(outcome="active_line_mechanism_repair_g10")
        return EnforceBlockBPart2Result(outcome="practice_suppression_meaning_repair_g10")

    if not active_line_revoicing_allowed and starts_with_mechanical_revoicing(text):
        if active_line_intent == "correction_of_bot" or active_line_repair_mode:
            return EnforceBlockBPart2Result(outcome="active_line_revoicing_correction_repair_g11")
        if active_line_intent == "understand_mechanism":
            return EnforceBlockBPart2Result(outcome="active_line_revoicing_mechanism_repair_g11")
        parts = re.split(r"(?<=[.!?])\s+", text, maxsplit=1)
        if len(parts) == 2 and parts[1].strip():
            return EnforceBlockBPart2Result(
                outcome="revoicing_strip_g11",
                return_text=parts[1].strip(),
            )

    if planner_next_move == "answer_known_concept" and planner_practice_policy == "forbidden":
        if "самореализац" in lowered_user and "нейросталкинг" in lowered_user:
            return EnforceBlockBPart2Result(outcome="known_concept_correlation_repair_g12")
        if "нейросталкинг" in lowered_user:
            return EnforceBlockBPart2Result(outcome="known_concept_neurostalking_repair_g12")
        if "самореализац" in lowered_user:
            return EnforceBlockBPart2Result(outcome="known_concept_self_realization_repair_g12")

    return EnforceBlockBPart2Result(outcome="not_matched")

  Каждое условие — точная копия строк 883-999. Порядок проверок не
  менять. list_like/first_item/sentence_parts/has_step_marker/parts —
  локальные ИСКЛЮЧИТЕЛЬНО внутри helper'а, наружу не отдаются (кроме
  вычисленного return_text для first_item_extraction_g9 и
  revoicing_strip_g11).

Шаг 3. В writer_agent.py заменить строки 883-999 на:

        block_b_part2_result = _classify_enforce_block_b_part2(
            text=text,
            user_message=user_message,
            lowered_user=lowered_user,
            lowered_text=lowered_text,
            planner_next_move=planner_next_move,
            planner_question_policy=planner_question_policy,
            planner_answer_shape=planner_answer_shape,
            planner_practice_policy=planner_practice_policy,
            user_mechanism_request=user_mechanism_request,
            user_requests_no_question=user_requests_no_question,
            user_requests_no_practice=user_requests_no_practice,
            has_question=has_question,
            has_unsolicited_practice=has_unsolicited_practice,
            user_step_request=user_step_request,
            active_line_intent=active_line_intent,
            active_line_practice_suppression=active_line_practice_suppression,
            active_line_should_offer_practice=active_line_should_offer_practice,
            active_line_repair_mode=active_line_repair_mode,
            active_line_revoicing_allowed=active_line_revoicing_allowed,
        )
        if block_b_part2_result.outcome == "mechanism_explanation_repair_g7":
            return self._defer_no_stub_repair(
                signal="mechanism_explanation_repair",
                text=text,
                must_answer=user_message,
            )
        if block_b_part2_result.outcome in {
            "one_step_g8",
            "sentence_parts_one_step_g9",
            "question_marker_one_step_g9",
            "no_step_marker_one_step_g9",
        }:
            return self._resolve_one_step_or_no_practice_fallback(
                text=text,
                user_message=user_message,
                lowered_user=lowered_user,
                canned_step_disallowed=canned_step_disallowed,
            )
        if block_b_part2_result.outcome == "first_item_extraction_g9":
            return block_b_part2_result.return_text
        if block_b_part2_result.outcome == "safety_grounding_g10":
            return "Я рядом. Сейчас важнее чуть стабилизироваться и снизить внутреннюю перегрузку. Без разбора, только опора здесь-и-сейчас."
        if block_b_part2_result.outcome == "active_line_correction_repair_g10":
            return self._defer_no_stub_repair(
                signal="active_line_correction_repair",
                text=text,
                must_answer=user_message,
            )
        if block_b_part2_result.outcome == "active_line_mechanism_repair_g10":
            return self._defer_no_stub_repair(
                signal="active_line_mechanism_repair",
                text=text,
                must_answer=user_message,
            )
        if block_b_part2_result.outcome == "practice_suppression_meaning_repair_g10":
            return self._defer_no_stub_repair(
                signal="practice_suppression_meaning_repair",
                text=text,
                must_answer=user_message,
            )
        if block_b_part2_result.outcome == "active_line_revoicing_correction_repair_g11":
            return self._defer_no_stub_repair(
                signal="active_line_revoicing_correction_repair",
                text=text,
                must_answer=user_message,
            )
        if block_b_part2_result.outcome == "active_line_revoicing_mechanism_repair_g11":
            return self._defer_no_stub_repair(
                signal="active_line_revoicing_mechanism_repair",
                text=text,
                must_answer=user_message,
            )
        if block_b_part2_result.outcome == "revoicing_strip_g11":
            return block_b_part2_result.return_text
        if block_b_part2_result.outcome == "known_concept_correlation_repair_g12":
            return self._defer_no_stub_repair(
                signal="known_concept_correlation_repair",
                text=text,
                must_answer=user_message,
            )
        if block_b_part2_result.outcome == "known_concept_neurostalking_repair_g12":
            return self._defer_no_stub_repair(
                signal="known_concept_neurostalking_repair",
                text=text,
                must_answer=user_message,
            )
        if block_b_part2_result.outcome == "known_concept_self_realization_repair_g12":
            return self._defer_no_stub_repair(
                signal="known_concept_self_realization_repair",
                text=text,
                must_answer=user_message,
            )
        return text

  ВАЖНО: последняя строка "return text" — это буквально существующая
  строка 1000 оригинала (конец метода _enforce_answer_compliance).
  block_b_part2_result.outcome == "not_matched" НЕ имеет отдельной
  проверки — просто отсутствие совпавшего if достаточно, управление
  проваливается прямо к этому "return text" (Особенность 2).

Шаг 4. Импорт helper'а в writer_agent.py (после импорта slice6).

Шаг 5. Runner run_prd_047_42_apply_28_enforce_slice7.py — архитектура
  APPLY-27 runner'а (реконструкция historical-класса из git show
  HISTORICAL_HEAD, где HISTORICAL_HEAD = HEAD ПЕРЕД этим PRD = c4a447a7),
  17 cases харнесса APPLY-20/21, build_before_snapshot()/build_after_
  snapshot() импортируемые, NORMALIZED_TIMESTAMP установлен ДО генерации
  файлов.

Шаг 6. Тесты:
  a) tests/contract/test_prd_047_42_apply_28_enforce_slice7.py — по
     образцу APPLY-27.
  b) tests/multiagent/test_writer_agent_enforce_slice7.py — прямые,
     ОБЯЗАТЕЛЬНО покрывают:
     - все 16 значимых исходов отдельными case'ами;
     - "not_matched": case, где ни одно из 6 групп не срабатывает
       (например planner_next_move="unrelated", planner_answer_shape=
       "other", user_step_request=False, active_line_intent="other",
       active_line_practice_suppression=False, active_line_revoicing_
       allowed=True, planner_practice_policy="allowed") -> outcome==
       "not_matched", и на call site это должно вести к return text
       (интеграционный тест: вызвать helper, затем воспроизвести
       call-site диспетчеризацию, подтвердить итоговый return == text);
     - EDGE-CASE группы 9 (Особенность 1): text="-\n" (list_like=True,
       first_item=None) с условиями, не задевающими группу 8
       (planner_answer_shape != "one_step", planner_next_move !=
       "give_direct_step"), но входящими в группу 9 через user_step_
       request=True -> ОБЯЗАН провалиться в проверку sentence_parts
       (не вернуть list_like-ветку, не упасть с исключением) ->
       итоговый outcome по факту остальных условий (например
       "no_step_marker_one_step_g9", если условия совпадают);
     - вычисление return_text для first_item_extraction_g9 (текст
       "- пункт один\nвторая строка") и revoicing_strip_g11 (текст из
       двух предложений через starts_with_mechanical_revoicing);
     - минимум один case на приоритет: planner_answer_shape="one_step"
       ОДНОВРЕМЕННО с условиями, которые формально попадали бы в группу
       9 -> группа 8 обязана сработать первой (порядок групп).

Шаг 7. before/after снапшоты — побайтовая идентичность по всем 17 cases.

Шаг 8. Все контрактные тесты APPLY-6..27 (чистое дерево).

Шаг 9. no_mutation_proof.md: 24 защищённых файла (список v4.31, hash
  writer_agent_enforce_slice6.py = 7336a62b8cc4580e1d5ea702fcdb07f1024b7a41,
  зафиксирован архитектором сразу после приёмки APPLY-27) + логи
  APPLY-20..27 неприкосновенны.
```

---

## 3. Не делаем

```text
- НЕ трогаем строку 1000 "return text" — это конец метода, а не часть
  выносимого блока, переносить/дублировать/менять её нельзя.
- НЕ добавляем явную ветку `if outcome == "not_matched":` на call site
  — просто оставляем `return text` последней безусловной строкой
  (Особенность 2).
- НЕ передаём ни один self-метод внутрь helper'а ни в каком виде.
- НЕ упрощаем вложенную структуру группы 9 (list_like -> first_item ->
  return) в единое условие — это изменит поведение на edge-case, где
  list_like=True, а first_item=None (Особенность 1).
- НЕ объединяем 4 разных "one_step"-тега (one_step_g8, sentence_parts_
  one_step_g9, question_marker_one_step_g9, no_step_marker_one_step_g9)
  в один тег ВНУТРИ классификатора — они разные физические условия,
  разные теги нужны для тестируемости; объединять можно только на call
  site при диспетчеризации.
- НЕ дедуплицируем повторяющиеся сигналы (mechanism_explanation_repair,
  known_concept_*) с их предыдущими появлениями в APPLY-27 — они
  отдельные физические условия в другом месте кода.
- НЕ запускаем run_prd-скрипты прошлых PRD как скрипты (красная линия).
```

---

## 4. Acceptance criteria

```text
ACCEPTED, если:
1. before/after снапшоты по 17 cases побайтово идентичны.
2. Прямые тесты покрывают все 16 значимых исходов, "not_matched" с
   правильной итоговой диспетчеризацией в return text, EDGE-CASE
   группы 9 (list_like=True, first_item=None), вычисление return_text
   для двух вычисляемых исходов, и минимум один приоритетный case
   (группа 8 против группы 9).
3. Все контрактные тесты APPLY-6..27 зелёные без изменений.
4. no_mutation_proof: 0 изменений по 24 защищённым (включая
   writer_agent_enforce_slice6.py = 7336a62b8cc4580e1d5ea702fcdb07f1024b7a41)
   + логам APPLY-20..27.
5. Канонический изолированный прогон pytest tests/ -k writer -q: ровно
   19 известных падений, тот же список.
6. Строка 1000 "return text" физически не изменена ни на символ, не
   продублирована, не удалена.

BLOCKED, если: снапшот разошёлся хоть в байте; текст любого литерала
отличается хоть на символ от оригинала; edge-case группы 9 обработан
неверно (например наивный "elif" вместо вложенного "if" без else,
из-за чего list_like=True без first_item ошибочно завершает группу 9
вместо провала в sentence_parts); порядок проверки условий изменился;
строка 1000 задета; любой protected file или лог прошлого PRD изменился.
```

---

## 5. Задел на следующий шаг

```text
После APPLY-28 Block B закрыт ПОЛНОСТЬЮ — весь метод
_enforce_answer_compliance (строки 576-1000 текущей нумерации)
декомпозирован. Следующий шаг — НЕ ещё один срез этого метода, а
СЛЕДУЮЩИЙ метод: boundary mapping и декомпозиция
_enforce_mvp_free_dialogue_compliance (начинается сразу после, строка
1002 текущей нумерации; длина на момент последней оценки — 225 строк,
пересчитать точно по факту HEAD после APPLY-28). Этот метод НЕ
картирован вообще — своя отдельная разведка с нуля, не переносить
предположения о структуре из writer_agent.py.

После этого — Сценарий А (writer_agent.py в порядке) близок к
завершению; лёгкий шлюз в Эпоху 2 (владелец-решение #4) откроется по
факту завершения _enforce_mvp_free_dialogue_compliance, если не
всплывёт скрытая сложность при её разведке (не предполагать заранее —
прямой прецедент того, как карта APPLY-20 дважды занижала фактическую
структуру).
```

---

## 6. Simple explanation (для Асхата)

```text
Что делаем: последний кусок "редактора ответов" внутри самого метода —
шесть условий подряд: "объясни механизм подробнее", "дай один
конкретный шаг" (в том числе умное извлечение уже готового пункта
списка из текста, если бот сам написал список), защита от навязчивой
практики при активной линии разговора, защита от "механического
повторения слов пользователя", и финальный путь для уже знакомых
понятий. После этого блока в методе больше ничего нет — весь
"редактор ответов" будет разобран.

Особенность этого шага: нашёлся один по-настоящему тонкий случай внутри
самого сложного куска (пункт списка есть, но после маркера ничего нет)
— я явно расписал его Исполнителю и потребовал отдельный тест именно на
него, чтобы не потерять это поведение случайно.

Что делать прямо сейчас:
1. Передать Исполнителю файл обновления Части 4 (v4.31) — он же
   фиксирует итог APPLY-27.
2. Затем передать этот файл — PRD-047.42-APPLY-28.
3. Когда будет "готово" — прислать хэши, проверю независимо, с особым
   фокусом на edge-case группы 9.
```

---

## 7. Completion metadata (заполняется после приёмки)

```text
Статус исполнения: (не заполнено)
Основной implementation commit: (не заполнено)
Main push status: (не заполнено)
```
