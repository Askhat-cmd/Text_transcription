# PRD-047.42-APPLY-29 + APPLY-30 — Execution tasks / tests / checks (исполнитель: agent)

Источники:
- `TO_DO_LIST/PRD-047.42-APPLY-29_Mvp_Slice1_Part1_v1_RU.md`
- `TO_DO_LIST/PRD-047.42-APPLY-30_Mvp_Slice2_Part2_v1_RU.md`

Рабочий файл, ведётся во время реализации, не часть мастер-плана.
Оба PRD выполняются последовательно в одной сессии (APPLY-29, затем
APPLY-30, границы APPLY-30 пересчитываются заново после APPLY-29, как
явно требует сам ПРД).

## Пересчёт границ APPLY-29 (Шаг 1)

Проверено по живому HEAD (05c9de9): строки **1019-1126** — ровно от
`if pragmatics_repair_dissatisfaction:` (группа A) до конца группы J
(`return self._strip_optional_followup_invitation(text) or text`).
Строка 1127 пустая, строка **1128** — начало group K
(`if practice_overview_requested or planner_answer_shape ==
"practice_catalog_explanation":`), сразу под срезом. Полное совпадение
с ПРД. `offer_repair_context` (строка 1018) подтверждён как
мёртвый код — единственное вхождение во всём методе (только своё же
присваивание) — НЕ переносится в helper, документируется как есть.
Метод `_enforce_mvp_free_dialogue_compliance` начинается на строке 979,
writer_agent.py = 1238 строк — совпадает с master plan v4.32.

## Задачи APPLY-29

- [x] Шаг 1 — границы 1019-1126 подтверждены, offer_repair_context
  подтверждён мёртвым кодом.
- [ ] Шаг 2 — создать `writer_agent_mvp_slice1.py` (классификатор с 17
  тегами, включая not_matched; ВСЕГДА возвращает updated_text/
  updated_lowered_text; computed_target для группы A; last_debug_patch
  только для двух исходов группы J, патч одинаковый, исходы разные).
- [ ] Шаг 3 — заменить строки 1019-1126 в `writer_agent.py` на вызов
  классификатора; СРАЗУ после вызова — `text = mvp1_result.updated_text;
  lowered_text = mvp1_result.updated_lowered_text` (ДО проверки outcome);
  диспетчеризация по outcome; not_matched — без ветки, управление
  проваливается в группу K (инлайн-код, до APPLY-30).
- [ ] Шаг 4 — импорт helper'а в `writer_agent.py` (после enforce_slice7).
- [ ] Шаг 5 — runner `TO_DO_LIST/tools/run_prd_047_42_apply_29_mvp_slice1.py`
  по образцу APPLY-27/28: HISTORICAL_HEAD = 05c9de9, 17 cases.
- [ ] Шаг 6a — `bot_psychologist/tests/contract/test_prd_047_42_apply_29_mvp_slice1.py`.
- [ ] Шаг 6b — `bot_psychologist/tests/multiagent/test_writer_agent_mvp_slice1.py`:
  - все 16 значимых исходов + not_matched;
  - МУТАЦИЯ text/lowered_text (group B не возвращает, "хочешь"+"?" в
    тексте) — updated_text ОБЯЗАН отличаться от входного;
  - computed_target для группы A (оба варианта: repair_answer_last_question
    и repair_user_dissatisfaction);
  - две ветки group J с одинаковым патчем, разными исходами (тест на
    оба, оба патча идентичны по содержимому, разные по outcome).
- [ ] Шаг 7 — before/after снапшоты по 17 cases, побайтовая идентичность.
- [ ] Шаг 8 — все контрактные тесты APPLY-6..28 зелёные (чистое дерево / реран).
- [ ] Шаг 9 — `no_mutation_proof.md`: 25 защищённых + логи APPLY-20..28
  неприкосновенны.
- [ ] Канонический изолированный прогон `pytest tests/ -k writer -q` —
  ровно 19 известных падений.
- [ ] Заполнить completion metadata в PRD-29 файле (§6).
- [ ] Основной коммит + push в main (APPLY-29).
- [ ] Микро-коммит с отметкой чек-листа/completion metadata + push (APPLY-29).

## Пересчёт границ APPLY-30 (после APPLY-29, ОБЯЗАТЕЛЬНО заново)

ПРД явно требует пересчитать границы K-P + финал ПОСЛЕ merge APPLY-29,
так как строки сдвинутся. Выполнить это как отдельный шаг перед стартом
APPLY-30 (см. ниже, после того как APPLY-29 будет закоммичен).

## Задачи APPLY-30

- [ ] Шаг 0 — пересчитать границы групп K-P + финальный блок метода по
  свежему HEAD (после коммита APPLY-29). Зафиксировать фактические
  номера строк здесь перед реализацией.
- [ ] Шаг 2 — создать `writer_agent_mvp_slice2.py` (классификатор с 9
  тегами, включая not_matched как терминальный default с вычисленным
  return_text=sanitized_final; last_debug_patch с ВЫЧИСЛЯЕМЫМ
  answer_fit_repair_applied, НЕ литералом True; `_strip_optional_
  followup_invitation` импортируется как чистая модульная функция —
  единственное обоснованное отступление от "self остаётся в
  writer_agent.py", не создаёт прецедент вслепую).
- [ ] Шаг 3 — заменить строки групп K-P в `writer_agent.py` на вызов
  классификатора + диспетчеризация; последние 2 строки — безусловный
  финал (self._set_final_answer_shape_debug(planner_answer_shape or
  "compact_direct") + return mvp2_result.return_text).
- [ ] Шаг 4 — импорт helper'а (после mvp_slice1).
- [ ] Шаг 5 — runner `TO_DO_LIST/tools/run_prd_047_42_apply_30_mvp_slice2.py`
  — та же архитектура, HISTORICAL_HEAD = HEAD после APPLY-29.
- [ ] Шаг 6a — `bot_psychologist/tests/contract/test_prd_047_42_apply_30_mvp_slice2.py`.
- [ ] Шаг 6b — `bot_psychologist/tests/multiagent/test_writer_agent_mvp_slice2.py`:
  - все 9 значимых исходов (включая not_matched: оба под-случая
    answer_obligation в множестве / не в множестве);
  - ВЫЧИСЛЯЕМОЕ (не литеральное) answer_fit_repair_applied в группе P:
    два case (concrete_need=True и False), оба должны попасть в
    last_debug_patch с соответствующим bool.
- [ ] Шаг 7 — before/after снапшоты по 17 cases, побайтовая идентичность.
- [ ] Шаг 8 — все контрактные тесты APPLY-6..29 зелёные (чистое дерево / реран).
- [ ] Шаг 9 — `no_mutation_proof.md`: 26 защищённых + логи APPLY-20..29
  неприкосновенны.
- [ ] Канонический изолированный прогон `pytest tests/ -k writer -q` —
  ровно 19 известных падений.
- [ ] Проверить: метод `_enforce_mvp_free_dialogue_compliance` полностью
  декомпозирован (0 инлайн-групп, только 2 вызова классификаторов +
  диспетчеризация).
- [ ] Заполнить completion metadata в PRD-30 файле (§6).
- [ ] Основной коммит + push в main (APPLY-30).
- [ ] Микро-коммит с отметкой чек-листа/completion metadata + push (APPLY-30).

## Не делаем

- НЕ чиним offer_repair_context — документируем как мёртвый код (APPLY-29).
- НЕ забываем переприсвоить updated_text/updated_lowered_text СРАЗУ
  после вызова классификатора, ДО проверки outcome (APPLY-29).
- НЕ хардкодим True для answer_fit_repair_applied в группе P — только
  вычисленное значение (APPLY-30).
- НЕ переносим self._strip_optional_followup_invitation как self-вызов —
  используем чистую модульную функцию, на которую он ссылается (APPLY-30).
- НЕ передаём self-методы внутрь helper'ов ни в каком виде (оба PRD).
- НЕ объединяем физически разные исходы с одинаковым патчем в один тег
  внутри классификатора (оба PRD).
- НЕ запускаем run_prd-скрипты прошлых PRD как скрипты (красная линия).

## Acceptance

APPLY-29: снапшоты 17/17 byte-identical; тест на мутацию text/lowered_text
проходит; контрактные APPLY-6..28 зелёные; канонический прогон — 19
падений; 25 защищённых не тронуты; логи APPLY-20..28 целы.

APPLY-30: снапшоты 17/17 byte-identical; тест на вычисляемое
answer_fit_repair_applied проходит для обоих bool; контрактные APPLY-6..29
зелёные; канонический прогон — 19 падений; 26 защищённых не тронуты;
логи APPLY-20..29 целы; метод полностью декомпозирован.
