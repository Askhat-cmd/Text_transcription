# PRD-047.42-APPLY-30 — _enforce_mvp_free_dialogue_compliance slice 2 (Part 2: practice-catalog / direct-no-question / repair-and-expand / concept-explanation / expansion-application / stale-stub / метод завершён) v1 RU

**Проект:** Bot Psychologist / Neo MindBot
**Дата:** 2026-07-23
**Статус:** draft / ready for IDE-agent implementation
**Основание:** Part 1 (APPLY-29) закрывает группы A-J. Этот PRD
закрывает группы K-P и ФИНАЛЬНЫЙ fallback метода — после него
`_enforce_mvp_free_dialogue_compliance` декомпозирован полностью.
Тот же ускоренный темп, ссылки на ADR-125/126/127 вместо
переобъяснения механики.

---

## 0. Короткое решение

```text
Строки 1128-1201 (74 строки, HEAD 05c9de9 — пересчитать после APPLY-29,
границы сдвинутся). Группы K, L, M, N, O, P + безусловный финальный
блок метода (сейчас строки 1191-1201: computed sanitized_final, return).

В отличие от Part 1: last_debug здесь одна точка (группа P), но с
ВАЖНЫМ отличием от всех прошлых патчей в этой серии — значение
answer_fit_repair_applied здесь НЕ литерал True, а ВЫЧИСЛЯЕМОЕ
bool(answer_fit.get("concrete_need", False)) — может быть True ИЛИ
False. Патч обязан нести вычисленное значение, не хардкодить True.

Группа O — единственное место во всей декомпозиции (и предыдущего, и
этого метода), где РЕЗУЛЬТАТ self._strip_optional_followup_invitation
используется для ВНУТРЕННЕЙ логики принятия решения (длина, цвета,
"нейросталкинг" в вычищенном тексте), а не только относится на
терминальный return. Разведка подтвердила: этот self-метод — тонкая
обёртка @staticmethod над МОДУЛЬНОЙ функцией
_strip_optional_followup_invitation из writer_agent_fallback_helpers.py
(защищённый файл) — чистая, без self, без last_debug. Импортируется
напрямую в helper, как _contains_any/starts_with_mechanical_revoicing
в прошлых срезах. Это НЕ нарушает правило "self-вызовы остаются в
writer_agent.py" — правило касается вызовов через self (методов
инстанса с потенциальным доступом к last_debug), а не одноимённой
чистой функции, на которую self-метод является лишь тонкой обёрткой.

not_matched здесь = физический конец метода: финальный fallback
(sanitized_final, self._set_final_answer_shape_debug(planner_answer_
shape or "compact_direct")) вычисляется ВНУТРИ классификатора (только
._strip_optional_followup_invitation — чистая часть) и возвращается как
return_text при outcome="not_matched"; self-вызов
_set_final_answer_shape_debug остаётся на call site.

Дифференциальный фаззинг-тест (20 000 комбинаций против дословно
извлечённого кода строк 1128-1201) — 0 расхождений.

Новый модуль: writer_agent_mvp_slice2.py.

ПОСЛЕ этого PRD: _enforce_mvp_free_dialogue_compliance декомпозирован
ПОЛНОСТЬЮ — второй (и последний на сегодня) крупный метод в
writer_agent.py закрыт. Ожидаемый следующий шаг после приёмки — сверка
DoD §5.6 / Сценарий А целиком и обсуждение открытия шлюза в Эпоху 2 с
владельцем (не мой единоличный технический выбор — стратегическая
развилка).
```

---

## 1. Особенности

```text
1. Группа P: last_debug_patch несёт ВЫЧИСЛЕННОЕ значение
   answer_fit_repair_applied (bool(answer_fit.get("concrete_need",
   False))), не литерал True — единственный такой случай во всей
   декомпозиции обоих методов.
2. _strip_optional_followup_invitation импортируется напрямую как
   чистая функция (см. "Короткое решение") — единственное отступление
   от "self-вызовы остаются в writer_agent.py", обосновано и
   задокументировано, не создаёт последующий прецедент вслепую для
   ДРУГИХ self-методов без такой же проверки чистоты.
3. detect_stale_stub вызывается здесь (группа P) НЕЗАВИСИМО от вызова
   в группе J (Part 1) — не переиспользуется, пересчитывается заново,
   как в оригинале (тот же text к этому моменту не изменяется между
   Part 1 и Part 2, кроме случая мутации из группы B в Part 1, которая
   уже разрешена через updated_text до входа в Part 2).
```

---

## 2. Технический план

```text
Шаг 1. Пересчитать границы K-P + финал по свежему HEAD (после APPLY-29
  merge).

Шаг 2. Создать writer_agent_mvp_slice2.py:

from __future__ import annotations

import re
from dataclasses import dataclass, field
from typing import Any, Literal, Optional

from .writer_agent_fallback_helpers import _strip_optional_followup_invitation
from ..stale_stub_detector import detect_stale_stub


MvpPart2Outcome = Literal[
    "not_matched",
    "practice_catalog_repair",
    "direct_no_forced_question",
    "repair_and_expand",
    "concept_explanation_repair",
    "preserved_application_answer",
    "concept_expansion_repair",
    "expanded_explanation_repair",
    "stale_stub_retry_deferred_to_gate",
]


@dataclass(frozen=True)
class MvpPart2Result:
    outcome: MvpPart2Outcome
    return_text: Optional[str] = None
    last_debug_patch: dict[str, Any] = field(default_factory=dict)


_ANSWER_OBLIGATIONS_PRESERVE = {
    "answer_direct_question",
    "answer_knowledge_question",
    "provide_one_bounded_practice",
    "answer_last_offer",
    "repair_and_answer_last_question",
}


def _classify_mvp_part2(
    *,
    text: str,
    user_message: str,
    lowered_user: str,
    concept: str,
    practice_overview_requested: bool,
    planner_answer_shape: str,
    planner_question_policy: str,
    has_question: bool,
    expansion_requested: bool,
    repair_and_expand_requested: bool,
    user_repair_signal: bool,
    should_answer_directly: bool,
    asks_define_known_term: bool,
    has_external_surveillance_frame: bool,
    application_request: bool,
    active_line_intent: str,
    answer_obligation: str,
    answer_fit: dict[str, Any],
) -> MvpPart2Result:
    if practice_overview_requested or planner_answer_shape == "practice_catalog_explanation":
        list_items = re.findall(r"(?m)^\s*(?:[-*•]|\d+[.)])\s+", text)
        if len(list_items) < 3 or len(text) < 420:
            return MvpPart2Result(outcome="practice_catalog_repair")

    if planner_question_policy == "none" and has_question and not expansion_requested:
        return MvpPart2Result(
            outcome="direct_no_forced_question",
            return_text=re.sub(r"\s*\?+\s*", ". ", text).strip(),
        )

    if repair_and_expand_requested or user_repair_signal:
        return MvpPart2Result(outcome="repair_and_expand")

    if should_answer_directly and (asks_define_known_term or has_external_surveillance_frame):
        return MvpPart2Result(outcome="concept_explanation_repair")

    if (expansion_requested or application_request) and len(text) < 260:
        if answer_obligation in _ANSWER_OBLIGATIONS_PRESERVE:
            preserved_text = _strip_optional_followup_invitation(text)
            preserved_lower = preserved_text.lower()
            if (
                len(preserved_text) >= 120
                or any(color in preserved_lower for color in ("красн", "оранж", "зелен"))
                or "нейросталкинг" in preserved_lower
            ):
                return MvpPart2Result(
                    outcome="preserved_application_answer",
                    return_text=preserved_text,
                )
        if concept == "нейросталкинг" or "нейросталкинг" in lowered_user or active_line_intent == "known_concept_question":
            return MvpPart2Result(outcome="concept_expansion_repair")
        return MvpPart2Result(outcome="expanded_explanation_repair")

    stale_stub = detect_stale_stub(text)
    if bool(stale_stub.get("detected", False)):
        return MvpPart2Result(
            outcome="stale_stub_retry_deferred_to_gate",
            return_text=text,
            last_debug_patch={
                "answer_fit_repair_applied": bool(answer_fit.get("concrete_need", False)),
                "template_leakage_repair_deferred_to_gate": True,
            },
        )

    sanitized_final = text
    if answer_obligation in _ANSWER_OBLIGATIONS_PRESERVE:
        sanitized_final = _strip_optional_followup_invitation(text) or text
    return MvpPart2Result(outcome="not_matched", return_text=sanitized_final)

Шаг 3. В writer_agent.py заменить строки 1128-1201 (пересчитать точно)
  на:

        mvp2_result = _classify_mvp_part2(
            text=text,
            user_message=user_message,
            lowered_user=lowered_user,
            concept=concept,
            practice_overview_requested=practice_overview_requested,
            planner_answer_shape=planner_answer_shape,
            planner_question_policy=planner_question_policy,
            has_question=has_question,
            expansion_requested=expansion_requested,
            repair_and_expand_requested=repair_and_expand_requested,
            user_repair_signal=user_repair_signal,
            should_answer_directly=should_answer_directly,
            asks_define_known_term=asks_define_known_term,
            has_external_surveillance_frame=has_external_surveillance_frame,
            application_request=application_request,
            active_line_intent=active_line_intent,
            answer_obligation=answer_obligation,
            answer_fit=answer_fit,
        )
        if mvp2_result.outcome == "practice_catalog_repair":
            return self._defer_no_stub_repair(
                signal="mvp_practice_catalog_repair",
                text=text,
                must_answer="practice_catalog_explanation",
            )
        if mvp2_result.outcome == "direct_no_forced_question":
            self._set_final_answer_shape_debug("direct_no_forced_question")
            return mvp2_result.return_text
        if mvp2_result.outcome == "repair_and_expand":
            return self._defer_no_stub_repair(
                signal="mvp_repair_and_expand",
                text=text,
                must_answer=user_message,
            )
        if mvp2_result.outcome == "concept_explanation_repair":
            return self._defer_no_stub_repair(
                signal="mvp_concept_explanation_repair",
                text=text,
                must_answer=user_message,
            )
        if mvp2_result.outcome == "preserved_application_answer":
            self._set_final_answer_shape_debug("preserved_application_answer")
            return mvp2_result.return_text
        if mvp2_result.outcome == "concept_expansion_repair":
            return self._defer_no_stub_repair(
                signal="mvp_concept_expansion_repair",
                text=text,
                must_answer=user_message,
            )
        if mvp2_result.outcome == "expanded_explanation_repair":
            return self._defer_no_stub_repair(
                signal="mvp_expanded_explanation_repair",
                text=text,
                must_answer=user_message,
            )
        if mvp2_result.outcome == "stale_stub_retry_deferred_to_gate":
            self.last_debug.update(mvp2_result.last_debug_patch)
            self._set_final_answer_shape_debug("stale_stub_retry_deferred_to_gate")
            return mvp2_result.return_text
        self._set_final_answer_shape_debug(planner_answer_shape or "compact_direct")
        return mvp2_result.return_text

  Последние 2 строки — безусловные, конец метода (аналог "return text"
  в прошлом методе, но здесь возврат уже вычислен классификатором,
  self-вызов._set_final_answer_shape_debug остаётся на call site).

Шаг 4. Импорт после mvp_slice1.

Шаг 5. Runner run_prd_047_42_apply_30_mvp_slice2.py — та же архитектура.

Шаг 6. Тесты — все 9 значимых исходов (включая not_matched как
  терминальный default, с обоими под-случаями answer_obligation в
  множестве / не в множестве) + отдельный тест на ВЫЧИСЛЯЕМОЕ (не
  литеральное) значение answer_fit_repair_applied в группе P (два
  case: answer_fit={"concrete_need": True} и {"concrete_need": False},
  оба должны попасть в last_debug_patch с соответствующим bool, не
  захардкоженным True).

Шаг 7-9. Снапшоты, контрактные APPLY-6..29, no_mutation_proof (26
  защищённых + APPLY-20..29 логи).
```

---

## 3. Не делаем

```text
- НЕ хардкодим True для answer_fit_repair_applied в группе P — только
  вычисленное значение.
- НЕ переносим self._strip_optional_followup_invitation как self-вызов
  в этот helper — используем именно чистую модульную функцию, на
  которую он ссылается (см. Особенность 2), с явным обоснованием в
  документации helper'а.
- НЕ считаем этот импорт прецедентом "любой self-метод можно так
  импортировать" — каждый случай требует отдельной проверки чистоты.
```

---

## 4. Acceptance criteria

```text
ACCEPTED: снапшоты 17/17 byte-identical; тест на вычисляемое
  answer_fit_repair_applied проходит для обоих bool-значений; все
  контрактные APPLY-6..29 зелёные; канонический прогон — 19 падений,
  тот же список; 26 защищённых не тронуты; логи APPLY-20..29 целы;
  метод _enforce_mvp_free_dialogue_compliance полностью декомпозирован
  (0 инлайн-групп внутри тела метода, только 2 вызова классификаторов +
  диспетчеризация).
BLOCKED: answer_fit_repair_applied захардкожен как True; любой другой
  критерий по прецеденту APPLY-27/28/29.
```

---

## 5. Простыми словами

```text
Это последний кусок нового метода. После него оба самых больших метода
в файле бота будут полностью разобраны на понятные, тестируемые части.

Нашли один тонкий момент: в одном месте отладочная запись — не
"всегда true", а зависит от условия, это учтено отдельным тестом на
оба варианта.

Что делать сейчас: передать Исполнителю СНАЧАЛА APPLY-29, потом этот
файл. После приёмки обоих — напишите мне, я сверю оба независимо и
подведу итог по Сценарию А в целом.
```

---

## 6. Completion metadata

```text
Статус исполнения: COMPLETED_ACCEPTED.
Основной implementation commit: 8c0878b
```
