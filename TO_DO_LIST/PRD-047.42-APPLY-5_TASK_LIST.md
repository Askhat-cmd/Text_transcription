# PRD-047.42-APPLY-5 Task List

## Source Gate
- [x] Read `TO_DO_LIST/PRD.047_42_APPLY_5_writer_agent_slice4_v1_RU.md` fully.
- [x] Confirm target slice in `bot_psychologist/bot_agent/multiagent/agents/writer_agent.py`.
- [x] Confirm non-goals: no changes to `__init__`, `_resolve_model`, `_call_llm`, `_enforce_answer_compliance`, `_enforce_mvp_free_dialogue_compliance`, slice 1-3 writer helper modules, `writer_contract.py`, `admin_routes.py`, admin split modules, or diagnostic-center files.

## Baseline Evidence
- [x] Create `TO_DO_LIST/logs/PRD-047.42-APPLY-5/`.
- [x] Run baseline pytest set and save full output to `test_results_before.log`.
- [x] Capture baseline snapshot for the 4 required `write()` paths and save `write_path_snapshot_before.json`.
- [x] Record expected pre-existing failure set.

## Implementation
- [x] Add `bot_psychologist/bot_agent/multiagent/agents/writer_agent_lifecycle_mixin.py`.
- [x] Move `_resolve_runtime_settings` and `write()` into `WriterAgentLifecycleMixin`, 1:1.
- [x] Make `WriterAgent` inherit as `WriterAgent(WriterAgentLifecycleMixin, WriterAgentFallbackStateMixin)`.
- [x] Remove only the moved method bodies from `writer_agent.py`.
- [x] Add direct slice tests for lifecycle/write-path behavior without real provider calls.

## Verification
- [x] Run the same pytest baseline set plus new lifecycle tests and save full output to `test_results_after.log`.
- [x] Capture after snapshot for the same 4 `write()` paths and save `write_path_snapshot_after.json`.
- [x] Confirm before/after snapshot identity for all 4 paths.
- [x] Run `python -m py_compile` on touched python files.
- [x] Capture no-mutation proof for protected files via `git hash-object`.
- [x] Run `git diff --check`.

## Reports
- [x] Write `extraction_log.md` with explicit MRO order rationale.
- [x] Write `no_mutation_proof.md`.
- [x] Write `implementation_report.md`.
- [x] Write `next_recommendation.md`.

## Docs
- [x] Update `docs/MASTER_STRATEGIC_PLAN_NEO_MindBot_v4_RU.md` to v4.9 with the provided Part 4 text and journal line.
- [x] Update `docs/PROJECT_STATE.md`.
- [x] Update `docs/ROADMAP.md`.
- [x] Update `docs/PRD_INDEX.md`.
- [x] Update `docs/DECISIONS.md`.
- [x] Mark completion in `TO_DO_LIST/PRD.047_42_APPLY_5_writer_agent_slice4_v1_RU.md`.

## Delivery
- [x] Main commit and push to `main`.
- [x] Completion micro-push with final metadata/docs sync.
- [x] Verify `HEAD == origin/main` and clean working tree.
