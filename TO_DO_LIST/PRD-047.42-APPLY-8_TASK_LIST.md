# PRD-047.42-APPLY-8 Task List

## Scope
- [x] Fix only `test_variable_inventory_contains_expected_spine_variables` via frozen baseline from `e5f5f32`.
- [x] Keep `writer_agent.py`, `_call_llm` production slices, `writer_contract.py`, `admin_routes.py`, and the `10` admin modules out of scope.
- [x] Preserve default live behavior of `build_variable_inventory()` when `source_text` is not provided.

## Implementation
- [x] Add optional `source_text: str | None = None` to `_extract_call_llm_node()` in `TO_DO_LIST/tools/run_prd_047_42_apply_6_call_llm_boundary_mapping.py`.
- [x] Thread the same optional `source_text` through `build_variable_inventory()` without changing its current default path.
- [x] Generate a frozen JSON fixture from historical `writer_agent.py` at commit `e5f5f32` and store provenance in the file.
- [x] Rewrite only `test_variable_inventory_contains_expected_spine_variables` to read the frozen fixture instead of recalculating against live code.

## Tests And Evidence
- [x] Capture baseline run showing the single broken APPLY-6 contract test before the fix.
- [x] Capture baseline output of live `build_variable_inventory()` before edits for default-path compatibility proof.
- [x] Run `pytest bot_psychologist/tests/contract/test_prd_047_42_apply_6_call_llm_boundary_mapping.py`.
- [x] Run `pytest bot_psychologist/tests/contract/test_prd_047_42_apply_7_call_llm_slice1.py`.
- [x] Run the focused writer-agent regression subset used in APPLY-7/8 and confirm the same lone pre-existing fail `test_semantic_hits_limit_to_two`.
- [x] Re-run the live `build_variable_inventory()` snapshot after edits and prove it matches the pre-edit baseline.

## Reports
- [x] Write `TO_DO_LIST/logs/PRD-047.42-APPLY-8/implementation_report.md`.
- [x] Write `TO_DO_LIST/logs/PRD-047.42-APPLY-8/no_mutation_proof.md`.
- [x] Write `TO_DO_LIST/logs/PRD-047.42-APPLY-8/next_recommendation.md`.
- [x] Write `TO_DO_LIST/logs/PRD-047.42-APPLY-8/test_output.md`.

## Docs Sync
- [x] Update `docs/PROJECT_STATE.md`.
- [x] Update `docs/ROADMAP.md`.
- [x] Update `docs/PRD_INDEX.md`.
- [x] Update `docs/DECISIONS.md`.
- [x] Update completion metadata in `TO_DO_LIST/PRD-047.42-APPLY-8_Fix_Variable_Inventory_Test_Frozen_Baseline_v1_RU.md`.

## Delivery
- [x] Run `git diff --check`.
- [x] Commit implementation and push `main`.
- [x] Mark completion metadata and do the required micro-push.
- [x] Verify `HEAD == origin/main` and clean working tree.
