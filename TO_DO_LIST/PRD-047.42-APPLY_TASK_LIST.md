# PRD-047.42-APPLY Task List - admin_routes Decomposition v1

Date: 2026-07-09
Status: in_progress
Scope: Stage 2a only for `bot_psychologist/api/admin_routes.py`; preserve `main.py` import contract, route order, route behavior, and leave `writer_agent.py` / `writer_contract.py` untouched.

## 0. Source Gate
- [x] Read `TO_DO_LIST/PRD-047.42_APPLY_admin_routes_v1_RU.md`.
- [x] Read `TO_DO_LIST/logs/PRD-047.42/admin_routes_boundary_map.md`.
- [x] Inspect current git working tree before implementation.
- [x] Read current `bot_psychologist/api/admin_routes.py` and `bot_psychologist/api/main.py` in full.
- [x] Inspect existing admin API / runtime contract tests and fixture patterns for route mocking.
- [x] Inspect existing `TO_DO_LIST/tools/run_prd_*` runner style and place the new runner under `TO_DO_LIST/tools`, not repo-root `tools/`.

## 1. Hard Boundaries
- [x] Do not edit `writer_agent.py` or `writer_contract.py`; prove no mutation at the end.
- [x] Do not change `api/main.py` import line semantics: it must still import `admin_router` and `admin_router_v1` from `.admin_routes`.
- [x] Do not change any admin URL path, HTTP method, handler signature, or payload semantics.
- [x] Do not remove or "fix" `legacy_compat` fragments; only move them by boundary map.
- [x] Do not introduce new runtime paths, flags, DB changes, retrieval changes, or safety changes.

## 2. Baseline Evidence
- [x] Build a read-only baseline snapshot for every registered route in `admin_router` and `admin_router_v1`.
- [x] Reuse existing admin test fixture / mocking patterns instead of inventing a new route test harness.
- [x] Normalize unstable response fields only where necessary and document each normalization rule.
- [x] Save the complete baseline to `TO_DO_LIST/logs/PRD-047.42-APPLY/routes_snapshot_before.json`.

## 3. Modular Extraction
- [x] Create the 10 mapped modules under `bot_psychologist/api/`:
  - [x] `admin_runtime_compat.py`
  - [x] `admin_surface_bootstrap.py`
  - [x] `admin_surface_helpers.py`
  - [x] `admin_runtime_effective_payload.py`
  - [x] `admin_diagnostics_payload.py`
  - [x] `admin_config_schema.py`
  - [x] `admin_config_routes.py`
  - [x] `admin_prompt_routes.py`
  - [x] `admin_agent_ops_routes.py`
  - [x] `admin_misc_routes.py`
- [x] Move code by the accepted boundary map line ranges only.
- [x] Keep module extraction log with original line ranges, destination file, and commit hash per module.
- [x] Commit each module extraction separately as required by the PRD.

## 4. Thin Aggregator
- [x] Rewrite `bot_psychologist/api/admin_routes.py` into a thin aggregator that re-exports the same `admin_router` and `admin_router_v1`.
- [x] Preserve route registration order exactly.
- [x] Preserve import-time side effects and shared constants required by the moved handlers.
- [x] Confirm `api/main.py` requires no code changes.

## 5. Snapshot Coverage And Tests
- [x] Expand behavior snapshot coverage from the prior representative subset to all registered admin routes.
- [x] Add/adjust focused tests so every registered route is included in the snapshot contract path.
- [x] Run relevant subsets after each module extraction, not only at the end.
- [x] Save post-refactor snapshot to `TO_DO_LIST/logs/PRD-047.42-APPLY/routes_snapshot_after.json`.
- [x] Produce `TO_DO_LIST/logs/PRD-047.42-APPLY/routes_diff_report.md` with explicit before/after verdict.

## 6. Reports
- [x] Generate `TO_DO_LIST/logs/PRD-047.42-APPLY/module_extraction_log.md`.
- [x] Generate `TO_DO_LIST/logs/PRD-047.42-APPLY/external_contract_proof.md`.
- [x] Generate `TO_DO_LIST/logs/PRD-047.42-APPLY/no_mutation_proof.md`.
- [x] Generate `TO_DO_LIST/logs/PRD-047.42-APPLY/regression_before.log`.
- [x] Generate `TO_DO_LIST/logs/PRD-047.42-APPLY/regression_after.log`.
- [x] Generate `TO_DO_LIST/logs/PRD-047.42-APPLY/implementation_report.md`.
- [x] Generate `TO_DO_LIST/logs/PRD-047.42-APPLY/next_recommendation.md`.

## 7. Verification
- [x] Run the new runner: `TO_DO_LIST/tools/run_prd_047_42_apply_admin_routes.py`.
- [x] Run focused admin contract / snapshot tests.
- [x] Run regression before/after per PRD standard.
- [ ] Run live smoke before/after if the project protocol still expects it for this class of change.
- [ ] Run `git diff --check`.
- [x] Verify `writer_agent.py` and `writer_contract.py` stayed untouched.

## 8. Docs And Completion
- [ ] Update docs only if implementation changes architectural status or project state beyond the existing PRD cadence.
- [ ] Mark this task list complete with verdict and commit metadata.
- [ ] Mark completion in the PRD file as requested.
- [ ] Push the main implementation commits to `origin/main`.
- [ ] Make the required completion micro-commit and push it.
- [ ] Verify `HEAD == origin/main` and clean working tree.

## Notes
- Full route snapshot coverage reached `77/77` route cases (`48` legacy + `29` v1).
- Route diff verdict is `clean` with `0` response differences after decomposition.
- Focused post-refactor backend/admin suite currently passes `101` tests; the remaining UI string subset fails the same `5` assertions both before and after on the baseline worktree and is treated as pre-existing noise, not a PRD-local regression.
