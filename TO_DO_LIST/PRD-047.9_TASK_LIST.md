# PRD-047.9 TASK LIST

## 1. Source Gates
- [x] Verify `branch == main` and workspace status.
- [x] Confirm PRD-047.9 file exists and readable.
- [x] Save `TO_DO_LIST/logs/PRD-047.9/source_gates.json`.

## 2. Unified Dialogue Policy (Preset, Not Branch)
- [x] Extend `dialogue_policy.py` with `build_effective_dialogue_policy(...)`.
- [x] Enforce authority order: minimal safety > explicit user request > knowledge/concept need > writer freedom > planner/diagnostic advisory.
- [x] Represent profile presets (`safe_guided`, `mvp_free_dialogue`) via one policy surface.

## 3. Context Unclamp + Recency Preservation
- [x] Replace `conversation_context[:2000]` in writer prompt path.
- [x] Add recency-preserving formatter preserving latest turns and truncating older context with marker.
- [x] Add policy-aware context budget (`safe_guided` lower, `mvp_free_dialogue` higher).
- [x] Expose prompt-canvas context diagnostics (budget, truncation, preserved turns).

## 4. Knowledge/Concept Practice Overview Routing
- [x] Update `knowledge_answer_routing_guard` to detect concept-practice overview queries.
- [x] Route `"какие практики ..."` and related forms to `answer_type=practice_overview` and direct concept grounding.
- [x] Increase writer semantic hit budget in MVP profile (count and per-hit chars).

## 5. Planner/Active-Line Adaptive Behavior
- [x] Add planner support for `answer_practice_overview` + `practice_catalog_explanation`.
- [x] Prevent forced `one_step` for overview requests in MVP profile unless explicit one-step request.
- [x] Add stale active-line mitigation via dialogue policy/advisory authority.

## 6. Writer Constraint Conflict Softening
- [x] In MVP profile, soften `max_sentences/do_not_over_explain` when user requests explanation/overview/examples/practices.
- [x] Add visible `MVP FREE DIALOGUE OVERRIDES` block in writer user prompt.
- [x] Ensure practice overview outputs multiple directions (>=3) instead of one micro-step.

## 7. Admin Runtime / Web Visibility
- [x] Extend `/api/admin/runtime/effective` with clear effective `dialogue_policy` block.
- [x] Align `writer_freedom_contract` display with MVP high-autonomy effective mode.
- [x] Update Web Runtime tab fields and tests.

## 8. Dataset + Runner + Artifacts
- [x] Add `tests/evaluation/prd_047_9_mvp_context_unclamp_cases.json`.
- [x] Add `scripts/run_prd_047_9_mvp_context_unclamp_cases.py` (`dry/direct/live`).
- [x] Produce required logs and samples under `TO_DO_LIST/logs/PRD-047.9/`.

## 9. Tests
- [x] Add/update tests required by PRD-047.9.
- [x] Run targeted pytest set + `tests/evaluation`.
- [x] Record outputs into `TO_DO_LIST/logs/PRD-047.9/test_command_output.txt`.

## 10. Live Acceptance (Mandatory)
- [x] Start fresh backend on `127.0.0.1:8016`.
- [x] Run live runner and ensure `status=passed` (not blocked).

## 11. Reports + Docs
- [x] Create `TO_DO_LIST/reports/PRD-047.9_IMPLEMENTATION_REPORT.md`.
- [x] Create `TO_DO_LIST/reports/PRD-047.9_NEXT_PRD_RECOMMENDATION.md`.
- [x] Update required docs and add ADR-066 entry in decisions docs.

## 12. Delivery`r`n- [x] Commit main implementation and push to `main`.`r`n- [x] Mark completed metadata in task/report.`r`n- [x] Micro-commit + micro-push post-push metadata sync.


