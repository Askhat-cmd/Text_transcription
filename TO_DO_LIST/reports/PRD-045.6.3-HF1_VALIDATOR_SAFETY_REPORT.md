# PRD-045.6.3-HF1 VALIDATOR SAFETY REPORT

## Validator Policy
Updated `validate_turn_llm_summary(...)` now rejects `ready` summaries when content is unsafe:
- `diagnosis_language`
- `direct_advice_voice`
- `transcript_style_dump`
- `summary_too_long`
- `quote_too_long`

## Preserved Behavior
- `ready + valid + hash-match` still passes.
- `pending/failed/hash_mismatch` still rejected for context usage.
- Optional semantic fields (`open_loop`, `important_quote`, `assistant_move`) remain optional.

## Test Evidence
- `test_turn_llm_summary_contract.py` includes explicit guard checks.
- `test_turn_llm_summary_context_assembly.py` includes unsafe summary fallback check.
- All targeted tests passed.
