# PRD-046.0.3-HF1 BotDB Health Audit

## Probe Timestamp
- Date: `2026-05-09`
- Runtime host: `127.0.0.1:8003`

## Endpoint Checks
1. `GET /api/status/`
- Result: `200`
- Body summary: `{"jobs":[]}`

2. `GET /api/registry/`
- Result: `200`
- Registry includes processed source with blocks (`Кузница Духа`).

3. `POST /api/query/`
- Result: `503`
- Body: `{"detail":"ChromaDB unavailable"}`

## Local Fallback Data Availability
- `ALL_BLOCKS_MERGED_PATH`: `C:/My_practice/Text_transcription/Bot_data_base/data/processed/all_blocks_merged.json`
- Exists: `yes`
- Parsed blocks count: `229`

## Runtime Conclusion
- BotDB API layer is reachable, but query path depends on unavailable Chroma runtime/indexing.
- Main bot can and should rely on local semantic/TF-IDF fallback while BotDB query path is unhealthy.
- HF1 circuit-breaker behavior is required and implemented to avoid repeated slow API waits.

## Follow-up Recommendation
- Separate PRD for Bot_data_base recovery:
1. Restore Chroma collection/index consistency (dimension mismatch appeared in registry history).
2. Verify embedding model dimensionality alignment for all ingestion jobs.
3. Re-run indexing and validate `/api/query/` returns `200` with chunks.
