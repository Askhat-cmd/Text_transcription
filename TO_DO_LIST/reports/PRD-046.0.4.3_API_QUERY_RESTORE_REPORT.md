# PRD-046.0.4.3 API Query Restore Report

## Scope
- Цель: подтвердить рабочий retrieval path через `/api/status`, `/api/registry`, `/api/query` после governed reindex.
- Безопасность: в артефактах сохраняются только sanitized previews (без `content_full`/raw full chunk text).

## Environment Notes
- Базовый порт `8003` в текущем локальном окружении периодически уходил в timeout.
- Для детерминированного smoke поднят чистый локальный экземпляр BotDB API на `http://127.0.0.1:8005`.
- Артефакты API smoke записаны по результатам `8005`.

## Endpoint Results (8005)
- `/api/status`: `200`
- `/api/registry`: `200`
- `/api/query`: `200` для всех 6/6 smoke-запросов
- `hits_count > 0`: `6/6`

## Metadata Contract Check
- В top hits присутствуют governance поля:
- `chunk_type`
- `allowed_use`
- `safety_flags`
- `lens_family`
- `not_for_direct_quote`
- `source_style_not_user_facing`
- Дополнительно присутствует `mixed_intent_severity` (через `chunking_quality` mapping на API-стороне).

## Safety / Redaction Check
- В `api_query_smoke_snapshot.json` отсутствуют `content_full` и raw full text dump.
- Сохраняется только `content_preview` (<=160 символов).

## Artifacts
- `TO_DO_LIST/logs/PRD-046.0.4.3/api_status_snapshot.json`
- `TO_DO_LIST/logs/PRD-046.0.4.3/api_registry_snapshot.json`
- `TO_DO_LIST/logs/PRD-046.0.4.3/api_query_smoke_snapshot.json`

## Conclusion
- API retrieval path восстановлен и подтверждён на live smoke (`8005`).
- Ограничение среды: старый локальный процесс на `8003` нестабилен (timeouts), не блокирует принятие PRD как инфраструктурного восстановления reindex+API contract.
