# PRD-046.0.4.3 Next PRD Recommendation

## Recommended Next PRD
- `PRD-046.0.5 — Offline LLM Summary + Lens Enrichment v1`

## Why
- Governed gate: `ready`.
- Reindex: completed from governed `all_blocks_merged.json` with `indexed_blocks_count=229` and `collection_count_after=229`.
- API query restore smoke: `200` and positive hits for 6/6 queries.
- Bot retrieval path smoke: API path active, fallback not used.

## Known Environment Limitation
- Локальный API процесс на `8003` показывал timeout/нестабильность; smoke подтверждён на чистом экземпляре `8005`.
- Это operational issue окружения запуска, а не blocker governed retrieval contract.

## If HF Needed
- Если требуется жёстко стабилизировать именно порт `8003` (service watchdog/process hygiene), выделить отдельный hotfix:
- `PRD-046.0.4.3-HF1 — BotDB API runtime process stabilization`.
