# PRD-046.0.6 NEXT PRD RECOMMENDATION

## Decision
Recommended next PRD: **`PRD-046.0.6-HF1 — Retrieval Governance Safety Fix v1`**

## Why
- Retrieval quality threshold met (`top5_semantic_match_rate=1.0`).
- Governance coverage threshold met (`top5_governance_present_rate=1.0`).
- API stability threshold met (`queries_failed=0`).
- Raw leak threshold met (`raw_full_text_leak_detected=false`).
- **Safety threshold failed**: `internal_only_unsafe_exposure_count=4` (expected `0`).

## HF1 Focus
1. Tighten retrieval policy/ranking so `internal_only` hits do not surface in non-safety categories.
2. Preserve existing governance metadata pass-through and no raw-leak guarantees.
3. Re-run PRD-046.0.6 eval dataset unchanged to verify gate closure.

## Deferred
- `PRD-046.0.7 Admin Review Workflow v1` should start only after HF1 closes `internal_only` safety leakage.
