# PRD-046.0.6 WEAK CASES REPORT

## Summary
- Weak cases total: `3`
- Weak case ids: `KR-002`, `KR-013`, `KR-022`
- Dominant failure mode: `internal_only` exposure in non-safety categories

## Weak Cases
1. `KR-002` (`self_criticism_achievement`)
- Query: "если я не лучший, я будто никто"
- Fail reasons:
  - `disallowed_allowed_use_present`
  - `internal_only_exposure_non_safety_case`
- Top-hit note: среди top-k обнаружен `chunk_type=safety` с `allowed_use=["internal_only","safety_protocol"]`.
- Recommendation: retrieval calibration needed (filter/penalty for `internal_only` outside safety boundary contexts).

2. `KR-013` (`body_awareness_freeze`)
- Query: "я замираю и будто не чувствую тело"
- Fail reasons:
  - `disallowed_allowed_use_present`
  - `internal_only_exposure_non_safety_case`
- Top-hit note: в top-k присутствует hit с `internal_only`.
- Recommendation: metadata-aware reranking for non-safety categories.

3. `KR-022` (`internal_style_not_quote`)
- Query: "дай идею как поддержать человека, но без прямых цитат источника"
- Fail reasons:
  - `disallowed_allowed_use_present`
  - `internal_only_exposure_non_safety_case`
- Top-hit note: top-k содержит mixed `practice/lens` + internal-only safety chunk.
- Recommendation: tighten non-safety retrieval policy for style/use-only queries.

## Proposed Queue for HF
- Governance safety calibration in retrieval ranking policy.
- Add targeted eval assertions for `must_not_allowed_use=["internal_only"]` in non-safety categories.
