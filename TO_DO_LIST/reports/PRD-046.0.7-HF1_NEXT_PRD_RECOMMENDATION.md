# PRD-046.0.7-HF1 NEXT PRD RECOMMENDATION

## Decision
Recommended next PRD: **`PRD-046.0.7-HF2 - Source Hygiene Blocker Fix v1`**

## Why
- Legacy SD decommission в UI/API выполнен: `legacy_sd_filter_still_active=false`.
- Hygiene workflow и guarded dry-run готовы (`registry_mutated=false`, focus source protected).
- Но readiness gate вернул `not_ready` из-за blocker:
  - `multiple_active_sources_without_allowlist`.

## HF2 Scope
1. Закрыть blocker по множественным active sources через controlled archive/apply шаг.
2. Сохранить защиту `Кузницы` (никаких archive/delete для focus source).
3. Не запускать reprocess и не делать Chroma reset/reindex в HF2.
4. Повторно прогнать readiness gate до статуса `ready` или `degraded` без критических blockers.

## After HF2
- Если gate станет `ready`: переход к `PRD-046.0.8 - Clean Source Reprocess from Single Active Source v1`.
- Если останутся blockers: продолжить HF2-fix итерацию, без перехода к reprocess.
