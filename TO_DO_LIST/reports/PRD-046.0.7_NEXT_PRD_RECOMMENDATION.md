# PRD-046.0.7 NEXT PRD RECOMMENDATION

## Decision
Recommended next PRD: **`PRD-046.0.7.1 - Controlled Review Decision Apply v1`**

## Why
- Review queue workflow реализован и стабилен: `review_items_count=87`, детерминированные `priority/recommended_action`.
- Safety-гейты workflow пройдены: `raw_full_text_leak_detected=false`, `forbidden_key_hits=0`, `secret_like_hits=0`.
- No-mutation proof подтверждён: `input_file_sha256_before == input_file_sha256_after`, `source_mutated=false`.
- Decision overlay уже валидируется отдельным контрактом, но **ещё не применяется** к production KB (и это правильно для текущего шага).

## Scope For PRD-046.0.7.1
1. Читать только валидированный decisions overlay.
2. Применять только advisory enrichment-поля (`summary/lens/tags/use_when/avoid_when/...`).
3. Не менять governance authority (`text/chunk_type/allowed_use/safety_flags/...`).
4. Добавить preflight invariants + dry-run/apply режим.
5. Отдельно фиксировать post-apply eval/smoke.

## Deferred
- Diagnostic Center rollout остаётся deferred до controlled apply цикла и его acceptance.
