# PRD-046.1.19 Runtime Pilot Readiness Report

## Что готово
- Подготовлен полный plan-only readiness контур для controlled runtime pilot без execution.
- Зафиксированы обязательные gates:
  - source gate от PRD-046.1.18;
  - pilot scope;
  - cohort policy;
  - toggle matrix;
  - runtime preflight requirements;
  - limited live smoke plan;
  - rollback-first runbook;
  - hard-stop criteria;
  - monitoring artifact contract;
  - normal-user / KB-governance / trace-sanitization guards.

## Что не запускалось
- Runtime pilot не запускался.
- Live smoke не запускался.
- Provider calls не выполнялись.
- Runtime activation не выполнялась.

## Preflight условия перед execution PRD
- чистое git-дерево перед запуском;
- синхронизированные PROJECT_STATE/ROADMAP/PRD_INDEX;
- подтверждённый pass PRD-046.1.18;
- conservative defaults по флагам;
- план runtime reachability (`:8003`) для live smoke;
- подтверждение focus source/chroma consistency;
- готовые normal-user controls и rollback-first путь.

## Разрешённый cohort
- максимально консервативный старт: `max_initial_cohort_size=1`;
- allowlist synthetic operator id: `pilot_runtime_operator_001`;
- обязательные normal-user control cases: `>=2`;
- normal-user apply запрещён.

## Флаги
- defaults: `ENABLED=false`, `FORCE_DISABLED=true`, `MODE=shadow`.
- planned execution state фиксирован в toggle matrix, но не применялся.
- rollback/hard-stop state фиксирован и требует возврата в `FORCE_DISABLED=true`.

## Rollback
- rollback-first обязателен до и после execution.
- stale apply после force-disabled является blocker.
- normal-user apply после rollback является blocker.

## Hard stops
- normal-user path change;
- KB quote/authority leakage;
- safety regressions;
- trace sanitization violations;
- rollback failure;
- production mutation.

## Почему broad rollout запрещён
- Этот PRD — только runway для будущего controlled execution.
- Любой runtime execution разрешается только отдельным PRD после preflight и с rollback-first governance.
