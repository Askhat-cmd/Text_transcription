# PRD-047.16 Next PRD Recommendation

- Recommended next PRD: `PRD-047.17 - Offline Summary/Lens/Use-When Enrichment over Mechanism Metadata v1`

Reason:

- `PRD-047.16` established the metadata contract, compatibility adapter, and read-only audit layer.
- Real local blocks are available and can already be normalized without runtime mutation.
- Audit found quality warnings rather than schema blockers, so the next leverage point is offline enrichment over:
  - `mechanism_hints`
  - `use_when`
  - `avoid_when`
  - `contraindications`
  - `safe_user_translation`

Do not do in `PRD-047.17`:

- no live Writer-path activation of the new metadata;
- no Chroma reindex inside the same step unless a separate PRD explicitly accepts it;
- no deterministic routing from metadata into final-answer authority.
