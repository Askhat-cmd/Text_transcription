# PRD-047.17 Next PRD Recommendation

Recommended next step:

`PRD-047.18 - Manual Review / Curated Candidate Acceptance Workflow v1`

Why this path is next:

- `PRD-047.17` already produced `80` real-source enrichment candidates with full source trace, risk levels, and manual-review reasons.
- Candidates are intentionally `candidate_only`; the missing step is not more generation, but governed acceptance/rejection workflow.
- The dominant residual gap is curated review of:
  - `missing_summary`
  - `missing_mechanism_hints`
  - `practice_current_metadata_missing_contraindications`
  - `source_fragment_requires_derived_candidate_review`
- Runtime activation, Writer-path mutation, and Chroma changes remain explicitly out of scope until accepted candidate subsets are curated.

Alternative later path after review:

- `PRD-047.18 - Mechanism-Aware Retrieval Eval Set v1`

Do not do next:

- do not apply candidates directly to live metadata;
- do not activate LLM enrichment in runtime;
- do not reindex Chroma as part of candidate generation alone.
