# PRD-047.22 Next PRD Recommendation

Recommended next step: `PRD-047.23 - Overlay + Writer KB Payload Live Evidence / Evaluation v1`

Rationale:
- selected retrieval hits now reach Writer as structured KB cards instead of blind truncated snippets;
- neurostalking regression is closed with sentence/paragraph-aware excerpting;
- trace and Web UI now expose `writer_kb_payload_trace` and `future_graduation_notes` read-only;
- overlay metadata remains non-authoritative and disabled by default, so live evidence can now isolate payload quality instead of transport corruption.

Not recommended yet:
- live metadata apply;
- Chroma reindex;
- retrieval-ranking changes;
- unified runtime consolidation beyond evidence collection.
