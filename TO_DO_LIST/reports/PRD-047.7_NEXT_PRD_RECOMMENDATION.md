# PRD-047.7 NEXT PRD RECOMMENDATION

## Recommendation
- Option D: `No-op / start real guided session`.

## Rationale
- Protocol infrastructure is implemented and tested.
- Scenario set and feedback capture loop are ready.
- Sanitized summary artifacts are generated correctly.
- No blocking runtime mutations were introduced.

## Decision Rule for PRD-047.8
Start real owner-guided sessions first. After sufficient real feedback:
1. Use `PRD-047.8 Live Tone Calibration v1` if feedback shows too rigid/too generic/not alive enough.
2. Use `PRD-047.8 Planner Scenario Calibration v2` if planner move mismatches dominate.
3. Use `PRD-047.8 Web Admin Feedback UI v1` if CLI workflow is operationally inconvenient.
