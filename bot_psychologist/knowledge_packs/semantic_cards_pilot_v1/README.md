# Semantic Cards Pilot v1

This pilot pack contains a small curated set of advisory semantic chunk cards for PRD-047.27.

The cards are not a new knowledge authority, not a routing rule engine, and not a replacement for retrieved KB chunks. They are optional Writer KB payload support items used only when `SEMANTIC_CARDS_PILOT_ENABLED=true` in local/dev/test runtime.

