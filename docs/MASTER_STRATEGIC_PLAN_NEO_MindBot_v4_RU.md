# МАСТЕР-ПЛАН РАЗВИТИЯ — Neo MindBot v4

**Проект:** Bot Psychologist / Neo MindBot
**Репозиторий:** `Askhat-cmd/Text_transcription`
**Локальный путь владельца:** `C:\My_practice\Text_transcription`
**Дата:** 2026-07-09
**Версия:** v4.23 — единый мастер-план (14 правок MP-1..MP-14 по аудиту Fable R08
+ обновление курса после PRD-047.42-APPLY-14 + красная линия против случайного
 перезапуска PRD-инструментов; исправление — Часть 4 была на версии после
 APPLY-8, актуализирована до APPLY-10)
**Что это:** самодостаточный документ, по которому любой архитектор в новом
чате, на любом этапе, может продолжить ведение проекта без дополнительного
контекста. Сшивает три ранее раздельных источника в одну временную ось:
STRATEGIC_PLAN_v3 (консолидация), FABLE_LAW_INDEX (корпус законов),
FABLE_STRATEGIC_RESPONSE_07 (5 тактов эры "мозгов").

**Отношение к предыдущим документам:**
```text
STRATEGIC_PLAN_v1 (Thin Spine)      -> историч.; принципы живут (см. §2).
STRATEGIC_PLAN_v2 (Product/Retrieval)-> выполнен; product-инварианты в силе.
STRATEGIC_PLAN_v3 (Consolidation)   -> поглощён этим документом (Эпоха 1).
FABLE_LAW_INDEX + RESPONSE_01..07   -> поглощены этим документом (Эпоха 2).
RATIFICATION_FABLE_LAW_CORPUS_v1    -> живёт отдельно, это рабочий чек-лист
                                       владельца, ссылается отсюда (§9).
```

---

# ЧАСТЬ 0 — КАК ЧИТАТЬ ЭТОТ ДОКУМЕНТ

Новый архитектор, открывший проект впервые, читает по порядку: §1 (что за
проект), §2 (незыблемые принципы), §3 (роли и как устроена работа), §4
(где мы сейчас — единственное место, которое устаревает и обновляется),
затем ту Эпоху, в которой находится проект.

Проект движется через **две эпохи**, строго последовательно:

```text
ЭПОХА 1 — Консолидация (архитектурная чистота). Идёт сейчас.
          Цель: единая система без исторического мусора.
          Механизм: линейная серия PRD с жёстким Definition of Done.

ЭПОХА 2 — "Мозги" (интеллект бота). Не начата.
          Цель: retrieval/восприятие/голос по законам Fable-корпуса.
          Механизм: 5 тактов, стартует только после закрытия Эпохи 1
          И ратификации корпуса законов владельцем.
```

Между эпохами — **шлюз** (§8): два условия, оба обязательны, оба проверяемы.
Ни один архитектор не имеет права начать Эпоху 2, пока шлюз не пройден.

Слово **"пилот"** в этом проекте означает **внутреннюю готовность системы**
(целостность, чистая архитектура, модульность, документация), а НЕ запуск
на внешних людях. Единственный пользователь — владелец-разработчик, и это
надолго по сознательному решению. Safety, работа с реальными пользователями,
продвижение — отдельная, ещё не спланированная фаза за пределами обеих эпох.

---

# ЧАСТЬ 1 — ЧТО ЗА ПРОЕКТ

Neo MindBot — русскоязычный психологический бот-помощник с pipeline-
архитектурой поверх governed RAG:

```text
User turn -> State Analyzer -> Thread Manager -> Memory Retrieval
-> Context Assembly -> Diagnostic Card -> Writer Move Compliance
-> Writer -> Validator -> Memory Update
```

Скрытая философия бота (первоисточник, он же БД): корпус "Кузница Духа" и
"chuvstvo_do_slova". Идея — человек не сломан, программа не есть он сам,
страдание часто защитный механизм, практика — приглашение, не плеть. Эта
оптика должна жить как мягкая суть Writer'а, но по закону (см. Эпоха 2,
такт 3в), а не как отдельный слой поверх. Суждение об оптике корпуса и
утверждение идентичности Writer'а — не делегируемая человеческая точка
владельца (вердикт R05 §6 / R06); механическая пересборка идентичности в
такте 3(в) выполняется Executor'ом под этим суждением. [Если владелец решает
писать текст идентичности лично — это его решение сверх вердикта,
зафиксировать при ратификации.]

История: 755+ коммитов, несколько сотен PRD. Проект прошёл product-стадию
(latest-turn authority, Writer как финальный автор, БД как скрытая
компетенция) и вышел на frozen baseline (PRD-047.37). Известная боль
владельца — переусложнение через сотни PRD; выход из него и есть Эпоха 1.

---

# ЧАСТЬ 2 — НЕЗЫБЛЕМЫЕ ПРИНЦИПЫ (действуют в обеих эпохах)

```text
Из product-эпохи (v1/v2), не пересматриваются:
- Writer — финальный автор ответа; всё остальное advisory.
- Latest turn authority: последняя реплика человека — главный target.
- Скрытая компетенция: БД/чанки/карты никогда не user-facing.
- Evidence before refactor; simplify after proof; delete after replacement;
  document after deletion.
- Trace наблюдает, но не командует.
- Никакого rule engine / словарей терминов / marker-routing как решения.
- Каждый PRD маленький: source gate, non-goals, tests, no-mutation proof,
  clean commit/push, HEAD == origin/main.

Стоящая конвенция (введена 2026-07-03, не PRD):
- Запрещено создавать новые постоянные permanent_quality_eval_regression-
  модули (паттерн diagnostic_center_*). Новые регресс-проверки идут в один
  лёгкий общий набор. 60 существующих замороженных файлов не трогаются.

Стоящая конвенция (введена 2026-07-09, не PRD):
- Инструменты TO_DO_LIST/tools/run_prd_*.py нельзя запускать "просто
  проверить, что работает". Эти скрипты не read-only — при запуске они
  перезаписывают свои же готовые отчёты состоянием "работа только
  началась" (пример: 2026-07-09, run_prd_047_42_god_file_boundary_mapping.py
  дважды случайно перезапускался вне контекста реальной задачи и откатывал
  уже принятые PRD-047.42 отчёты до pending-состояния локально, без
  коммита — дважды поймано и откачено через git checkout, реальных
  повреждений не было, но время архитектора/владельца тратилось на
  разбор ложных тревог). Запускать эти скрипты можно только как часть
  исполнения реального PRD, которому они принадлежат.
```

---

# ЧАСТЬ 3 — РОЛИ И МЕХАНИКА РАБОТЫ

```text
Architect (chat-сессия, Claude/др.) — читает evidence (коммиты, отчёты,
    trace), решает следующий PRD, пишет PRD-документы, обновляет §4.
Executor (IDE-агент, Claude Code)   — читает PRD целиком, создаёт TASK_LIST,
    реализует, тестирует, коммитит, пишет отчёты в TO_DO_LIST/logs/PRD-XXX/.
Owner (владелец)                    — приносит Architect'у evidence после
    цикла, утверждает деструктивные шаги, ратифицирует законы Эпохи 2.
```

**Двусторонняя связь Architect <-> Executor** держится на трёх артефактах,
все в git: `docs/PROJECT_STATE.md`, `docs/PRD_INDEX.md`, и `.md`-отчёты в
`TO_DO_LIST/logs/PRD-XXX/`. Сырые артефакты (raw json, screenshots,
live_turn_exports) — НЕ в git (untracked с PRD-047.39; новые кладутся в
`TO_DO_LIST/logs/PRD-XXX/raw/`, эта подпапка в .gitignore). Architect
восстанавливает контекст из PROJECT_STATE + отчётов, не из сырья.

**Evidence hierarchy — что приносить Architect'у в начале сессии:**
```text
1. Commit hash после PRD (HEAD == origin/main, clean tree).
2. Путь к TO_DO_LIST/logs/PRD-XXX/ и список реально появившихся файлов.
3. next_recommendation.md последнего PRD.
4. Любые BLOCKER/WARNING из acceptance decision.
5. Для Эпохи 1 Phase B — diff по затронутым файлам.
```
Architect не принимает "всё прошло" на слово — только через артефакты.

---

# ЧАСТЬ 4 — ГДЕ МЫ СЕЙЧАС  ← ЕДИНСТВЕННЫЙ ОБНОВЛЯЕМЫЙ РАЗДЕЛ

Эпоха: 1 (Консолидация).
Последний принятый PRD: PRD-047.42-APPLY-19 (ACCEPTED, commits
  4d922282 / 7f344967; обновление плана до v4.22 — 1312399b).
  Верифицировано архитектором независимо через GitHub: пересчёт
  snapshot из живого кода (совпадение по всем полям, кроме служебной
  метки generated_at_utc — см. хроническое замечание ниже), SHA1
  user_prompt по всем трём сценариям (цепочка не менялась с APPLY-15),
  duration_ms=123 детерминирован в обоих snapshot'ах, 134 passed,
  ровно 2 обращения к perf_counter на поток, канонический широкий
  прогон — ровно 19 падений, список идентичен эталону, журнал цел.

🏁 МЕТОД _call_llm СЧИТАЕТСЯ ГОТОВЫМ. Итог всей линии APPLY-6..19:
  boundary mapping + 12 слайсов (slice1..slice12) + владелец-решение #3
  (await остаётся в методе). В методе остались: вызовы слайсов,
  format()-сборка из готовых полей, core_required_fields, чистые
  пробросы, единственный await и return. writer_agent.py = 1451 строка
  (было 1543 на старте серии слайсов 9-12, ~2100+ на старте линии).

Следующий PRD: PRD-047.42-APPLY-20 — BOUNDARY MAPPING
  _enforce_answer_compliance (строки 582-1191, 610 строк) по образцу
  APPLY-6: БЕЗ переноса кода, 0-diff по writer_agent.py. Результат —
  карта кластеров + детерминированный snapshot-харнесс для метода +
  журнал покрытия правил. Метод — каскад из ~40 последовательных
  if-правил, мутирующих text; это другой тип кода, чем _call_llm,
  картирование строго обязательно до любого переноса.
  Затем: инкрементальная декомпозиция по принятой карте; после —
  _enforce_mvp_free_dialogue_compliance (225 строк, строки 1192+).
Шлюз в Эпоху 2: НЕ пройден. Работаем по Сценарию А
  (владелец-решение #2 от 2026-07-14).

Корпус законов Fable: получен полностью (R01-R07), НЕ ратифицирован.

ВЛАДЕЛЕЦ-РЕШЕНИЕ (2026-07-13, действует): 4 файла — thread_manager.py
  (1482), orchestrator.py (1481), api/debug_routes.py (1380),
  writer_context_package.py (1376) — превышают порог DoD §5.6
  (~1200 строк), но их декомпозиция сознательно отложена владельцем на
  ОТДЕЛЬНЫЙ трек ПОСЛЕ реализации Эпохи 2 "умности" — не пересматривать
  без явного нового решения владельца.

ВЛАДЕЛЕЦ-РЕШЕНИЕ #2 (2026-07-14, действует): цель текущей рабочей
  линии — Сценарий А. _call_llm готов; остаток линии — картирование и
  декомпозиция _enforce_answer_compliance и
  _enforce_mvp_free_dialogue_compliance. Буквальный DoD §5.6 в конце
  линии закрыт НЕ будет — расхождение сознательное, не blocker.

ВЛАДЕЛЕЦ-РЕШЕНИЕ #3 (2026-07-16, исполнено в APPLY-19):
  provider_dispatch (одиночный await create_agent_completion) не
  выносился — остался в _call_llm. Кластер закрыт решением
  "остаётся на месте".

PRD-047.42-APPLY-19 закрыт (Stage 3c, последний кластер _call_llm):
- кластер response_unpack_cost_and_return (25 строк, границы 569-593)
  вынесен в writer_agent_call_llm_slice12.py; await и return остались
  в методе (владелец-решение #3);
- тайминговая дисциплина соблюдена: import time + time.perf_counter()
  (подмена атрибута общего модуля работает), ровно 2 вызова
  perf_counter на поток, duration_ms=123 детерминирован; в прямых
  тестах есть ловушка на from-import (monkeypatch атрибута модуля);
- вторая self-зависимость (_estimate_cost из защищённого
  fallback_state_mixin) передана callable'ом, mixin не тронут
  (hash-proof подтверждён архитектором);
- KeyError-семантика runtime_settings["..."] сохранена (без .get());
- ХРОНИЧЕСКОЕ ЗАМЕЧАНИЕ (3-й случай: APPLY-15, APPLY-16, APPLY-19):
  метка generated_at_utc в закоммиченных snapshot'ах отстаёт от
  константы NORMALIZED_TIMESTAMP runner'а — snapshot'ы генерируются
  до финализации константы. Содержательно payload каждый раз
  идентичен (подтверждается независимым пересчётом архитектора),
  НЕ blocker. Гигиеническое правило для будущих PRD: перегенерировать
  оба snapshot'а ПОСЛЕ установки финального NORMALIZED_TIMESTAMP —
  зашито в план APPLY-20.

Защищённые файлы (канонический список архитектора) — 17:
  14 из transfer brief v3
  + slice9  (f69c7217137ddc23ea1c09e7ba6dd9a1886d0249)
  + slice10 (dd34d823f61c608c3bd8a2be0fd48763cec2c838)
  + slice11 (fa776248a89124c8b34d7d94134a448bee9a3cdc).
  После приёмки APPLY-20 (mapping, 0-diff): список не растёт;
  slice12 (hash зафиксировать из HEAD 7f344967) добавить = 18.

Отложено сознательно (не потеряно):
- core_required_fields (10 обязательных ctx[...] без дефолтов) — вне
  серии, отдельное решение архитектора.
- Декомпозиция _enforce_answer_compliance — ПОСЛЕ приёмки карты
  APPLY-20, по карте.
- _enforce_mvp_free_dialogue_compliance (225 строк) — после.
- thread_manager.py, orchestrator.py, api/debug_routes.py,
  writer_context_package.py — отдельный трек, после Эпохи 2.
- writer_contract.py (979 строк) — под порогом ~1200, отдельное решение.
- diagnostic_center_* файлов (по факту не менее 27, docs говорят 19) ->
  PRD-047.42b, инвентаризация/классификация dead_confirmed.
- LEGACY_PIPELINE_ENABLED -> PRD-047.43.
- ~19-128 collection/environment-зависимых падений -> PRD-047.45.
- docs/testing.md, S7, Panic/Medical Escalation, трейс-поля — без изменений.

Журнал правок (новая строка поверх существующих):

- 2026-07-16 v4.22 -> v4.23: PRD-047.42-APPLY-19 принят ACCEPTED —
  МЕТОД _call_llm ГОТОВ (итог линии APPLY-6..19: карта + 12 слайсов +
  владелец-решение #3 об await). Тайминговая дисциплина perf_counter
  соблюдена, duration_ms детерминирован. Зафиксировано хроническое
  замечание о метке generated_at_utc (3-й случай, не blocker) и
  гигиеническое правило перегенерации snapshot'ов. Следующий шаг —
  APPLY-20: boundary mapping _enforce_answer_compliance (610 строк,
  0-diff картирование по образцу APPLY-6).

- 2026-07-16 v4.21 -> v4.22: PRD-047.42-APPLY-18 принят ACCEPTED
  (runtime_settings_and_system_prompt -> slice11; третий случай
  namesake dialogue_profile и первая self-зависимость решены чисто).
  ВЛАДЕЛЕЦ-РЕШЕНИЕ #3: provider_dispatch (одиночный await) НЕ
  выносится — остаётся в _call_llm; вместо двух PRD один финальный
  APPLY-19 (response_unpack_cost_and_return -> slice12). После его
  приёмки _call_llm считается готовым.

- 2026-07-16 v4.20 -> v4.21: PRD-047.42-APPLY-17 принят ACCEPTED.
  Первый пост-format кластер (prompt_constraint_append_and_debug_
  bookkeeping -> slice10, 48 строк). Сохранены дописывание user_prompt
  и асимметрия is-not-None/isinstance; порядок 18 ключей last_debug
  зафиксирован тестом (влияет на байты snapshot). Закрыта дыра
  покрытия: ветка дописывания не задевается snapshot-харнессом —
  добавлены прямые тесты обеих веток. Осталось 3 кластера _call_llm;
  следующий — runtime_settings_and_system_prompt_selection (APPLY-18).

- 2026-07-16 v4.19 -> v4.20: PRD-047.42-APPLY-16 принят ACCEPTED.
  Шестой, последний срез — СЕРИЯ ДЕКОМПОЗИЦИИ WRITER_USER_TEMPLATE.
  format() ЗАКРЫТА ПОЛНОСТЬЮ (slice4..slice9, ~176 kwargs). Зеркальная
  namesake-ловушка dialogue_profile сохранена корректно (локальная
  переменная параметром — противоположно APPLY-15). Попутно: обнаружена
  и восстановлена (34b1d5ce) запись журнала v4.17 -> v4.18, случайно
  стёртая при обновлении до v4.19; в процедуру передачи добавлен
  обязательный финальный diff-контроль журнала. Следующий шаг —
  кластер prompt_constraint_append_and_debug_bookkeeping.

- 2026-07-13 v4.18 -> v4.19: PRD-047.42-APPLY-15 принят ACCEPTED.
  Пятый срез серии decomposition WRITER_USER_TEMPLATE.format() — 30
  аргументов. Обнаружена и сохранена namesake-ловушка (dialogue_profile
  kwarg = независимый ctx.get(), не ссылка на одноимённую переменную).
  После следующего PRD (APPLY-16) вся серия декомпозиции
  WRITER_USER_TEMPLATE.format() будет закрыта.


- 2026-07-13 v4.17 -> v4.18: PRD-047.42-APPLY-14 принят ACCEPTED.
  Четвёртый срез серии decomposition WRITER_USER_TEMPLATE.format() —
  25 аргументов, все чистые ctx.get() без исключений-пробросов.
  Двухшаговая инструкция передачи Части 4 Исполнителю (текст отдельно,
  строка журнала отдельно) сработала корректно — история журнала цела.


- 2026-07-13 v4.16 -> v4.17: PRD-047.42-APPLY-13 принят ACCEPTED.
  Третий, самый простой срез серии decomposition WRITER_USER_TEMPLATE.
  format() — впервые все аргументы кластера не зависят от внешних
  локальных переменных. Владелец принял явное решение отложить
  декомпозицию 4 вновь найденных god-файлов (thread_manager.py,
  orchestrator.py, debug_routes.py, writer_context_package.py) до
  после реализации Эпохи 2 "умности" — сознательное отступление от
  буквального порядка шлюза, зафиксировано как приоритет владельца.

2026-07-13 v4.15 -> v4.16: PRD-047.42-APPLY-12 принят ACCEPTED. Второй срез серии декомпозиции WRITER_USER_TEMPLATE.format(). Архитектор заранее выявил 4 kwarg без вычислений (чистый проброс) и прямо указал, что их не нужно выносить в датакласс — экономия лишнего кода без потери строгости. SHA1 user_prompt подтвержден независимо, совпадает с предыдущим срезом (контент промпта не менялся уже дважды подряд).
- 2026-07-13 v4.14 -> v4.15: PRD-047.42-APPLY-11 принят ПРИНЯТ — первый срез новой серии (декомпозиция WRITER_USER_TEMPLATE.format()). Новая механика нарезки (датакласс внутри одного вызова, а не замена последовательности операторов) подтверждена работоспособной: вынесено 39 аргументов, user_prompt и last_debug идентичны побайтово, проверено архитектором независимо (SHA1 по всем трем сценариям). Урок: для этого типа резки требуется дополнительная проверка именно текста user_prompt, а не только итогового ответа модели — добавлено как обязательный критерий для всех последующих PRD этой серии.
- 2026-07-13 v4.13 -> v4.14: ИСПРАВЛЕНИЕ. Часть 4 была ошибочно
  зафиксирована в состоянии «после APPLY-8» при передаче Исполнителю
  устаревшего файла, хотя PRD-047.42-APPLY-9 и
  PRD-047.42-APPLY-10 уже были приняты. Актуально для реального
  состояния репозитория (последний принятый вариант — APPLY-10),
  проверено архитектором заново по GitHub независимо от старой записи
  в документе. Механика debug-патча для кластеров с привязкой к
  состоянию _call_llm подтверждена работоспособной на APPLY-10 (полное
  побайтовое совпадение self.last_debug), можно применять и дальше без
  повторного доказательства с нуля.
- 2026-07-10 v4.12 -> v4.13: PRD-047.42-APPLY-8 принят ACCEPTED.
  Устаревающий структурный тест из APPLY-6 починен через замороженный
  baseline (коммит e5f5f32), происхождение фикстуры проверено
  архитектором независимым пересчётом, не только по заявленному
  provenance-полю. Default-поведение build_variable_inventory()
  доказано побайтово неизменным. Правило "contract-тесты сверяются с
  замороженным снимком, не с live-пересчётом" закреплено как общее,
  не разовое. Границы следующего кластера (request_detectors_and_
  mvp_override_block) пересчитаны архитектором в текущем файле:
  275-348 вместо устаревших 333-406 из исходной карты APPLY-6.
- 2026-07-10 v4.11 -> v4.12: PRD-047.42-APPLY-7 (_call_llm slice 1,
  2 чистых кластера, typed dataclass) принят ACCEPTED_WITH_WARNINGS.
  Поведение доказано идентичным (snapshot 3 сценариев). Найден
  структурно устаревающий тест из PRD-047.42-APPLY-6 (переписывается
  первым в новой сессии). Урок: контрактные тесты для boundary-mapping
  PRD должны сверяться с замороженным снимком, а не пересчитывать
  структуру заново — иначе не переживут последующую резку.
- 2026-07-10 v4.8 -> v4.9: PRD-047.42-APPLY-4 (writer_agent.py slice 3,
  mixin, 8 self-методов + 3 константы) принят ACCEPTED. Четвёртый
  успешный срез god-файла подряд, второй раз укрупнённым шагом. Дальше —
  первый по-настоящему рискованный кусок (write()/runtime settings),
  шаг снова сужается.
- 2026-07-10 v4.9 -> v4.10: PRD-047.42-APPLY-5 (writer_agent.py slice 4,
  write()/_resolve_runtime_settings) принят ACCEPTED. Пятый успешный
  срез god-файла. Найдена и починена скрытая monkeypatch-зависимость
  между модулями — пример ценности контрактных тестов из PRD-047.42.
  Далее — начало разбора _call_llm (803 строки), несколькими шагами.
- 2026-07-10 v4.7 -> v4.8: PRD-047.42-APPLY-3 (writer_agent.py slice 2,
  8 static-методов) принят ACCEPTED. Третий успешный срез god-файла.
  Владелец подтвердил укрупнение шагов декомпозиции там, где безопасно
  (mixin-перенос class-методов) — зафиксировано как рабочий принцип.
- 2026-07-09 v4.6 -> v4.7: PRD-047.42-APPLY-2 (writer_agent.py slice 1,
  4 pure-функции) принят ACCEPTED. Второй успешный срез god-файла:
  0 изменений в writer_contract.py и всех 11 admin-файлах, 1 тест-fail
  подтверждён как pre-existing, не регрессия.
- 2026-07-09 v4.5 -> v4.6: PRD-047.42-APPLY (admin_routes.py
  decomposition, Stage 2a) принят ACCEPTED. Первая реальная резка
  god-файла завершена чисто: 10 модулей, 0 расхождений на полном
  снимке 77 route-кейсов, внешний контракт (main.py) не тронут.
  Следующие по очереди — writer_agent.py, затем writer_contract.py.
- 2026-07-09 v4.4 -> v4.5: добавлена красная линия (Часть 2) против
  запуска TO_DO_LIST/tools/run_prd_*.py вне контекста реальной задачи —
  два ложных алярма за одну сессию (PRD-046.1.30 отчёты, затем PRD-047.42
  отчёты) вызваны именно этим паттерном.
- 2026-07-09 v4.3 -> v4.4: PRD-047.42 (God-File Boundary Mapping,
  Stage 1) принят ACCEPTED. Точные карты границ для 3 god-файлов
  построены read-only, 0 изменений в исходниках подтверждено
  независимо. Stage 2 (перенос кода) выделен в отдельный
  PRD-047.42-APPLY, 19 diagnostic_center-файлов — в PRD-047.42b.
- 2026-07-09 v4.2 -> v4.3: PRD-047.41 (Flag Consolidation) принят
  ACCEPTED_WITH_WARNINGS. effective_config_registry_v1 построен на
  103/103 флага, secret-защита подтверждена, bucket A/B закрыты,
  LEGACY_PIPELINE_ENABLED отложен до PRD-047.43. Зафиксирован и закрыт
  ложный алярм от устаревшего gate-скрипта PRD-046.1.30.
- 2026-07-08 v4.1 -> v4.2: PRD-047.40 (Dead Pipeline Removal) принят
  ACCEPTED_WITH_WARNINGS. Обновлён статус курса, закрыт user_level_adapter
  unclear_needs_trace -> active, зафиксированы 3 отложенных follow-up.
- 2026-07-03 v4.0 -> v4.1: применены 14 правок MP-1..MP-14 по аудиту Fable
  R08 (закон-карта, канон чек-листа R07, дверь-как-свойства, ставки телом,
  буфер warnings, швы Эпохи 1, атрибуция философии, старшинство и этот журнал).

---

# ЧАСТЬ 5 — ЭПОХА 1: КОНСОЛИДАЦИЯ

## 5.1 Цель и лозунг

```text
Продукт уже скрыл сложность от пользователя (v2).
Теперь система должна скрыть сложность от самой себя.
```
Не "красивый код" — операционное требование: скорость и надёжность будущих
PRD не должны деградировать по мере роста проекта.

## 5.2 North Star

```text
Любой новый разработчик понимает, что активно в системе, за один проход
по docs/PROJECT_STATE.md — без археологии по сотням PRD и grep "legacy".
Любое изменение runtime требует правки одного очевидного места, не поиска
среди 103 флагов. Репозиторий весит пропорционально коду, не истории.
```

## 5.3 Последовательность PRD (кодовые — строго линейно; 044 параллелится, см. §5.4)

Кодовые PRD 047.40-047.43 далее именуются Phase B (термин унаследован от v3,
используется в §3 и §5.7).

```text
PRD-047.40  Dead Pipeline Removal
   Scope: физическое удаление 13 dead_confirmed компонентов
   (answer_basic/sag_aware/graph_powered + SD-цветовая промпт-система).
   Исключить user_level_adapter (unclear_needs_trace) до отдельной
   трассировки. Включает предварительный шаг: raw-log untrack
   (532 пути, 63.2MB, манифест PRD-047.39 — APPROVED владельцем/Architect).
   Gate: full regression + live smoke; test_no_legacy.py -> test_dead_code_removed.

PRD-047.41  Flag Consolidation
   Scope: 103 env-флага -> единый effective_config реестр со статусами
   active_tunable / frozen_default_only / retired.
   Gate: admin runtime показывает те же эффективные значения до/после.

PRD-047.42  God-File Decomposition
   Scope: разбиение по ответственности writer_agent.py (2188),
   admin_routes.py (2145), writer_contract.py (979) + 19 production_runtime
   diagnostic_center_* файлов.
   КОНСТРЕЙНТ: 60 diagnostic_center_* файлов зоны
   permanent_quality_eval_regression (по PRD-046.1.29 classification.json)
   ИСКЛЮЧЕНЫ из "разбить" — они архивируются, не рефакторятся.
   Gate: построчный diff поведения через contract-test набор.

PRD-047.43  Admin UI Dedup
   Scope: удаление "Advanced Controls / duplicate legacy sub-tabs",
   уже помеченных compatibility-only в PROJECT_STATE.
   Gate: browser/screenshot proof покрытия всех реальных use-case.

PRD-047.44  Docs of Record vs Archive Split (можно параллельно с B)
   Scope: PROJECT_STATE.md/DECISIONS.md = текущая истина; сотни PRD-файлов
   архивируются с единым индексом-таймлайном.
   ЗАДАЧА СЮДА: проставить в PROJECT_STATE явную строку, что
   "Концептуальная структура бота.md" — исторический документ (вердикт R05).

PRD-047.45  Test Suite Health Audit (после 047.40)
   Scope: полный pytest tests -q без collection errors; внешне-API тесты
   явно skip с причиной, не падают импортом.

PRD-047.46  Consolidation Freeze / Re-Gate
   Scope: повторный прогон гейта PRD-047.38 НА КОНСОЛИДИРОВАННОМ коде.
   Доказывает: чистка не сломала product-поведение.
```

## 5.4 Правила очерёдности

```text
- 040 -> 041 -> 042 -> 043 строго последовательны (каждый стартует с
  чистого git tree, иначе diff нечитаем).
- 044 (docs) параллелится с Phase B — не пересекается по файлам.
- 045 (test health) — после 040 (тесты ссылаются на удаляемые pipelines).
- 046 — обязательно последним, после всего.
```

## 5.5 Оценка

```text
7 базовых PRD (047.40-047.46) + 3-6 ожидаемых хотфиксов = ~10-13 итераций, ~3-6 недель
при текущем темпе. Не commitment — honest estimate. BLOCKER добавляет
narrow HF и честно увеличивает счётчик.
```

## 5.6 DEFINITION OF DONE Эпохи 1 (жёсткая граница против бесконечной чистки)

```text
- [ ] 0 dead_confirmed компонентов неразобранных (удалены или
      реклассифицированы как compat_shim_active).
- [ ] Все env-флаги имеют задокументированный статус в едином реестре.
- [ ] Ни один активный файл в bot_agent/multiagent/, api/ не >~1200 строк
      без явно записанной причины.
- [ ] pytest tests -q без collection errors (внешне-API тесты skip
      с причиной).
- [ ] git без process-артефактов в индексе; мёртвые ветки удалены.
- [ ] Один документ (PROJECT_STATE.md) как точка входа для нового
      разработчика.
- [ ] Незакрытых warnings по линии 047.40-046 не больше N (N фиксируется
      владельцем до старта 047.40 и не пересматривается); новый warning
      при полном буфере требует закрытия старого. Дверь эпохи не пропускает
      накопленный хвост (П9 уже активен: три подряд accepted_with_warnings
      в линии 047.37-39).
- [ ] PRD-047.46 (re-gate) вернул ACCEPTED / ACCEPTED_WITH_WARNINGS.
```
**Как только все пункты выполнены — Эпоха 1 закрыта. Дальше НЕ чистим,
даже если остались несовершенства. Новые находки -> backlog Эпохи 2.**

## 5.7 Риск-регистр Эпохи 1

(Литеры I/J/F/G унаследованы от регистра v3; пропуски означают закрытые
риски, их полный список — в архиве v3.)

```text
Risk I  Консолидация становится бесконечной.
  -> Definition of Done зафиксирован заранее; новые находки в backlog.
Risk J  Phase B меняет порядок ради удобства.
  -> Держать 040->041->042->043 строго без re-plan этого документа.
Risk F  История git переписывается ради места (filter-repo/BFG).
  -> Reject внутри консолидации; commit_hash из PROJECT_STATE должны
     оставаться разрешимы. Rewrite — отдельный явный PRD, если вообще.
Risk G  Инвентаризация тихо превращается в удаление в том же PRD.
  -> dead_confirmed -> только отчёт -> отдельный PRD на удаление.
```

---

# ЧАСТЬ 6 — ШЛЮЗ МЕЖДУ ЭПОХАМИ

Эпоха 2 не начинается, пока НЕ выполнены ОБА условия:

```text
Условие 1: Definition of Done Эпохи 1 (§5.6) полностью закрыт.
Условие 2: Ратификация корпуса законов Fable проведена владельцем
           (RATIFICATION_FABLE_LAW_CORPUS_v1.md — каждый закон помечен
           принято / принято с правкой / отвергнуто).
```

Обоснование условия 2 (из R07, часть 2, находка А): корпус из 40+
инвариантов написан целиком одной модельной семьёй, которая будет его же
исполнять и проверять — это риск на конституционном уровне (П10, П14).
Конституция, с которой ни разу не спорили, — декор. Отклонение хотя бы
пары законов — здоровый сигнал, не провал. Прецедент: поправка владельца
про "пилот" в R01 была таким актом и оказалась продуктивнейшим моментом
серии.

Во время Эпохи 1 в счёт Эпохи 2 разрешены ровно два дешёвых акта (такт 0):
строка исторического статуса для "Концептуальной структуры бота" в
PROJECT_STATE (входит в PRD-047.44) и уже принятая конвенция "нет новых
постоянных модулей". Всё остальное ждёт шлюза.

---

# ЧАСТЬ 7 — ЭПОХА 2: "МОЗГИ"

Стартует после шлюза. Управляется корпусом законов; полные тексты — в
файлах FABLE_STRATEGIC_RESPONSE_02..07 и FABLE_LAW_INDEX. Здесь — костяк.

## 7.1 Иерархия законов (R07)

```text
ГРУНТ (свойства среды, против которых написан корпус):
  П19 нулевая цена сложности · П14 замкнутый контур аттестации ·
  П25 одна психика как ground truth · П4 гравитация именованного

4 КОРНЯ:
  ФОРМА  — артефакт, не орган (сущности живут как данные+контракты,
           не как именованные модули с папками).
  ВЛАСТЬ — один автор, остальные советуют/свидетельствуют (Writer —
           единственный автор; суждения о человеке только у рамки;
           всё через границу декларативно).
  РОСТ   — только по записанному провалу, с потребителем и сроком.
  ИСТИНА — свидетельство несёт сорт; сорт повышает только человек.

УЗЕЛ: fixture-закон (данные + один прогонщик по требованию, срок годности,
      никогда runtime, никогда постоянный модуль) — пересечение
      Форма+Рост+Истина, не отдельный корень.

Законы по слоям:
  И1-И5 (знания, R03) · В1-В4 (восприятие, R04) · Л1-Л5 (лаборатория, R05)
  · Г1-Г3 (голос, R06).
Антипаттерны: А1-А7, Р1-Р7, ЛА1-ЛА5, Г-А1-Г-А4, П1-П25.

Закон-карта (одна строка на закон; полный текст — по карте файлов §10):
И1 один маленький закрытый словарь — общий язык запросной и знаниевой
   сторон; рост словаря — только контрактным событием.
И2 знание — материал, не директива; императив разрешён только жёстким
   границам закрытого списка; Writer вправе игнорировать всё.
И3 каждое поле/метаданное имеет названного потребителя; write-only нет.
И4 рост схемы/словаря/слоёв — только против записанного провала-фикстуры.
И5 первоисточник всегда достижим: производное несёт путь к фрагменту
   корпуса; выборочная сверка — человеком.
В1 одна рамка, один производитель; вторых судей о человеке нет.
В2 рамка — уценённая гипотеза: confidence + reason-цитата слов человека;
   latest turn опровергает без переговоров; без свежего подтверждения —
   структурная уценка.
В3 словари восприятия закрыты, версионируются вместе со словарём механизмов.
В4 рамка декларативна и удаляема: без неё ответ валиден, лишь консервативнее.
Л1 лаборатория свидетельствует, не решает; ничего не блокирует сама.
Л2 видимый ответ — артефакт; согласованность проверяется по границе
   ДОСТАВКИ, не использования (Н1).
Л3 свидетельство несёт сорт [внутр./внешн.] и происхождение (живой провал /
   придуманный сценарий); сорт повышает только человек.
Л4 мера успеха evidence-машины — цена человеческого взгляда, не число проверок.
Л5 контракт — о поведении на видимой границе; ноль имён внутренних стадий.
Г1 механизменная грамматика лицензируется рамочной гипотезой этого хода;
   без лицензии — грамматика присутствия (обязательство идентичности Writer'а).
Г2 живость = след слов, введённых человеком самим, не эха бота (Н2);
   реплика, переносимая в любой диалог, — штамп.
Г3 у живости нет эталонного стиля; она определяется только негативно.
Статус Writer Move Compliance (R04 §4): либо читает рамку, либо жёсткая
граница закрытого списка — третьего статуса не существует.

3 исправленные нестыковки (R07): Н1 (согласованность по границе доставки,
не использования — защищает право Writer'а игнорировать знание),
Н2 (Г2 наследует происхожденческую оговорку Р7), Н3 (лицензия Г1
асимметрична, В4 цела).
```

## 7.2 Ключевые вердикты корпуса (чтобы не переоткрывать)

```text
- Diagnostic Center v2 НЕ строить. Восприятие = рамка хода как артефакт,
  производимая одной функцией за ход (В1). "Концептуальная структура бота"
  (7 слоёв) — исторический документ.
- Bot Quality Lab как сущность НЕ нужна. Нужен один канал свидетельств:
  видимый ответ = артефакт, фикстура согласованности trace<->пакет<->ответ.
- Центр дизайна знаний — маленький закрытый словарь-посредник, НЕ schema
  и НЕ retrieval. "Умнее" живёт в общем языке двух сторон, не в чанках.
- Wake Up DB v1.1 — брать 8 полей ядра; writer_instruction опасен
  (грамматически дрейфует в императив, рецидив 047.35).
- Голос: канцелярит — приор модели (доказано trace ЧАТ_13), не баг входа.
  Три инварианта Г1-Г3. Голос не добавляется инструкцией "будь живым"
  поверх осадка — сначала снять осадок writer-контракта, потом собрать
  идентичность с Г-обязательствами внутри.
```

## 7.3 Дорожная карта — 5 тактов (R07 §3)

```text
Такт 0 (во время Эпохи 1): только 2 акта (см. §6). Всё прочее ждёт.

Такт 1 — Контрактный акт. ОДИН PRD, точка слияния всей серии.
  Словарь механизмов v1 + словари восприятия + контракт рамки
  (поля->3 потребителя) + контракт пакета (4 отсека, бюджет дефицита)
  + Behavior Contract (страница-две на видимой границе, владелец
  утверждает построчно, ни одного имени внутренней стадии). Ноль кода.

Такт 2 — Baseline. Полный параллелизм. 3 вида фикстур руками владельца:
  (а) retrieval из живых чатов, (б) эталонные рамки на диалогах,
  (в) ручная лаборатория 3-5 сценариев в Web Chat. Продукт: измеренная
  отправная точка (защита от П20 — сначала измерь).

Такт 3 — Три переплавки.
  (а) Пилотная партия фабрики: один механизменный кластер в новую схему,
      ревью владельца, похороны general_text-предков в той же партии.
  (б) Переплавка восприятия: один производитель рамки, публичные выходы
      тройки (State Analyzer / Thread Manager / Diagnostic Card — см.
      pipeline Части 1) хоронятся, preservation зелёный.
  (в) Пересборка идентичности Writer'а: снятие осадка writer-контракта
      (это же плановая консолидация god-file), Г-обязательства внутрь.
  ПОРЯДОК: (в) СТРОГО после (б) — Г1 ссылается на рамку, которой до (б)
  нет. (а) и (б) при одном владельце безопаснее последовательно.

Такт 4 — Стыковка. Вторая точка слияния. Формирование запроса читает
  рамку и говорит словарём. Строго после (а) и (б). Здесь решается
  центральная ставка Г-1.

Такт 5 — Рутина контура обучения: цикл провал->фикстура->партия/правка
  ->замер. Сквозной прогонщик 3-5 сценариев только теперь.

ПРАВИЛО: полный reindex не является шагом ни на одном такте.
```

## 7.4 Ставки, принятые на веру (проверяются практикой, не спором)

```text
Г-1 (центральная): словарь механизмов реально поднимает точность retrieval.
    Решается тактами 3(а)+4. Нет измеримого выигрыша — центр дизайна R03
    ПЕРЕСМАТРИВАЕТСЯ, не дожимается партиями.
Г-2: один производитель рамки держит качество без промежуточных публичных
    выходов; богатого trace хватает для отладки. — такт 3(б).
Г-3: грамматический класс «объяснение» детектируем без списков фраз;
    если нет — проверка Г1 честно остаётся человеческой. — такты 2(в)/5.
Г-4: партийная миграция с похоронами предшественников выполнима без
    поломок. — первая партия такта 3(а).
Г-5: живых чатов одного владельца хватает как источника фикстур; быстрое
    «всё зелёное» — потолок входа, не успех. — такты 2/5.
Г-6: уценка гипотез (В2) реально гасит петлю Р7 в живых диалогах. — фикстуры
    удержания/отпускания нити.
Г-7: осадок writer-контракта — второй источник канцелярита; если после
    снятия осадка механистичность цела — источник в приорах, вся нагрузка
    на Г1. — такт 3(в).
Г-8: потолок глубины рамки реально управляет дозировкой. — сквозные
    фикстуры такта 5.
В акт ратификации входит подпись под этим списком: «принимаю как
непроверенное» — владелец расписывается не только в согласии с законами,
но и в знании, где корпус стоит на вере.
```

## 7.5 Определение прибытия Эпохи 2 (чтобы у эры была дверь, П13)

```text
Эра «мозгов» реализована, когда одновременно верны пять СВОЙСТВ (не шагов):
1. Обе стороны говорят словарём, и это видно на фикстурах против baseline.
2. Рамку производит один производитель; вторых судей о человеке — ноль.
3. Пакет живёт по контракту; идентичность Writer'а собрана без осадка,
   с Г-обязательствами внутри.
4. Контур обучения крутится как рутина, и фикстуры РЕАЛЬНО меняют
   приоритеты партий (если ни один приоритет не изменился — контур
   декоративен).
5. Сквозные фикстуры ловят расхождения доставки.
В момент объявления эры завершённой записывается условие входа в следующую
фазу (П13) — у любой отложенной фазы, включая safety, должна быть дверь.
Дальше — не новая эра чистки, а установившийся цикл.
```

---

# ЧАСТЬ 8 — ЕДИНЫЙ OWNER-ЧЕК-ЛИСТ (канон R07, предмет ратификации)

Владелец задаёт себе при каждом крупном решении в любой эпохе. Ответ
дольше десяти секунд — сам по себе найденный паттерн. Это канонический
чек-лист R07 дословно — он же предмет ратификации; три «архитектурных»
вопроса, которые могли бы сюда попасть, живут в теле плана (постоянный
модуль — конвенция Части 2; имя стадии в контракте — Л5 в такте 1 и §7.2;
живость числом — Г-карта в §7.1), не в каноне.

```text
ФОРМА:
1. Я даю этому имя, папку или модуль? Может ли это жить как данные+контракт?
2. Это контракт или контент? Контракт -> PRD; контент -> данные с журналом.
3. Если это «новый способ» — что хоронится в том же акте? (П1/П2)
ВЛАСТЬ:
4. Может ли Writer это проигнорировать, и останется ли ответ валидным?
5. Не появился ли второй судья о человеке — или читатель чужого
   промежуточного выхода?
6. Есть ли место, где слово пользователя, вес на чанке или список фраз
   решает напрямую? (rule engine во всех формах)
РОСТ:
7. Какой записанный провал это закрывает? Нет фикстуры — нет изменения.
8. Кто названный потребитель, и когда/от чего это умрёт?
9. Заплатил бы я за эту работу подрядчику по рыночной ставке?
ИСТИНА:
10. Это свидетельство — [внутр.] или [внешн.]? Что из отчётов я проверил
    руками на этой неделе?
11. Это измеримо на артефакте до Writer'а — или только по ощущению ответа?
12. Что в этой проверке способно меня удивить? Реплика/фикстура привязана
    к тому, что человек ввёл сам, — или к тому, чему его научил бот?
Еженедельная строка (П20): что система теперь умеет / что теперь нельзя
сломать — чего не было неделю назад. Пусто две недели подряд — остановка.
```

---

# ЧАСТЬ 9 — ДОЛГОСРОЧНЫЕ ПРЕДОСТЕРЕЖЕНИЯ (из R07, часть 2)

Не блокируют работу, но должны звучать в нужный момент:

```text
Б. "Вода из одного колодца": весь корпус учится на одном человеке
   (владелец = автор знаний + единственный собеседник + разметчик +
   читатель свидетельств). У Эпохи 2 есть ПОТОЛОК, заданный энтропией
   входа, не архитектурой. КОГДА метрики тактов 4-5 выйдут на плато —
   читать это в ПЕРВУЮ очередь как "потолок входа достигнут", не "нужен
   ещё закон/партия/поле". Строка в PROJECT_STATE (внести при такте 1):
   "плато внутренних метрик — кандидат-сигнал достигнутого потолка входа,
   не дефицита законов."

В. Формат внешнего аудита создаёт слепоту: внешний взгляд видит проект
   только глазами того, кого аудирует. Практика на будущее: иногда
   давать внешнему взгляду сырьё БЕЗ рамки вопроса ("вот материал, что
   видишь?"). Неоформленный аудит ловит то, что оформленный не может.

Г. Метрика вне всех законов: отношение часов строительства к часам
   использования. Раз в неделю — час без трейса и без фикстуры после,
   просто разговор с ботом как с ботом. Эра "мозгов" состоится не когда
   позеленеют фикстуры такта 5, а когда этот час перестанет быть
   дисциплиной. Это не пункт-задача — это напоминание о назначении.
```

---

# ЧАСТЬ 10 — КАРТА ФАЙЛОВ (куда смотреть за деталями)

```text
Этот документ (v4)                          — единая точка входа, ось времени.
docs/PROJECT_STATE.md                       — текущая истина по коду (в repo).
docs/PRD_INDEX.md                           — индекс всех PRD (в repo).
RATIFICATION_FABLE_LAW_CORPUS_v1.md         — чек-лист ратификации (шлюз).
  [пути: все FABLE_*.md и RATIFICATION_*.md лежат в корне репозитория рядом
   с этим планом; RESPONSE_02..08 и LAW_INDEX — там же. Точка входа не имеет
   права отправлять читателя на поиск — если файл перемещён, обновить эту карту.]
FABLE_LAW_INDEX_AND_WORKFLOW_STATE_v1.md    — сжатая карта корпуса законов.
FABLE_STRATEGIC_RESPONSE_02..07             — полные тексты законов Эпохи 2.
FABLE_STRATEGIC_RESPONSE_03/04/05           — sequence-детали тактов.
TO_DO_LIST/logs/PRD-XXX/                     — отчёты Executor'а по каждому PRD.
TO_DO_LIST/logs/PRD-046.1.29/               — classification.json для §5.3/042.
Кузница Духа v.2 / chuvstvo_do_slova        — первоисточник, суть Writer'а
                                              (такт 3в, акт авторства владельца).
```

---

# ЧАСТЬ 11 — ОДНОЙ ФРАЗОЙ

Проект идёт через две эпохи: сначала убрать исторический мусор до чистой
единой системы (Эпоха 1, линейная серия PRD с жёстким Definition of Done),
затем — только после шлюза с обязательной ратификацией законов — собрать
"мозги" бота по проверенному корпусу инвариантов (Эпоха 2, пять тактов).
Всё, что защищает от переусложнения, уже встроено: заранее зафиксированные
границы завершения, запрет на именованные органы и постоянные модули, и
единый чек-лист из двенадцати вопросов, ловящий уход в тупик до того, как
он стоит недели.
