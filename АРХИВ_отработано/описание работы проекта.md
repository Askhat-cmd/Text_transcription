

## 🎯 **Как работают системы вместе**

### **1. Две параллельные системы извлечения знаний**

```
┌─────────────────────────────────────────────────────────────┐
│               SARSEKENOV_PROCESSOR (главный процессор)      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────┐    ┌─────────────────────────┐   │
│  │   SAG v2.0 ГРАФ      │    │  НОВЫЕ ЭКСТРАКТОРЫ      │   │
│  │   КОЛЛЕКЦИЯ          │    │  (5 этапов)             │   │
│  │  (встроенная)        │    │  (внешние модули)       │   │
│  └──────────────────────┘    └─────────────────────────┘   │
│           │                             │                   │
│           ▼                             ▼                   │
│  ┌───────────────────┐      ┌──────────────────────────┐   │
│  │ Автоматический    │      │ LLM-based извлечение     │   │
│  │ анализ текста     │      │ через OpenAI GPT         │   │
│  │ (rule-based +     │      │ (prompt engineering)     │   │
│  │ graph matching)   │      │                          │   │
│  └───────────────────┘      └──────────────────────────┘   │
│           │                             │                   │
│           ▼                             ▼                   │
│  ┌───────────────────┐      ┌──────────────────────────┐   │
│  │ graph_entities    │      │ safety, causal_chains,   │   │
│  │ semantic_links    │      │ concept_hierarchy,       │   │
│  │ (встроенные)      │      │ case_studies,            │   │
│  │                   │      │ prerequisites            │   │
│  └───────────────────┘      └──────────────────────────┘   │
│           │                             │                   │
│           └──────────┬──────────────────┘                   │
│                      ▼                                      │
│            ┌─────────────────────┐                          │
│            │ ФИНАЛЬНЫЙ JSON      │                          │
│            │ (все данные вместе) │                          │
│            └─────────────────────┘                          │
└─────────────────────────────────────────────────────────────┘
```


### **2. SAG v2.0 Граф-Коллекция (442 узла + 259 отношений)**

**Что это:**

- Встроенная в `sarsekenov_processor.py` коллекция узлов и отношений
- **Автоматическое извлечение** через сопоставление с текстом (rule-based)
- Работает **без дополнительных API-запросов**

**Как работает:**

```python
# В sarsekenov_processor.py:
self.graph_nodes = {
    "нейросталкинг", "метанаблюдение", "осознавание", ...  # 442 узла
}

self.graph_relationships = {
    "ведет_к", "включает_в_себя", "используется_для", ...  # 259 отношений
}

# Извлечение (автоматическое):
def extract_graph_entities(self, content: str, keywords: list) -> list:
    entities = []
    content_lower = content.lower()
    
    # 1. Поиск узлов в тексте
    for concept in self.graph_nodes:
        if concept in content_lower:
            entities.append(concept)
    
    # 2. Нормализация через синонимы
    normalized = self._normalize_graph_entities(entities)
    
    # 3. Приоритизация (концепты нейросталкинга выше)
    return normalized[:30]  # Топ-30 сущностей
```

**Что извлекает:**

- `graph_entities`: массив топ-30 сущностей из 442 узлов
- `semantic_relationships`: связи между сущностями из 259 отношений

**Пример результата:**

```json
{
  "graph_entities": ["метанаблюдение", "осознавание", "паттерны", ...],
  "semantic_relationships": {
    "conceptual_links": [
      {"source": "метанаблюдение", "target": "осознавание", "strength": 0.85}
    ],
    "causal_links": [...],
    "practical_links": [...]
  }
}
```


***

### **3. Новые Экстракторы (5 этапов) - LLM-based**

**Что это:**

- Внешние модули в `text_processor/extractors/`
- Работают через **OpenAI GPT API** (prompt engineering)
- Извлекают **специализированные структуры**

**Как работает:**

```python
# В sarsekenov_processor.py (инициализация):
self.safety_extractor = SafetyInformationExtractor(client=self.client, model='gpt-4o-mini')
self.causal_extractor = CausalChainExtractor(client=self.client, model='gpt-4o-mini')
# ... и другие

# Вызов (для каждого блока):
for block in all_blocks:
    # 1. Safety информация
    safety_info = self.safety_extractor.extract(
        block_content=block["content"],
        practice_or_concept_name=block["title"]
    )
    block["safety"] = safety_info
    
    # 2. Причинно-следственные цепочки
    causal_data = self.causal_extractor.extract(block_content=block["content"])
    block["causal_chains"] = causal_data["processes"]
    
    # ... и другие экстракторы
```

**Какие экстракторы:**

1. **SafetyExtractor** → `safety` поле
2. **CausalChainExtractor** → `causal_chains` поле
3. **ConceptHierarchyExtractor** → `concept_hierarchy` поле
4. **CaseStudyExtractor** → `case_studies` поле
5. **PrerequisiteExtractor** → `prerequisites` поле

**Пример результата:**

```json
{
  "safety": {
    "contraindications": ["острый психоз"],
    "when_to_stop": ["усиление симптомов"]
  },
  "causal_chains": [
    {
      "name": "процесс исцеления",
      "steps": [...]
    }
  ],
  "concept_hierarchy": [...],
  "case_studies": [...],
  "prerequisites": {...}
}
```


***

### **4. Где сохраняются данные**

**Все данные сохраняются в одном финальном JSON:**

```
data/sag_final/{year}/{month}/{date}_{video_id}_{title}.for_vector.json
```

**Структура:**

```json
{
  // Метаданные документа
  "document_metadata": {
    "schema_version": "2.0",
    "total_graph_nodes": 442,      // 🆕 Из SAG v2.0
    "total_graph_relationships": 259  // 🆕 Из SAG v2.0
  },
  
  // Граф-сущности на уровне документа (SAG v2.0)
  "graph_entities": [["метанаблюдение", 5], ["осознавание", 3], ...],
  "semantic_relationships": {...},  // 🆕 Связи между сущностями
  
  // Блоки
  "blocks": [
    {
      "block_id": "...",
      "content": "...",
      
      // ═══ SAG v2.0 Граф-Коллекция (автоматическое) ═══
      "graph_entities": ["метанаблюдение", "осознавание", ...],
      "semantic_relationships": {
        "conceptual_links": [...],
        "causal_links": [...],
        "practical_links": [...]
      },
      
      // ═══ Новые Экстракторы (LLM-based) ═══
      "safety": {
        "contraindications": [...],
        "limitations": [...],
        "when_to_stop": [...],
        "when_to_seek_professional_help": [...],
        "notes": []
      },
      
      "causal_chains": [
        {
          "name": "процесс исцеления",
          "steps": [...],
          "intervention_points": [...]
        }
      ],
      
      "concept_hierarchy": [
        {
          "name": "осознавание",
          "level": "domain",
          "parent": "нейросталкинг",
          "relationship": "is_core_component_of"
        }
      ],
      
      "case_studies": [...],
      "prerequisites": {...}
    }
  ]
}
```


***

### **5. Ключевые отличия систем**

| Аспект | SAG v2.0 Граф-Коллекция | Новые Экстракторы |
| :-- | :-- | :-- |
| **Метод** | Rule-based matching + морфология | LLM-based (GPT промпты) |
| **API** | Нет (встроенная система) | Да (OpenAI GPT API) |
| **Скорость** | Мгновенно | ~2-5 секунд на блок |
| **Точность** | Высокая для известных терминов | Высокая для сложных структур |
| **Затраты** | Бесплатно | ~\$0.01-0.05 за блок |
| **Что извлекает** | Сущности + связи | Специализированные структуры |
| **Где в коде** | `sarsekenov_processor.py` | `text_processor/extractors/` |


***

### **6. Зачем две системы?**

**Дополняют друг друга:**

1. **SAG v2.0 Граф** → Быстрое извлечение основных концептов
    - Хорошо для: ключевых терминов, базовых связей
    - Плохо для: сложных структур, контекста
2. **Новые Экстракторы** → Глубокий анализ специализированных структур
    - Хорошо для: безопасности, причинно-следственных цепочек, иерархии
    - Плохо для: скорости, затрат

**Результат:**

- **Полнота**: Извлекаются и простые термины, и сложные структуры
- **Оптимизация**: Дорогие LLM-запросы только для специализированных задач
- **Качество**: Два независимых подхода валидируют друг друга

***

### **7. Настройка экстракторов**

**В `config.yaml`:**

```yaml
pipeline:
  sag_v2:
    # Включение/выключение каждого экстрактора
    use_safety_extractor: true
    use_causal_chain_extractor: true
    use_concept_hierarchy_extractor: true
    use_case_study_extractor: true
    use_prerequisite_extractor: true
    
    # Модели для каждого экстрактора
    models:
      safety: gpt-4o-mini
      causal: gpt-4o-mini
      hierarchy: gpt-4o-mini
      case_study: gpt-4o-mini
      prerequisite: gpt-4o-mini
```

**Гибкость:**

- Можно отключить любой экстрактор → экономия API-запросов
- Можно использовать разные модели → баланс качества/стоимости
- SAG v2.0 Граф всегда работает (встроенный)

***

 Обе системы работают параллельно и дополняют друг друга, создавая максимально полную картину знаний из лекций! 🚀

